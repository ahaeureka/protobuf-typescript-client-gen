"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v3.21.12
// source: file_storage.proto
Object.defineProperty(exports, "__esModule", { value: true });
exports.FileStorageServiceClientImpl = exports.FileStorageServiceServiceName = exports.ReadTextFileResponse = exports.ReadTextFileRequest = exports.GetPathInfoResponse = exports.GetPathInfoRequest = exports.ListFolderResponse = exports.ListFolderRequest = exports.PathEntry = exports.PathSelector = exports.VirtualPathRef = exports.FileInfo = exports.CallbackResult = exports.GetFileInfoRequest = exports.ListFilesResponse = exports.ListFilesRequest = exports.DeleteFileRequest = exports.DownloadFileChunk = exports.DownloadFileHttpMetadata = exports.DownloadFileRequest = exports.UploadedPartInfo = exports.GetUploadStatusResponse = exports.GetUploadStatusRequest = exports.AbortResumableUploadRequest = exports.PartEtag = exports.CompleteResumableUploadRequest = exports.UploadPartResponse = exports.UploadPartHeader = exports.UploadPartRequest = exports.InitResumableUploadResponse = exports.InitResumableUploadRequest = exports.UploadFileResponse = exports.UploadFileMetadata = exports.UploadFileRequest = exports.PathEntryKind = exports.UploadSessionStatus = exports.protobufPackage = void 0;
exports.uploadSessionStatusFromJSON = uploadSessionStatusFromJSON;
exports.uploadSessionStatusToJSON = uploadSessionStatusToJSON;
exports.pathEntryKindFromJSON = pathEntryKindFromJSON;
exports.pathEntryKindToJSON = pathEntryKindToJSON;
/* eslint-disable */
const wire_1 = require("@bufbuild/protobuf/wire");
const operators_1 = require("rxjs/operators");
const httpbody_1 = require("./google/api/httpbody");
const empty_1 = require("./google/protobuf/empty");
const timestamp_1 = require("./google/protobuf/timestamp");
exports.protobufPackage = "stew.api.v1";
var UploadSessionStatus;
(function (UploadSessionStatus) {
    UploadSessionStatus[UploadSessionStatus["UPLOAD_SESSION_STATUS_UNSPECIFIED"] = 0] = "UPLOAD_SESSION_STATUS_UNSPECIFIED";
    UploadSessionStatus[UploadSessionStatus["UPLOAD_SESSION_STATUS_ACTIVE"] = 1] = "UPLOAD_SESSION_STATUS_ACTIVE";
    UploadSessionStatus[UploadSessionStatus["UPLOAD_SESSION_STATUS_COMPLETED"] = 2] = "UPLOAD_SESSION_STATUS_COMPLETED";
    UploadSessionStatus[UploadSessionStatus["UPLOAD_SESSION_STATUS_ABORTED"] = 3] = "UPLOAD_SESSION_STATUS_ABORTED";
    UploadSessionStatus[UploadSessionStatus["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(UploadSessionStatus || (exports.UploadSessionStatus = UploadSessionStatus = {}));
function uploadSessionStatusFromJSON(object) {
    switch (object) {
        case 0:
        case "UPLOAD_SESSION_STATUS_UNSPECIFIED":
            return UploadSessionStatus.UPLOAD_SESSION_STATUS_UNSPECIFIED;
        case 1:
        case "UPLOAD_SESSION_STATUS_ACTIVE":
            return UploadSessionStatus.UPLOAD_SESSION_STATUS_ACTIVE;
        case 2:
        case "UPLOAD_SESSION_STATUS_COMPLETED":
            return UploadSessionStatus.UPLOAD_SESSION_STATUS_COMPLETED;
        case 3:
        case "UPLOAD_SESSION_STATUS_ABORTED":
            return UploadSessionStatus.UPLOAD_SESSION_STATUS_ABORTED;
        case -1:
        case "UNRECOGNIZED":
        default:
            return UploadSessionStatus.UNRECOGNIZED;
    }
}
function uploadSessionStatusToJSON(object) {
    switch (object) {
        case UploadSessionStatus.UPLOAD_SESSION_STATUS_UNSPECIFIED:
            return "UPLOAD_SESSION_STATUS_UNSPECIFIED";
        case UploadSessionStatus.UPLOAD_SESSION_STATUS_ACTIVE:
            return "UPLOAD_SESSION_STATUS_ACTIVE";
        case UploadSessionStatus.UPLOAD_SESSION_STATUS_COMPLETED:
            return "UPLOAD_SESSION_STATUS_COMPLETED";
        case UploadSessionStatus.UPLOAD_SESSION_STATUS_ABORTED:
            return "UPLOAD_SESSION_STATUS_ABORTED";
        case UploadSessionStatus.UNRECOGNIZED:
        default:
            return "UNRECOGNIZED";
    }
}
var PathEntryKind;
(function (PathEntryKind) {
    PathEntryKind[PathEntryKind["PATH_ENTRY_KIND_UNSPECIFIED"] = 0] = "PATH_ENTRY_KIND_UNSPECIFIED";
    PathEntryKind[PathEntryKind["PATH_ENTRY_KIND_FILE"] = 1] = "PATH_ENTRY_KIND_FILE";
    PathEntryKind[PathEntryKind["PATH_ENTRY_KIND_DIRECTORY"] = 2] = "PATH_ENTRY_KIND_DIRECTORY";
    PathEntryKind[PathEntryKind["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(PathEntryKind || (exports.PathEntryKind = PathEntryKind = {}));
function pathEntryKindFromJSON(object) {
    switch (object) {
        case 0:
        case "PATH_ENTRY_KIND_UNSPECIFIED":
            return PathEntryKind.PATH_ENTRY_KIND_UNSPECIFIED;
        case 1:
        case "PATH_ENTRY_KIND_FILE":
            return PathEntryKind.PATH_ENTRY_KIND_FILE;
        case 2:
        case "PATH_ENTRY_KIND_DIRECTORY":
            return PathEntryKind.PATH_ENTRY_KIND_DIRECTORY;
        case -1:
        case "UNRECOGNIZED":
        default:
            return PathEntryKind.UNRECOGNIZED;
    }
}
function pathEntryKindToJSON(object) {
    switch (object) {
        case PathEntryKind.PATH_ENTRY_KIND_UNSPECIFIED:
            return "PATH_ENTRY_KIND_UNSPECIFIED";
        case PathEntryKind.PATH_ENTRY_KIND_FILE:
            return "PATH_ENTRY_KIND_FILE";
        case PathEntryKind.PATH_ENTRY_KIND_DIRECTORY:
            return "PATH_ENTRY_KIND_DIRECTORY";
        case PathEntryKind.UNRECOGNIZED:
        default:
            return "UNRECOGNIZED";
    }
}
function createBaseUploadFileRequest() {
    return { payload: undefined };
}
exports.UploadFileRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        switch (message.payload?.$case) {
            case "metadata":
                exports.UploadFileMetadata.encode(message.payload.metadata, writer.uint32(10).fork()).join();
                break;
            case "chunk_data":
                writer.uint32(18).bytes(message.payload.chunk_data);
                break;
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseUploadFileRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.payload = { $case: "metadata", metadata: exports.UploadFileMetadata.decode(reader, reader.uint32()) };
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.payload = { $case: "chunk_data", chunk_data: reader.bytes() };
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            payload: isSet(object.metadata)
                ? { $case: "metadata", metadata: exports.UploadFileMetadata.fromJSON(object.metadata) }
                : isSet(object.chunkData)
                    ? { $case: "chunk_data", chunk_data: bytesFromBase64(object.chunkData) }
                    : isSet(object.chunk_data)
                        ? { $case: "chunk_data", chunk_data: bytesFromBase64(object.chunk_data) }
                        : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.payload?.$case === "metadata") {
            obj.metadata = exports.UploadFileMetadata.toJSON(message.payload.metadata);
        }
        else if (message.payload?.$case === "chunk_data") {
            obj.chunkData = base64FromBytes(message.payload.chunk_data);
        }
        return obj;
    },
    create(base) {
        return exports.UploadFileRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseUploadFileRequest();
        switch (object.payload?.$case) {
            case "metadata": {
                if (object.payload?.metadata !== undefined && object.payload?.metadata !== null) {
                    message.payload = { $case: "metadata", metadata: exports.UploadFileMetadata.fromPartial(object.payload.metadata) };
                }
                break;
            }
            case "chunk_data": {
                if (object.payload?.chunk_data !== undefined && object.payload?.chunk_data !== null) {
                    message.payload = { $case: "chunk_data", chunk_data: object.payload.chunk_data };
                }
                break;
            }
        }
        return message;
    },
};
function createBaseUploadFileMetadata() {
    return { filename: "", content_type: "", folder: "", business_context: "" };
}
exports.UploadFileMetadata = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.filename !== "") {
            writer.uint32(10).string(message.filename);
        }
        if (message.content_type !== "") {
            writer.uint32(18).string(message.content_type);
        }
        if (message.folder !== "") {
            writer.uint32(26).string(message.folder);
        }
        if (message.business_context !== "") {
            writer.uint32(34).string(message.business_context);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseUploadFileMetadata();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.filename = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.content_type = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.folder = reader.string();
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.business_context = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            filename: isSet(object.filename) ? globalThis.String(object.filename) : "",
            content_type: isSet(object.contentType)
                ? globalThis.String(object.contentType)
                : isSet(object.content_type)
                    ? globalThis.String(object.content_type)
                    : "",
            folder: isSet(object.folder) ? globalThis.String(object.folder) : "",
            business_context: isSet(object.businessContext)
                ? globalThis.String(object.businessContext)
                : isSet(object.business_context)
                    ? globalThis.String(object.business_context)
                    : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.filename !== "") {
            obj.filename = message.filename;
        }
        if (message.content_type !== "") {
            obj.contentType = message.content_type;
        }
        if (message.folder !== "") {
            obj.folder = message.folder;
        }
        if (message.business_context !== "") {
            obj.businessContext = message.business_context;
        }
        return obj;
    },
    create(base) {
        return exports.UploadFileMetadata.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseUploadFileMetadata();
        message.filename = object.filename ?? "";
        message.content_type = object.content_type ?? "";
        message.folder = object.folder ?? "";
        message.business_context = object.business_context ?? "";
        return message;
    },
};
function createBaseUploadFileResponse() {
    return { file_info: undefined, callback_result: undefined };
}
exports.UploadFileResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.file_info !== undefined) {
            exports.FileInfo.encode(message.file_info, writer.uint32(10).fork()).join();
        }
        if (message.callback_result !== undefined) {
            exports.CallbackResult.encode(message.callback_result, writer.uint32(18).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseUploadFileResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.file_info = exports.FileInfo.decode(reader, reader.uint32());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.callback_result = exports.CallbackResult.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            file_info: isSet(object.fileInfo)
                ? exports.FileInfo.fromJSON(object.fileInfo)
                : isSet(object.file_info)
                    ? exports.FileInfo.fromJSON(object.file_info)
                    : undefined,
            callback_result: isSet(object.callbackResult)
                ? exports.CallbackResult.fromJSON(object.callbackResult)
                : isSet(object.callback_result)
                    ? exports.CallbackResult.fromJSON(object.callback_result)
                    : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.file_info !== undefined) {
            obj.fileInfo = exports.FileInfo.toJSON(message.file_info);
        }
        if (message.callback_result !== undefined) {
            obj.callbackResult = exports.CallbackResult.toJSON(message.callback_result);
        }
        return obj;
    },
    create(base) {
        return exports.UploadFileResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseUploadFileResponse();
        message.file_info = (object.file_info !== undefined && object.file_info !== null)
            ? exports.FileInfo.fromPartial(object.file_info)
            : undefined;
        message.callback_result = (object.callback_result !== undefined && object.callback_result !== null)
            ? exports.CallbackResult.fromPartial(object.callback_result)
            : undefined;
        return message;
    },
};
function createBaseInitResumableUploadRequest() {
    return {
        filename: "",
        content_type: "",
        folder: "",
        total_size: 0,
        part_size: 0,
        business_context: "",
        checksum: "",
    };
}
exports.InitResumableUploadRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.filename !== "") {
            writer.uint32(10).string(message.filename);
        }
        if (message.content_type !== "") {
            writer.uint32(18).string(message.content_type);
        }
        if (message.folder !== "") {
            writer.uint32(26).string(message.folder);
        }
        if (message.total_size !== 0) {
            writer.uint32(32).int64(message.total_size);
        }
        if (message.part_size !== 0) {
            writer.uint32(40).int32(message.part_size);
        }
        if (message.business_context !== "") {
            writer.uint32(50).string(message.business_context);
        }
        if (message.checksum !== "") {
            writer.uint32(58).string(message.checksum);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseInitResumableUploadRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.filename = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.content_type = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.folder = reader.string();
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.total_size = longToNumber(reader.int64());
                    continue;
                }
                case 5: {
                    if (tag !== 40) {
                        break;
                    }
                    message.part_size = reader.int32();
                    continue;
                }
                case 6: {
                    if (tag !== 50) {
                        break;
                    }
                    message.business_context = reader.string();
                    continue;
                }
                case 7: {
                    if (tag !== 58) {
                        break;
                    }
                    message.checksum = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            filename: isSet(object.filename) ? globalThis.String(object.filename) : "",
            content_type: isSet(object.contentType)
                ? globalThis.String(object.contentType)
                : isSet(object.content_type)
                    ? globalThis.String(object.content_type)
                    : "",
            folder: isSet(object.folder) ? globalThis.String(object.folder) : "",
            total_size: isSet(object.totalSize)
                ? globalThis.Number(object.totalSize)
                : isSet(object.total_size)
                    ? globalThis.Number(object.total_size)
                    : 0,
            part_size: isSet(object.partSize)
                ? globalThis.Number(object.partSize)
                : isSet(object.part_size)
                    ? globalThis.Number(object.part_size)
                    : 0,
            business_context: isSet(object.businessContext)
                ? globalThis.String(object.businessContext)
                : isSet(object.business_context)
                    ? globalThis.String(object.business_context)
                    : "",
            checksum: isSet(object.checksum) ? globalThis.String(object.checksum) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.filename !== "") {
            obj.filename = message.filename;
        }
        if (message.content_type !== "") {
            obj.contentType = message.content_type;
        }
        if (message.folder !== "") {
            obj.folder = message.folder;
        }
        if (message.total_size !== 0) {
            obj.totalSize = Math.round(message.total_size);
        }
        if (message.part_size !== 0) {
            obj.partSize = Math.round(message.part_size);
        }
        if (message.business_context !== "") {
            obj.businessContext = message.business_context;
        }
        if (message.checksum !== "") {
            obj.checksum = message.checksum;
        }
        return obj;
    },
    create(base) {
        return exports.InitResumableUploadRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseInitResumableUploadRequest();
        message.filename = object.filename ?? "";
        message.content_type = object.content_type ?? "";
        message.folder = object.folder ?? "";
        message.total_size = object.total_size ?? 0;
        message.part_size = object.part_size ?? 0;
        message.business_context = object.business_context ?? "";
        message.checksum = object.checksum ?? "";
        return message;
    },
};
function createBaseInitResumableUploadResponse() {
    return { upload_id: "", part_size: 0, total_parts: 0, expires_at: undefined };
}
exports.InitResumableUploadResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.upload_id !== "") {
            writer.uint32(10).string(message.upload_id);
        }
        if (message.part_size !== 0) {
            writer.uint32(16).int32(message.part_size);
        }
        if (message.total_parts !== 0) {
            writer.uint32(24).int32(message.total_parts);
        }
        if (message.expires_at !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.expires_at), writer.uint32(34).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseInitResumableUploadResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.upload_id = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.part_size = reader.int32();
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.total_parts = reader.int32();
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.expires_at = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            upload_id: isSet(object.uploadId)
                ? globalThis.String(object.uploadId)
                : isSet(object.upload_id)
                    ? globalThis.String(object.upload_id)
                    : "",
            part_size: isSet(object.partSize)
                ? globalThis.Number(object.partSize)
                : isSet(object.part_size)
                    ? globalThis.Number(object.part_size)
                    : 0,
            total_parts: isSet(object.totalParts)
                ? globalThis.Number(object.totalParts)
                : isSet(object.total_parts)
                    ? globalThis.Number(object.total_parts)
                    : 0,
            expires_at: isSet(object.expiresAt)
                ? fromJsonTimestamp(object.expiresAt)
                : isSet(object.expires_at)
                    ? fromJsonTimestamp(object.expires_at)
                    : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.upload_id !== "") {
            obj.uploadId = message.upload_id;
        }
        if (message.part_size !== 0) {
            obj.partSize = Math.round(message.part_size);
        }
        if (message.total_parts !== 0) {
            obj.totalParts = Math.round(message.total_parts);
        }
        if (message.expires_at !== undefined) {
            obj.expiresAt = message.expires_at.toISOString();
        }
        return obj;
    },
    create(base) {
        return exports.InitResumableUploadResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseInitResumableUploadResponse();
        message.upload_id = object.upload_id ?? "";
        message.part_size = object.part_size ?? 0;
        message.total_parts = object.total_parts ?? 0;
        message.expires_at = object.expires_at ?? undefined;
        return message;
    },
};
function createBaseUploadPartRequest() {
    return { payload: undefined };
}
exports.UploadPartRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        switch (message.payload?.$case) {
            case "header":
                exports.UploadPartHeader.encode(message.payload.header, writer.uint32(10).fork()).join();
                break;
            case "chunk_data":
                writer.uint32(18).bytes(message.payload.chunk_data);
                break;
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseUploadPartRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.payload = { $case: "header", header: exports.UploadPartHeader.decode(reader, reader.uint32()) };
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.payload = { $case: "chunk_data", chunk_data: reader.bytes() };
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            payload: isSet(object.header)
                ? { $case: "header", header: exports.UploadPartHeader.fromJSON(object.header) }
                : isSet(object.chunkData)
                    ? { $case: "chunk_data", chunk_data: bytesFromBase64(object.chunkData) }
                    : isSet(object.chunk_data)
                        ? { $case: "chunk_data", chunk_data: bytesFromBase64(object.chunk_data) }
                        : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.payload?.$case === "header") {
            obj.header = exports.UploadPartHeader.toJSON(message.payload.header);
        }
        else if (message.payload?.$case === "chunk_data") {
            obj.chunkData = base64FromBytes(message.payload.chunk_data);
        }
        return obj;
    },
    create(base) {
        return exports.UploadPartRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseUploadPartRequest();
        switch (object.payload?.$case) {
            case "header": {
                if (object.payload?.header !== undefined && object.payload?.header !== null) {
                    message.payload = { $case: "header", header: exports.UploadPartHeader.fromPartial(object.payload.header) };
                }
                break;
            }
            case "chunk_data": {
                if (object.payload?.chunk_data !== undefined && object.payload?.chunk_data !== null) {
                    message.payload = { $case: "chunk_data", chunk_data: object.payload.chunk_data };
                }
                break;
            }
        }
        return message;
    },
};
function createBaseUploadPartHeader() {
    return { upload_id: "", part_number: 0 };
}
exports.UploadPartHeader = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.upload_id !== "") {
            writer.uint32(10).string(message.upload_id);
        }
        if (message.part_number !== 0) {
            writer.uint32(16).int32(message.part_number);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseUploadPartHeader();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.upload_id = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.part_number = reader.int32();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            upload_id: isSet(object.uploadId)
                ? globalThis.String(object.uploadId)
                : isSet(object.upload_id)
                    ? globalThis.String(object.upload_id)
                    : "",
            part_number: isSet(object.partNumber)
                ? globalThis.Number(object.partNumber)
                : isSet(object.part_number)
                    ? globalThis.Number(object.part_number)
                    : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.upload_id !== "") {
            obj.uploadId = message.upload_id;
        }
        if (message.part_number !== 0) {
            obj.partNumber = Math.round(message.part_number);
        }
        return obj;
    },
    create(base) {
        return exports.UploadPartHeader.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseUploadPartHeader();
        message.upload_id = object.upload_id ?? "";
        message.part_number = object.part_number ?? 0;
        return message;
    },
};
function createBaseUploadPartResponse() {
    return { part_number: 0, etag: "", bytes_written: 0 };
}
exports.UploadPartResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.part_number !== 0) {
            writer.uint32(8).int32(message.part_number);
        }
        if (message.etag !== "") {
            writer.uint32(18).string(message.etag);
        }
        if (message.bytes_written !== 0) {
            writer.uint32(24).int64(message.bytes_written);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseUploadPartResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.part_number = reader.int32();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.etag = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.bytes_written = longToNumber(reader.int64());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            part_number: isSet(object.partNumber)
                ? globalThis.Number(object.partNumber)
                : isSet(object.part_number)
                    ? globalThis.Number(object.part_number)
                    : 0,
            etag: isSet(object.etag) ? globalThis.String(object.etag) : "",
            bytes_written: isSet(object.bytesWritten)
                ? globalThis.Number(object.bytesWritten)
                : isSet(object.bytes_written)
                    ? globalThis.Number(object.bytes_written)
                    : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.part_number !== 0) {
            obj.partNumber = Math.round(message.part_number);
        }
        if (message.etag !== "") {
            obj.etag = message.etag;
        }
        if (message.bytes_written !== 0) {
            obj.bytesWritten = Math.round(message.bytes_written);
        }
        return obj;
    },
    create(base) {
        return exports.UploadPartResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseUploadPartResponse();
        message.part_number = object.part_number ?? 0;
        message.etag = object.etag ?? "";
        message.bytes_written = object.bytes_written ?? 0;
        return message;
    },
};
function createBaseCompleteResumableUploadRequest() {
    return { upload_id: "", parts: [] };
}
exports.CompleteResumableUploadRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.upload_id !== "") {
            writer.uint32(10).string(message.upload_id);
        }
        for (const v of message.parts) {
            exports.PartEtag.encode(v, writer.uint32(18).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseCompleteResumableUploadRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.upload_id = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.parts.push(exports.PartEtag.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            upload_id: isSet(object.uploadId)
                ? globalThis.String(object.uploadId)
                : isSet(object.upload_id)
                    ? globalThis.String(object.upload_id)
                    : "",
            parts: globalThis.Array.isArray(object?.parts) ? object.parts.map((e) => exports.PartEtag.fromJSON(e)) : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.upload_id !== "") {
            obj.uploadId = message.upload_id;
        }
        if (message.parts?.length) {
            obj.parts = message.parts.map((e) => exports.PartEtag.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return exports.CompleteResumableUploadRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseCompleteResumableUploadRequest();
        message.upload_id = object.upload_id ?? "";
        message.parts = object.parts?.map((e) => exports.PartEtag.fromPartial(e)) || [];
        return message;
    },
};
function createBasePartEtag() {
    return { part_number: 0, etag: "" };
}
exports.PartEtag = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.part_number !== 0) {
            writer.uint32(8).int32(message.part_number);
        }
        if (message.etag !== "") {
            writer.uint32(18).string(message.etag);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBasePartEtag();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.part_number = reader.int32();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.etag = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            part_number: isSet(object.partNumber)
                ? globalThis.Number(object.partNumber)
                : isSet(object.part_number)
                    ? globalThis.Number(object.part_number)
                    : 0,
            etag: isSet(object.etag) ? globalThis.String(object.etag) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.part_number !== 0) {
            obj.partNumber = Math.round(message.part_number);
        }
        if (message.etag !== "") {
            obj.etag = message.etag;
        }
        return obj;
    },
    create(base) {
        return exports.PartEtag.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBasePartEtag();
        message.part_number = object.part_number ?? 0;
        message.etag = object.etag ?? "";
        return message;
    },
};
function createBaseAbortResumableUploadRequest() {
    return { upload_id: "" };
}
exports.AbortResumableUploadRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.upload_id !== "") {
            writer.uint32(10).string(message.upload_id);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseAbortResumableUploadRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.upload_id = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            upload_id: isSet(object.uploadId)
                ? globalThis.String(object.uploadId)
                : isSet(object.upload_id)
                    ? globalThis.String(object.upload_id)
                    : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.upload_id !== "") {
            obj.uploadId = message.upload_id;
        }
        return obj;
    },
    create(base) {
        return exports.AbortResumableUploadRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseAbortResumableUploadRequest();
        message.upload_id = object.upload_id ?? "";
        return message;
    },
};
function createBaseGetUploadStatusRequest() {
    return { upload_id: "" };
}
exports.GetUploadStatusRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.upload_id !== "") {
            writer.uint32(10).string(message.upload_id);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseGetUploadStatusRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.upload_id = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            upload_id: isSet(object.uploadId)
                ? globalThis.String(object.uploadId)
                : isSet(object.upload_id)
                    ? globalThis.String(object.upload_id)
                    : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.upload_id !== "") {
            obj.uploadId = message.upload_id;
        }
        return obj;
    },
    create(base) {
        return exports.GetUploadStatusRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseGetUploadStatusRequest();
        message.upload_id = object.upload_id ?? "";
        return message;
    },
};
function createBaseGetUploadStatusResponse() {
    return {
        upload_id: "",
        status: 0,
        completed_parts: [],
        total_parts: 0,
        expires_at: undefined,
        filename: "",
        total_size: 0,
    };
}
exports.GetUploadStatusResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.upload_id !== "") {
            writer.uint32(10).string(message.upload_id);
        }
        if (message.status !== 0) {
            writer.uint32(16).int32(message.status);
        }
        for (const v of message.completed_parts) {
            exports.UploadedPartInfo.encode(v, writer.uint32(26).fork()).join();
        }
        if (message.total_parts !== 0) {
            writer.uint32(32).int32(message.total_parts);
        }
        if (message.expires_at !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.expires_at), writer.uint32(42).fork()).join();
        }
        if (message.filename !== "") {
            writer.uint32(50).string(message.filename);
        }
        if (message.total_size !== 0) {
            writer.uint32(56).int64(message.total_size);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseGetUploadStatusResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.upload_id = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.status = reader.int32();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.completed_parts.push(exports.UploadedPartInfo.decode(reader, reader.uint32()));
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.total_parts = reader.int32();
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.expires_at = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
                case 6: {
                    if (tag !== 50) {
                        break;
                    }
                    message.filename = reader.string();
                    continue;
                }
                case 7: {
                    if (tag !== 56) {
                        break;
                    }
                    message.total_size = longToNumber(reader.int64());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            upload_id: isSet(object.uploadId)
                ? globalThis.String(object.uploadId)
                : isSet(object.upload_id)
                    ? globalThis.String(object.upload_id)
                    : "",
            status: isSet(object.status) ? uploadSessionStatusFromJSON(object.status) : 0,
            completed_parts: globalThis.Array.isArray(object?.completedParts)
                ? object.completedParts.map((e) => exports.UploadedPartInfo.fromJSON(e))
                : globalThis.Array.isArray(object?.completed_parts)
                    ? object.completed_parts.map((e) => exports.UploadedPartInfo.fromJSON(e))
                    : [],
            total_parts: isSet(object.totalParts)
                ? globalThis.Number(object.totalParts)
                : isSet(object.total_parts)
                    ? globalThis.Number(object.total_parts)
                    : 0,
            expires_at: isSet(object.expiresAt)
                ? fromJsonTimestamp(object.expiresAt)
                : isSet(object.expires_at)
                    ? fromJsonTimestamp(object.expires_at)
                    : undefined,
            filename: isSet(object.filename) ? globalThis.String(object.filename) : "",
            total_size: isSet(object.totalSize)
                ? globalThis.Number(object.totalSize)
                : isSet(object.total_size)
                    ? globalThis.Number(object.total_size)
                    : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.upload_id !== "") {
            obj.uploadId = message.upload_id;
        }
        if (message.status !== 0) {
            obj.status = uploadSessionStatusToJSON(message.status);
        }
        if (message.completed_parts?.length) {
            obj.completedParts = message.completed_parts.map((e) => exports.UploadedPartInfo.toJSON(e));
        }
        if (message.total_parts !== 0) {
            obj.totalParts = Math.round(message.total_parts);
        }
        if (message.expires_at !== undefined) {
            obj.expiresAt = message.expires_at.toISOString();
        }
        if (message.filename !== "") {
            obj.filename = message.filename;
        }
        if (message.total_size !== 0) {
            obj.totalSize = Math.round(message.total_size);
        }
        return obj;
    },
    create(base) {
        return exports.GetUploadStatusResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseGetUploadStatusResponse();
        message.upload_id = object.upload_id ?? "";
        message.status = object.status ?? 0;
        message.completed_parts = object.completed_parts?.map((e) => exports.UploadedPartInfo.fromPartial(e)) || [];
        message.total_parts = object.total_parts ?? 0;
        message.expires_at = object.expires_at ?? undefined;
        message.filename = object.filename ?? "";
        message.total_size = object.total_size ?? 0;
        return message;
    },
};
function createBaseUploadedPartInfo() {
    return { part_number: 0, etag: "", size: 0, completed_at: undefined };
}
exports.UploadedPartInfo = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.part_number !== 0) {
            writer.uint32(8).int32(message.part_number);
        }
        if (message.etag !== "") {
            writer.uint32(18).string(message.etag);
        }
        if (message.size !== 0) {
            writer.uint32(24).int64(message.size);
        }
        if (message.completed_at !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.completed_at), writer.uint32(34).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseUploadedPartInfo();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.part_number = reader.int32();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.etag = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.size = longToNumber(reader.int64());
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.completed_at = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            part_number: isSet(object.partNumber)
                ? globalThis.Number(object.partNumber)
                : isSet(object.part_number)
                    ? globalThis.Number(object.part_number)
                    : 0,
            etag: isSet(object.etag) ? globalThis.String(object.etag) : "",
            size: isSet(object.size) ? globalThis.Number(object.size) : 0,
            completed_at: isSet(object.completedAt)
                ? fromJsonTimestamp(object.completedAt)
                : isSet(object.completed_at)
                    ? fromJsonTimestamp(object.completed_at)
                    : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.part_number !== 0) {
            obj.partNumber = Math.round(message.part_number);
        }
        if (message.etag !== "") {
            obj.etag = message.etag;
        }
        if (message.size !== 0) {
            obj.size = Math.round(message.size);
        }
        if (message.completed_at !== undefined) {
            obj.completedAt = message.completed_at.toISOString();
        }
        return obj;
    },
    create(base) {
        return exports.UploadedPartInfo.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseUploadedPartInfo();
        message.part_number = object.part_number ?? 0;
        message.etag = object.etag ?? "";
        message.size = object.size ?? 0;
        message.completed_at = object.completed_at ?? undefined;
        return message;
    },
};
function createBaseDownloadFileRequest() {
    return { file_id: "", checksum: "", verify_only: false };
}
exports.DownloadFileRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.file_id !== "") {
            writer.uint32(10).string(message.file_id);
        }
        if (message.checksum !== "") {
            writer.uint32(18).string(message.checksum);
        }
        if (message.verify_only !== false) {
            writer.uint32(24).bool(message.verify_only);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseDownloadFileRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.file_id = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.checksum = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.verify_only = reader.bool();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            file_id: isSet(object.fileId)
                ? globalThis.String(object.fileId)
                : isSet(object.file_id)
                    ? globalThis.String(object.file_id)
                    : "",
            checksum: isSet(object.checksum) ? globalThis.String(object.checksum) : "",
            verify_only: isSet(object.verifyOnly)
                ? globalThis.Boolean(object.verifyOnly)
                : isSet(object.verify_only)
                    ? globalThis.Boolean(object.verify_only)
                    : false,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.file_id !== "") {
            obj.fileId = message.file_id;
        }
        if (message.checksum !== "") {
            obj.checksum = message.checksum;
        }
        if (message.verify_only !== false) {
            obj.verifyOnly = message.verify_only;
        }
        return obj;
    },
    create(base) {
        return exports.DownloadFileRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseDownloadFileRequest();
        message.file_id = object.file_id ?? "";
        message.checksum = object.checksum ?? "";
        message.verify_only = object.verify_only ?? false;
        return message;
    },
};
function createBaseDownloadFileHttpMetadata() {
    return { filename: "", content_disposition: "", etag: "" };
}
exports.DownloadFileHttpMetadata = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.filename !== "") {
            writer.uint32(10).string(message.filename);
        }
        if (message.content_disposition !== "") {
            writer.uint32(18).string(message.content_disposition);
        }
        if (message.etag !== "") {
            writer.uint32(26).string(message.etag);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseDownloadFileHttpMetadata();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.filename = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.content_disposition = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.etag = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            filename: isSet(object.filename) ? globalThis.String(object.filename) : "",
            content_disposition: isSet(object.contentDisposition)
                ? globalThis.String(object.contentDisposition)
                : isSet(object.content_disposition)
                    ? globalThis.String(object.content_disposition)
                    : "",
            etag: isSet(object.etag) ? globalThis.String(object.etag) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.filename !== "") {
            obj.filename = message.filename;
        }
        if (message.content_disposition !== "") {
            obj.contentDisposition = message.content_disposition;
        }
        if (message.etag !== "") {
            obj.etag = message.etag;
        }
        return obj;
    },
    create(base) {
        return exports.DownloadFileHttpMetadata.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseDownloadFileHttpMetadata();
        message.filename = object.filename ?? "";
        message.content_disposition = object.content_disposition ?? "";
        message.etag = object.etag ?? "";
        return message;
    },
};
function createBaseDownloadFileChunk() {
    return { data: new Uint8Array(0) };
}
exports.DownloadFileChunk = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.data.length !== 0) {
            writer.uint32(10).bytes(message.data);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseDownloadFileChunk();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.data = reader.bytes();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0) };
    },
    toJSON(message) {
        const obj = {};
        if (message.data.length !== 0) {
            obj.data = base64FromBytes(message.data);
        }
        return obj;
    },
    create(base) {
        return exports.DownloadFileChunk.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseDownloadFileChunk();
        message.data = object.data ?? new Uint8Array(0);
        return message;
    },
};
function createBaseDeleteFileRequest() {
    return { file_id: "" };
}
exports.DeleteFileRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.file_id !== "") {
            writer.uint32(10).string(message.file_id);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseDeleteFileRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.file_id = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            file_id: isSet(object.fileId)
                ? globalThis.String(object.fileId)
                : isSet(object.file_id)
                    ? globalThis.String(object.file_id)
                    : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.file_id !== "") {
            obj.fileId = message.file_id;
        }
        return obj;
    },
    create(base) {
        return exports.DeleteFileRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseDeleteFileRequest();
        message.file_id = object.file_id ?? "";
        return message;
    },
};
function createBaseListFilesRequest() {
    return { folder: "", page_size: 0, page_token: "" };
}
exports.ListFilesRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.folder !== "") {
            writer.uint32(10).string(message.folder);
        }
        if (message.page_size !== 0) {
            writer.uint32(16).int32(message.page_size);
        }
        if (message.page_token !== "") {
            writer.uint32(26).string(message.page_token);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseListFilesRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.folder = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.page_size = reader.int32();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.page_token = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            folder: isSet(object.folder) ? globalThis.String(object.folder) : "",
            page_size: isSet(object.pageSize)
                ? globalThis.Number(object.pageSize)
                : isSet(object.page_size)
                    ? globalThis.Number(object.page_size)
                    : 0,
            page_token: isSet(object.pageToken)
                ? globalThis.String(object.pageToken)
                : isSet(object.page_token)
                    ? globalThis.String(object.page_token)
                    : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.folder !== "") {
            obj.folder = message.folder;
        }
        if (message.page_size !== 0) {
            obj.pageSize = Math.round(message.page_size);
        }
        if (message.page_token !== "") {
            obj.pageToken = message.page_token;
        }
        return obj;
    },
    create(base) {
        return exports.ListFilesRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseListFilesRequest();
        message.folder = object.folder ?? "";
        message.page_size = object.page_size ?? 0;
        message.page_token = object.page_token ?? "";
        return message;
    },
};
function createBaseListFilesResponse() {
    return { files: [], next_page_token: "", total_count: 0 };
}
exports.ListFilesResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        for (const v of message.files) {
            exports.FileInfo.encode(v, writer.uint32(10).fork()).join();
        }
        if (message.next_page_token !== "") {
            writer.uint32(18).string(message.next_page_token);
        }
        if (message.total_count !== 0) {
            writer.uint32(24).int32(message.total_count);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseListFilesResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.files.push(exports.FileInfo.decode(reader, reader.uint32()));
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.next_page_token = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.total_count = reader.int32();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            files: globalThis.Array.isArray(object?.files) ? object.files.map((e) => exports.FileInfo.fromJSON(e)) : [],
            next_page_token: isSet(object.nextPageToken)
                ? globalThis.String(object.nextPageToken)
                : isSet(object.next_page_token)
                    ? globalThis.String(object.next_page_token)
                    : "",
            total_count: isSet(object.totalCount)
                ? globalThis.Number(object.totalCount)
                : isSet(object.total_count)
                    ? globalThis.Number(object.total_count)
                    : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.files?.length) {
            obj.files = message.files.map((e) => exports.FileInfo.toJSON(e));
        }
        if (message.next_page_token !== "") {
            obj.nextPageToken = message.next_page_token;
        }
        if (message.total_count !== 0) {
            obj.totalCount = Math.round(message.total_count);
        }
        return obj;
    },
    create(base) {
        return exports.ListFilesResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseListFilesResponse();
        message.files = object.files?.map((e) => exports.FileInfo.fromPartial(e)) || [];
        message.next_page_token = object.next_page_token ?? "";
        message.total_count = object.total_count ?? 0;
        return message;
    },
};
function createBaseGetFileInfoRequest() {
    return { file_id: "" };
}
exports.GetFileInfoRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.file_id !== "") {
            writer.uint32(10).string(message.file_id);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseGetFileInfoRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.file_id = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            file_id: isSet(object.fileId)
                ? globalThis.String(object.fileId)
                : isSet(object.file_id)
                    ? globalThis.String(object.file_id)
                    : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.file_id !== "") {
            obj.fileId = message.file_id;
        }
        return obj;
    },
    create(base) {
        return exports.GetFileInfoRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseGetFileInfoRequest();
        message.file_id = object.file_id ?? "";
        return message;
    },
};
function createBaseCallbackResult() {
    return { accepted: false, business_id: "", message: "" };
}
exports.CallbackResult = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.accepted !== false) {
            writer.uint32(8).bool(message.accepted);
        }
        if (message.business_id !== "") {
            writer.uint32(18).string(message.business_id);
        }
        if (message.message !== "") {
            writer.uint32(26).string(message.message);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseCallbackResult();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.accepted = reader.bool();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.business_id = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.message = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            accepted: isSet(object.accepted) ? globalThis.Boolean(object.accepted) : false,
            business_id: isSet(object.businessId)
                ? globalThis.String(object.businessId)
                : isSet(object.business_id)
                    ? globalThis.String(object.business_id)
                    : "",
            message: isSet(object.message) ? globalThis.String(object.message) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.accepted !== false) {
            obj.accepted = message.accepted;
        }
        if (message.business_id !== "") {
            obj.businessId = message.business_id;
        }
        if (message.message !== "") {
            obj.message = message.message;
        }
        return obj;
    },
    create(base) {
        return exports.CallbackResult.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseCallbackResult();
        message.accepted = object.accepted ?? false;
        message.business_id = object.business_id ?? "";
        message.message = object.message ?? "";
        return message;
    },
};
function createBaseFileInfo() {
    return {
        id: "",
        filename: "",
        content_type: "",
        file_size: 0,
        folder: "",
        owner_id: "",
        checksum: "",
        storage_backend: "",
        created_at: undefined,
        updated_at: undefined,
        local_path: "",
        storage_key: "",
    };
}
exports.FileInfo = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.id !== "") {
            writer.uint32(10).string(message.id);
        }
        if (message.filename !== "") {
            writer.uint32(18).string(message.filename);
        }
        if (message.content_type !== "") {
            writer.uint32(26).string(message.content_type);
        }
        if (message.file_size !== 0) {
            writer.uint32(32).int64(message.file_size);
        }
        if (message.folder !== "") {
            writer.uint32(42).string(message.folder);
        }
        if (message.owner_id !== "") {
            writer.uint32(50).string(message.owner_id);
        }
        if (message.checksum !== "") {
            writer.uint32(58).string(message.checksum);
        }
        if (message.storage_backend !== "") {
            writer.uint32(66).string(message.storage_backend);
        }
        if (message.created_at !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.created_at), writer.uint32(74).fork()).join();
        }
        if (message.updated_at !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.updated_at), writer.uint32(82).fork()).join();
        }
        if (message.local_path !== "") {
            writer.uint32(90).string(message.local_path);
        }
        if (message.storage_key !== "") {
            writer.uint32(98).string(message.storage_key);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseFileInfo();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.id = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.filename = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.content_type = reader.string();
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.file_size = longToNumber(reader.int64());
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.folder = reader.string();
                    continue;
                }
                case 6: {
                    if (tag !== 50) {
                        break;
                    }
                    message.owner_id = reader.string();
                    continue;
                }
                case 7: {
                    if (tag !== 58) {
                        break;
                    }
                    message.checksum = reader.string();
                    continue;
                }
                case 8: {
                    if (tag !== 66) {
                        break;
                    }
                    message.storage_backend = reader.string();
                    continue;
                }
                case 9: {
                    if (tag !== 74) {
                        break;
                    }
                    message.created_at = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
                case 10: {
                    if (tag !== 82) {
                        break;
                    }
                    message.updated_at = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
                case 11: {
                    if (tag !== 90) {
                        break;
                    }
                    message.local_path = reader.string();
                    continue;
                }
                case 12: {
                    if (tag !== 98) {
                        break;
                    }
                    message.storage_key = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            id: isSet(object.id) ? globalThis.String(object.id) : "",
            filename: isSet(object.filename) ? globalThis.String(object.filename) : "",
            content_type: isSet(object.contentType)
                ? globalThis.String(object.contentType)
                : isSet(object.content_type)
                    ? globalThis.String(object.content_type)
                    : "",
            file_size: isSet(object.fileSize)
                ? globalThis.Number(object.fileSize)
                : isSet(object.file_size)
                    ? globalThis.Number(object.file_size)
                    : 0,
            folder: isSet(object.folder) ? globalThis.String(object.folder) : "",
            owner_id: isSet(object.ownerId)
                ? globalThis.String(object.ownerId)
                : isSet(object.owner_id)
                    ? globalThis.String(object.owner_id)
                    : "",
            checksum: isSet(object.checksum) ? globalThis.String(object.checksum) : "",
            storage_backend: isSet(object.storageBackend)
                ? globalThis.String(object.storageBackend)
                : isSet(object.storage_backend)
                    ? globalThis.String(object.storage_backend)
                    : "",
            created_at: isSet(object.createdAt)
                ? fromJsonTimestamp(object.createdAt)
                : isSet(object.created_at)
                    ? fromJsonTimestamp(object.created_at)
                    : undefined,
            updated_at: isSet(object.updatedAt)
                ? fromJsonTimestamp(object.updatedAt)
                : isSet(object.updated_at)
                    ? fromJsonTimestamp(object.updated_at)
                    : undefined,
            local_path: isSet(object.localPath)
                ? globalThis.String(object.localPath)
                : isSet(object.local_path)
                    ? globalThis.String(object.local_path)
                    : "",
            storage_key: isSet(object.storageKey)
                ? globalThis.String(object.storageKey)
                : isSet(object.storage_key)
                    ? globalThis.String(object.storage_key)
                    : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.id !== "") {
            obj.id = message.id;
        }
        if (message.filename !== "") {
            obj.filename = message.filename;
        }
        if (message.content_type !== "") {
            obj.contentType = message.content_type;
        }
        if (message.file_size !== 0) {
            obj.fileSize = Math.round(message.file_size);
        }
        if (message.folder !== "") {
            obj.folder = message.folder;
        }
        if (message.owner_id !== "") {
            obj.ownerId = message.owner_id;
        }
        if (message.checksum !== "") {
            obj.checksum = message.checksum;
        }
        if (message.storage_backend !== "") {
            obj.storageBackend = message.storage_backend;
        }
        if (message.created_at !== undefined) {
            obj.createdAt = message.created_at.toISOString();
        }
        if (message.updated_at !== undefined) {
            obj.updatedAt = message.updated_at.toISOString();
        }
        if (message.local_path !== "") {
            obj.localPath = message.local_path;
        }
        if (message.storage_key !== "") {
            obj.storageKey = message.storage_key;
        }
        return obj;
    },
    create(base) {
        return exports.FileInfo.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseFileInfo();
        message.id = object.id ?? "";
        message.filename = object.filename ?? "";
        message.content_type = object.content_type ?? "";
        message.file_size = object.file_size ?? 0;
        message.folder = object.folder ?? "";
        message.owner_id = object.owner_id ?? "";
        message.checksum = object.checksum ?? "";
        message.storage_backend = object.storage_backend ?? "";
        message.created_at = object.created_at ?? undefined;
        message.updated_at = object.updated_at ?? undefined;
        message.local_path = object.local_path ?? "";
        message.storage_key = object.storage_key ?? "";
        return message;
    },
};
function createBaseVirtualPathRef() {
    return { folder: "", relative_path: "" };
}
exports.VirtualPathRef = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.folder !== "") {
            writer.uint32(10).string(message.folder);
        }
        if (message.relative_path !== "") {
            writer.uint32(18).string(message.relative_path);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseVirtualPathRef();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.folder = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.relative_path = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            folder: isSet(object.folder) ? globalThis.String(object.folder) : "",
            relative_path: isSet(object.relativePath)
                ? globalThis.String(object.relativePath)
                : isSet(object.relative_path)
                    ? globalThis.String(object.relative_path)
                    : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.folder !== "") {
            obj.folder = message.folder;
        }
        if (message.relative_path !== "") {
            obj.relativePath = message.relative_path;
        }
        return obj;
    },
    create(base) {
        return exports.VirtualPathRef.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseVirtualPathRef();
        message.folder = object.folder ?? "";
        message.relative_path = object.relative_path ?? "";
        return message;
    },
};
function createBasePathSelector() {
    return { selector: undefined };
}
exports.PathSelector = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        switch (message.selector?.$case) {
            case "file_id":
                writer.uint32(10).string(message.selector.file_id);
                break;
            case "path":
                exports.VirtualPathRef.encode(message.selector.path, writer.uint32(18).fork()).join();
                break;
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBasePathSelector();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.selector = { $case: "file_id", file_id: reader.string() };
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.selector = { $case: "path", path: exports.VirtualPathRef.decode(reader, reader.uint32()) };
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            selector: isSet(object.fileId)
                ? { $case: "file_id", file_id: globalThis.String(object.fileId) }
                : isSet(object.file_id)
                    ? { $case: "file_id", file_id: globalThis.String(object.file_id) }
                    : isSet(object.path)
                        ? { $case: "path", path: exports.VirtualPathRef.fromJSON(object.path) }
                        : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.selector?.$case === "file_id") {
            obj.fileId = message.selector.file_id;
        }
        else if (message.selector?.$case === "path") {
            obj.path = exports.VirtualPathRef.toJSON(message.selector.path);
        }
        return obj;
    },
    create(base) {
        return exports.PathSelector.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBasePathSelector();
        switch (object.selector?.$case) {
            case "file_id": {
                if (object.selector?.file_id !== undefined && object.selector?.file_id !== null) {
                    message.selector = { $case: "file_id", file_id: object.selector.file_id };
                }
                break;
            }
            case "path": {
                if (object.selector?.path !== undefined && object.selector?.path !== null) {
                    message.selector = { $case: "path", path: exports.VirtualPathRef.fromPartial(object.selector.path) };
                }
                break;
            }
        }
        return message;
    },
};
function createBasePathEntry() {
    return {
        kind: 0,
        name: "",
        path: "",
        parent_folder: "",
        file_id: "",
        content_type: "",
        size_bytes: 0,
        checksum: "",
        created_at: undefined,
        updated_at: undefined,
        has_children: false,
    };
}
exports.PathEntry = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.kind !== 0) {
            writer.uint32(8).int32(message.kind);
        }
        if (message.name !== "") {
            writer.uint32(18).string(message.name);
        }
        if (message.path !== "") {
            writer.uint32(26).string(message.path);
        }
        if (message.parent_folder !== "") {
            writer.uint32(34).string(message.parent_folder);
        }
        if (message.file_id !== "") {
            writer.uint32(42).string(message.file_id);
        }
        if (message.content_type !== "") {
            writer.uint32(50).string(message.content_type);
        }
        if (message.size_bytes !== 0) {
            writer.uint32(56).int64(message.size_bytes);
        }
        if (message.checksum !== "") {
            writer.uint32(66).string(message.checksum);
        }
        if (message.created_at !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.created_at), writer.uint32(74).fork()).join();
        }
        if (message.updated_at !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.updated_at), writer.uint32(82).fork()).join();
        }
        if (message.has_children !== false) {
            writer.uint32(88).bool(message.has_children);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBasePathEntry();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.kind = reader.int32();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.name = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.path = reader.string();
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.parent_folder = reader.string();
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.file_id = reader.string();
                    continue;
                }
                case 6: {
                    if (tag !== 50) {
                        break;
                    }
                    message.content_type = reader.string();
                    continue;
                }
                case 7: {
                    if (tag !== 56) {
                        break;
                    }
                    message.size_bytes = longToNumber(reader.int64());
                    continue;
                }
                case 8: {
                    if (tag !== 66) {
                        break;
                    }
                    message.checksum = reader.string();
                    continue;
                }
                case 9: {
                    if (tag !== 74) {
                        break;
                    }
                    message.created_at = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
                case 10: {
                    if (tag !== 82) {
                        break;
                    }
                    message.updated_at = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
                case 11: {
                    if (tag !== 88) {
                        break;
                    }
                    message.has_children = reader.bool();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            kind: isSet(object.kind) ? pathEntryKindFromJSON(object.kind) : 0,
            name: isSet(object.name) ? globalThis.String(object.name) : "",
            path: isSet(object.path) ? globalThis.String(object.path) : "",
            parent_folder: isSet(object.parentFolder)
                ? globalThis.String(object.parentFolder)
                : isSet(object.parent_folder)
                    ? globalThis.String(object.parent_folder)
                    : "",
            file_id: isSet(object.fileId)
                ? globalThis.String(object.fileId)
                : isSet(object.file_id)
                    ? globalThis.String(object.file_id)
                    : "",
            content_type: isSet(object.contentType)
                ? globalThis.String(object.contentType)
                : isSet(object.content_type)
                    ? globalThis.String(object.content_type)
                    : "",
            size_bytes: isSet(object.sizeBytes)
                ? globalThis.Number(object.sizeBytes)
                : isSet(object.size_bytes)
                    ? globalThis.Number(object.size_bytes)
                    : 0,
            checksum: isSet(object.checksum) ? globalThis.String(object.checksum) : "",
            created_at: isSet(object.createdAt)
                ? fromJsonTimestamp(object.createdAt)
                : isSet(object.created_at)
                    ? fromJsonTimestamp(object.created_at)
                    : undefined,
            updated_at: isSet(object.updatedAt)
                ? fromJsonTimestamp(object.updatedAt)
                : isSet(object.updated_at)
                    ? fromJsonTimestamp(object.updated_at)
                    : undefined,
            has_children: isSet(object.hasChildren)
                ? globalThis.Boolean(object.hasChildren)
                : isSet(object.has_children)
                    ? globalThis.Boolean(object.has_children)
                    : false,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.kind !== 0) {
            obj.kind = pathEntryKindToJSON(message.kind);
        }
        if (message.name !== "") {
            obj.name = message.name;
        }
        if (message.path !== "") {
            obj.path = message.path;
        }
        if (message.parent_folder !== "") {
            obj.parentFolder = message.parent_folder;
        }
        if (message.file_id !== "") {
            obj.fileId = message.file_id;
        }
        if (message.content_type !== "") {
            obj.contentType = message.content_type;
        }
        if (message.size_bytes !== 0) {
            obj.sizeBytes = Math.round(message.size_bytes);
        }
        if (message.checksum !== "") {
            obj.checksum = message.checksum;
        }
        if (message.created_at !== undefined) {
            obj.createdAt = message.created_at.toISOString();
        }
        if (message.updated_at !== undefined) {
            obj.updatedAt = message.updated_at.toISOString();
        }
        if (message.has_children !== false) {
            obj.hasChildren = message.has_children;
        }
        return obj;
    },
    create(base) {
        return exports.PathEntry.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBasePathEntry();
        message.kind = object.kind ?? 0;
        message.name = object.name ?? "";
        message.path = object.path ?? "";
        message.parent_folder = object.parent_folder ?? "";
        message.file_id = object.file_id ?? "";
        message.content_type = object.content_type ?? "";
        message.size_bytes = object.size_bytes ?? 0;
        message.checksum = object.checksum ?? "";
        message.created_at = object.created_at ?? undefined;
        message.updated_at = object.updated_at ?? undefined;
        message.has_children = object.has_children ?? false;
        return message;
    },
};
function createBaseListFolderRequest() {
    return { folder: "", page_size: 0, page_token: "", include_files: false, include_directories: false };
}
exports.ListFolderRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.folder !== "") {
            writer.uint32(10).string(message.folder);
        }
        if (message.page_size !== 0) {
            writer.uint32(16).int32(message.page_size);
        }
        if (message.page_token !== "") {
            writer.uint32(26).string(message.page_token);
        }
        if (message.include_files !== false) {
            writer.uint32(32).bool(message.include_files);
        }
        if (message.include_directories !== false) {
            writer.uint32(40).bool(message.include_directories);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseListFolderRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.folder = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.page_size = reader.int32();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.page_token = reader.string();
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.include_files = reader.bool();
                    continue;
                }
                case 5: {
                    if (tag !== 40) {
                        break;
                    }
                    message.include_directories = reader.bool();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            folder: isSet(object.folder) ? globalThis.String(object.folder) : "",
            page_size: isSet(object.pageSize)
                ? globalThis.Number(object.pageSize)
                : isSet(object.page_size)
                    ? globalThis.Number(object.page_size)
                    : 0,
            page_token: isSet(object.pageToken)
                ? globalThis.String(object.pageToken)
                : isSet(object.page_token)
                    ? globalThis.String(object.page_token)
                    : "",
            include_files: isSet(object.includeFiles)
                ? globalThis.Boolean(object.includeFiles)
                : isSet(object.include_files)
                    ? globalThis.Boolean(object.include_files)
                    : false,
            include_directories: isSet(object.includeDirectories)
                ? globalThis.Boolean(object.includeDirectories)
                : isSet(object.include_directories)
                    ? globalThis.Boolean(object.include_directories)
                    : false,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.folder !== "") {
            obj.folder = message.folder;
        }
        if (message.page_size !== 0) {
            obj.pageSize = Math.round(message.page_size);
        }
        if (message.page_token !== "") {
            obj.pageToken = message.page_token;
        }
        if (message.include_files !== false) {
            obj.includeFiles = message.include_files;
        }
        if (message.include_directories !== false) {
            obj.includeDirectories = message.include_directories;
        }
        return obj;
    },
    create(base) {
        return exports.ListFolderRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseListFolderRequest();
        message.folder = object.folder ?? "";
        message.page_size = object.page_size ?? 0;
        message.page_token = object.page_token ?? "";
        message.include_files = object.include_files ?? false;
        message.include_directories = object.include_directories ?? false;
        return message;
    },
};
function createBaseListFolderResponse() {
    return { entries: [], next_page_token: "", total_count: 0 };
}
exports.ListFolderResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        for (const v of message.entries) {
            exports.PathEntry.encode(v, writer.uint32(10).fork()).join();
        }
        if (message.next_page_token !== "") {
            writer.uint32(18).string(message.next_page_token);
        }
        if (message.total_count !== 0) {
            writer.uint32(24).int32(message.total_count);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseListFolderResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.entries.push(exports.PathEntry.decode(reader, reader.uint32()));
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.next_page_token = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.total_count = reader.int32();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            entries: globalThis.Array.isArray(object?.entries) ? object.entries.map((e) => exports.PathEntry.fromJSON(e)) : [],
            next_page_token: isSet(object.nextPageToken)
                ? globalThis.String(object.nextPageToken)
                : isSet(object.next_page_token)
                    ? globalThis.String(object.next_page_token)
                    : "",
            total_count: isSet(object.totalCount)
                ? globalThis.Number(object.totalCount)
                : isSet(object.total_count)
                    ? globalThis.Number(object.total_count)
                    : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.entries?.length) {
            obj.entries = message.entries.map((e) => exports.PathEntry.toJSON(e));
        }
        if (message.next_page_token !== "") {
            obj.nextPageToken = message.next_page_token;
        }
        if (message.total_count !== 0) {
            obj.totalCount = Math.round(message.total_count);
        }
        return obj;
    },
    create(base) {
        return exports.ListFolderResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseListFolderResponse();
        message.entries = object.entries?.map((e) => exports.PathEntry.fromPartial(e)) || [];
        message.next_page_token = object.next_page_token ?? "";
        message.total_count = object.total_count ?? 0;
        return message;
    },
};
function createBaseGetPathInfoRequest() {
    return { selector: undefined };
}
exports.GetPathInfoRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.selector !== undefined) {
            exports.PathSelector.encode(message.selector, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseGetPathInfoRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.selector = exports.PathSelector.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { selector: isSet(object.selector) ? exports.PathSelector.fromJSON(object.selector) : undefined };
    },
    toJSON(message) {
        const obj = {};
        if (message.selector !== undefined) {
            obj.selector = exports.PathSelector.toJSON(message.selector);
        }
        return obj;
    },
    create(base) {
        return exports.GetPathInfoRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseGetPathInfoRequest();
        message.selector = (object.selector !== undefined && object.selector !== null)
            ? exports.PathSelector.fromPartial(object.selector)
            : undefined;
        return message;
    },
};
function createBaseGetPathInfoResponse() {
    return { entry: undefined };
}
exports.GetPathInfoResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.entry !== undefined) {
            exports.PathEntry.encode(message.entry, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseGetPathInfoResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.entry = exports.PathEntry.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { entry: isSet(object.entry) ? exports.PathEntry.fromJSON(object.entry) : undefined };
    },
    toJSON(message) {
        const obj = {};
        if (message.entry !== undefined) {
            obj.entry = exports.PathEntry.toJSON(message.entry);
        }
        return obj;
    },
    create(base) {
        return exports.GetPathInfoResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseGetPathInfoResponse();
        message.entry = (object.entry !== undefined && object.entry !== null)
            ? exports.PathEntry.fromPartial(object.entry)
            : undefined;
        return message;
    },
};
function createBaseReadTextFileRequest() {
    return { selector: undefined, max_bytes: 0, fail_if_binary: false };
}
exports.ReadTextFileRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.selector !== undefined) {
            exports.PathSelector.encode(message.selector, writer.uint32(10).fork()).join();
        }
        if (message.max_bytes !== 0) {
            writer.uint32(16).int32(message.max_bytes);
        }
        if (message.fail_if_binary !== false) {
            writer.uint32(24).bool(message.fail_if_binary);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseReadTextFileRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.selector = exports.PathSelector.decode(reader, reader.uint32());
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.max_bytes = reader.int32();
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.fail_if_binary = reader.bool();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            selector: isSet(object.selector) ? exports.PathSelector.fromJSON(object.selector) : undefined,
            max_bytes: isSet(object.maxBytes)
                ? globalThis.Number(object.maxBytes)
                : isSet(object.max_bytes)
                    ? globalThis.Number(object.max_bytes)
                    : 0,
            fail_if_binary: isSet(object.failIfBinary)
                ? globalThis.Boolean(object.failIfBinary)
                : isSet(object.fail_if_binary)
                    ? globalThis.Boolean(object.fail_if_binary)
                    : false,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.selector !== undefined) {
            obj.selector = exports.PathSelector.toJSON(message.selector);
        }
        if (message.max_bytes !== 0) {
            obj.maxBytes = Math.round(message.max_bytes);
        }
        if (message.fail_if_binary !== false) {
            obj.failIfBinary = message.fail_if_binary;
        }
        return obj;
    },
    create(base) {
        return exports.ReadTextFileRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseReadTextFileRequest();
        message.selector = (object.selector !== undefined && object.selector !== null)
            ? exports.PathSelector.fromPartial(object.selector)
            : undefined;
        message.max_bytes = object.max_bytes ?? 0;
        message.fail_if_binary = object.fail_if_binary ?? false;
        return message;
    },
};
function createBaseReadTextFileResponse() {
    return {
        entry: undefined,
        text: "",
        content_type: "",
        size_bytes: 0,
        checksum: "",
        updated_at: undefined,
        truncated: false,
        lossy: false,
    };
}
exports.ReadTextFileResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.entry !== undefined) {
            exports.PathEntry.encode(message.entry, writer.uint32(10).fork()).join();
        }
        if (message.text !== "") {
            writer.uint32(18).string(message.text);
        }
        if (message.content_type !== "") {
            writer.uint32(26).string(message.content_type);
        }
        if (message.size_bytes !== 0) {
            writer.uint32(32).int64(message.size_bytes);
        }
        if (message.checksum !== "") {
            writer.uint32(42).string(message.checksum);
        }
        if (message.updated_at !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.updated_at), writer.uint32(50).fork()).join();
        }
        if (message.truncated !== false) {
            writer.uint32(56).bool(message.truncated);
        }
        if (message.lossy !== false) {
            writer.uint32(64).bool(message.lossy);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseReadTextFileResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.entry = exports.PathEntry.decode(reader, reader.uint32());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.text = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.content_type = reader.string();
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.size_bytes = longToNumber(reader.int64());
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.checksum = reader.string();
                    continue;
                }
                case 6: {
                    if (tag !== 50) {
                        break;
                    }
                    message.updated_at = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
                case 7: {
                    if (tag !== 56) {
                        break;
                    }
                    message.truncated = reader.bool();
                    continue;
                }
                case 8: {
                    if (tag !== 64) {
                        break;
                    }
                    message.lossy = reader.bool();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            entry: isSet(object.entry) ? exports.PathEntry.fromJSON(object.entry) : undefined,
            text: isSet(object.text) ? globalThis.String(object.text) : "",
            content_type: isSet(object.contentType)
                ? globalThis.String(object.contentType)
                : isSet(object.content_type)
                    ? globalThis.String(object.content_type)
                    : "",
            size_bytes: isSet(object.sizeBytes)
                ? globalThis.Number(object.sizeBytes)
                : isSet(object.size_bytes)
                    ? globalThis.Number(object.size_bytes)
                    : 0,
            checksum: isSet(object.checksum) ? globalThis.String(object.checksum) : "",
            updated_at: isSet(object.updatedAt)
                ? fromJsonTimestamp(object.updatedAt)
                : isSet(object.updated_at)
                    ? fromJsonTimestamp(object.updated_at)
                    : undefined,
            truncated: isSet(object.truncated) ? globalThis.Boolean(object.truncated) : false,
            lossy: isSet(object.lossy) ? globalThis.Boolean(object.lossy) : false,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.entry !== undefined) {
            obj.entry = exports.PathEntry.toJSON(message.entry);
        }
        if (message.text !== "") {
            obj.text = message.text;
        }
        if (message.content_type !== "") {
            obj.contentType = message.content_type;
        }
        if (message.size_bytes !== 0) {
            obj.sizeBytes = Math.round(message.size_bytes);
        }
        if (message.checksum !== "") {
            obj.checksum = message.checksum;
        }
        if (message.updated_at !== undefined) {
            obj.updatedAt = message.updated_at.toISOString();
        }
        if (message.truncated !== false) {
            obj.truncated = message.truncated;
        }
        if (message.lossy !== false) {
            obj.lossy = message.lossy;
        }
        return obj;
    },
    create(base) {
        return exports.ReadTextFileResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseReadTextFileResponse();
        message.entry = (object.entry !== undefined && object.entry !== null)
            ? exports.PathEntry.fromPartial(object.entry)
            : undefined;
        message.text = object.text ?? "";
        message.content_type = object.content_type ?? "";
        message.size_bytes = object.size_bytes ?? 0;
        message.checksum = object.checksum ?? "";
        message.updated_at = object.updated_at ?? undefined;
        message.truncated = object.truncated ?? false;
        message.lossy = object.lossy ?? false;
        return message;
    },
};
exports.FileStorageServiceServiceName = "stew.api.v1.FileStorageService";
class FileStorageServiceClientImpl {
    constructor(rpc, opts) {
        this.service = opts?.service || exports.FileStorageServiceServiceName;
        this.rpc = rpc;
        this.UploadFile = this.UploadFile.bind(this);
        this.InitResumableUpload = this.InitResumableUpload.bind(this);
        this.UploadPart = this.UploadPart.bind(this);
        this.CompleteResumableUpload = this.CompleteResumableUpload.bind(this);
        this.AbortResumableUpload = this.AbortResumableUpload.bind(this);
        this.GetUploadStatus = this.GetUploadStatus.bind(this);
        this.DownloadFile = this.DownloadFile.bind(this);
        this.DeleteFile = this.DeleteFile.bind(this);
        this.ListFiles = this.ListFiles.bind(this);
        this.GetFileInfo = this.GetFileInfo.bind(this);
        this.ListFolder = this.ListFolder.bind(this);
        this.GetPathInfo = this.GetPathInfo.bind(this);
        this.ReadTextFile = this.ReadTextFile.bind(this);
    }
    UploadFile(request) {
        const data = request.pipe((0, operators_1.map)((request) => exports.UploadFileRequest.encode(request).finish()));
        const promise = this.rpc.clientStreamingRequest(this.service, "UploadFile", data);
        return promise.then((data) => exports.UploadFileResponse.decode(new wire_1.BinaryReader(data)));
    }
    InitResumableUpload(request) {
        const data = exports.InitResumableUploadRequest.encode(request).finish();
        const promise = this.rpc.request(this.service, "InitResumableUpload", data);
        return promise.then((data) => exports.InitResumableUploadResponse.decode(new wire_1.BinaryReader(data)));
    }
    UploadPart(request) {
        const data = request.pipe((0, operators_1.map)((request) => exports.UploadPartRequest.encode(request).finish()));
        const promise = this.rpc.clientStreamingRequest(this.service, "UploadPart", data);
        return promise.then((data) => exports.UploadPartResponse.decode(new wire_1.BinaryReader(data)));
    }
    CompleteResumableUpload(request) {
        const data = exports.CompleteResumableUploadRequest.encode(request).finish();
        const promise = this.rpc.request(this.service, "CompleteResumableUpload", data);
        return promise.then((data) => exports.UploadFileResponse.decode(new wire_1.BinaryReader(data)));
    }
    AbortResumableUpload(request) {
        const data = exports.AbortResumableUploadRequest.encode(request).finish();
        const promise = this.rpc.request(this.service, "AbortResumableUpload", data);
        return promise.then((data) => empty_1.Empty.decode(new wire_1.BinaryReader(data)));
    }
    GetUploadStatus(request) {
        const data = exports.GetUploadStatusRequest.encode(request).finish();
        const promise = this.rpc.request(this.service, "GetUploadStatus", data);
        return promise.then((data) => exports.GetUploadStatusResponse.decode(new wire_1.BinaryReader(data)));
    }
    DownloadFile(request) {
        const data = exports.DownloadFileRequest.encode(request).finish();
        const promise = this.rpc.request(this.service, "DownloadFile", data);
        return promise.then((data) => httpbody_1.HttpBody.decode(new wire_1.BinaryReader(data)));
    }
    DeleteFile(request) {
        const data = exports.DeleteFileRequest.encode(request).finish();
        const promise = this.rpc.request(this.service, "DeleteFile", data);
        return promise.then((data) => empty_1.Empty.decode(new wire_1.BinaryReader(data)));
    }
    ListFiles(request) {
        const data = exports.ListFilesRequest.encode(request).finish();
        const promise = this.rpc.request(this.service, "ListFiles", data);
        return promise.then((data) => exports.ListFilesResponse.decode(new wire_1.BinaryReader(data)));
    }
    GetFileInfo(request) {
        const data = exports.GetFileInfoRequest.encode(request).finish();
        const promise = this.rpc.request(this.service, "GetFileInfo", data);
        return promise.then((data) => exports.FileInfo.decode(new wire_1.BinaryReader(data)));
    }
    ListFolder(request) {
        const data = exports.ListFolderRequest.encode(request).finish();
        const promise = this.rpc.request(this.service, "ListFolder", data);
        return promise.then((data) => exports.ListFolderResponse.decode(new wire_1.BinaryReader(data)));
    }
    GetPathInfo(request) {
        const data = exports.GetPathInfoRequest.encode(request).finish();
        const promise = this.rpc.request(this.service, "GetPathInfo", data);
        return promise.then((data) => exports.GetPathInfoResponse.decode(new wire_1.BinaryReader(data)));
    }
    ReadTextFile(request) {
        const data = exports.ReadTextFileRequest.encode(request).finish();
        const promise = this.rpc.request(this.service, "ReadTextFile", data);
        return promise.then((data) => exports.ReadTextFileResponse.decode(new wire_1.BinaryReader(data)));
    }
}
exports.FileStorageServiceClientImpl = FileStorageServiceClientImpl;
function bytesFromBase64(b64) {
    if (globalThis.Buffer) {
        return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
    }
    else {
        const bin = globalThis.atob(b64);
        const arr = new Uint8Array(bin.length);
        for (let i = 0; i < bin.length; ++i) {
            arr[i] = bin.charCodeAt(i);
        }
        return arr;
    }
}
function base64FromBytes(arr) {
    if (globalThis.Buffer) {
        return globalThis.Buffer.from(arr).toString("base64");
    }
    else {
        const bin = [];
        arr.forEach((byte) => {
            bin.push(globalThis.String.fromCharCode(byte));
        });
        return globalThis.btoa(bin.join(""));
    }
}
function toTimestamp(date) {
    const seconds = Math.trunc(date.getTime() / 1000);
    const nanos = (date.getTime() % 1000) * 1000000;
    return { seconds, nanos };
}
function fromTimestamp(t) {
    let millis = (t.seconds || 0) * 1000;
    millis += (t.nanos || 0) / 1000000;
    return new globalThis.Date(millis);
}
function fromJsonTimestamp(o) {
    if (o instanceof globalThis.Date) {
        return o;
    }
    else if (typeof o === "string") {
        return new globalThis.Date(o);
    }
    else {
        return fromTimestamp(timestamp_1.Timestamp.fromJSON(o));
    }
}
function longToNumber(int64) {
    const num = globalThis.Number(int64.toString());
    if (num > globalThis.Number.MAX_SAFE_INTEGER) {
        throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
    }
    if (num < globalThis.Number.MIN_SAFE_INTEGER) {
        throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
    }
    return num;
}
function isSet(value) {
    return value !== null && value !== undefined;
}
