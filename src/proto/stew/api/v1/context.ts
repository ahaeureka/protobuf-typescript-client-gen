// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v3.21.12
// source: stew/api/v1/context.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Any } from "../../../google/protobuf/any";

export const protobufPackage = "stew.api.v1";

export interface Tenant {
  sub: string;
  name: string;
  tenant_id: string;
  roles: string[];
  permissions: string[];
  departments: string[];
  attributes: { [key: string]: Any };
}

export interface Tenant_AttributesEntry {
  key: string;
  value: Any | undefined;
}

export interface ClientContext {
  ip: string;
  device: string;
  location: string;
  token: string;
  tenant: Tenant | undefined;
  ua: string;
  referer: string;
  origin: string;
  host: string;
  browser: string;
  os: string;
  country: string;
  region: string;
  additional: { [key: string]: Any };
}

export interface ClientContext_AdditionalEntry {
  key: string;
  value: Any | undefined;
}

function createBaseTenant(): Tenant {
  return { sub: "", name: "", tenant_id: "", roles: [], permissions: [], departments: [], attributes: {} };
}

export const Tenant: MessageFns<Tenant> = {
  encode(message: Tenant, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sub !== "") {
      writer.uint32(10).string(message.sub);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.tenant_id !== "") {
      writer.uint32(26).string(message.tenant_id);
    }
    for (const v of message.roles) {
      writer.uint32(34).string(v!);
    }
    for (const v of message.permissions) {
      writer.uint32(42).string(v!);
    }
    for (const v of message.departments) {
      writer.uint32(50).string(v!);
    }
    globalThis.Object.entries(message.attributes).forEach(([key, value]: [string, Any]) => {
      Tenant_AttributesEntry.encode({ key: key as any, value }, writer.uint32(58).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Tenant {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTenant();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sub = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tenant_id = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.roles.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.permissions.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.departments.push(reader.string());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          const entry7 = Tenant_AttributesEntry.decode(reader, reader.uint32());
          if (entry7.value !== undefined) {
            message.attributes[entry7.key] = entry7.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Tenant {
    return {
      sub: isSet(object.sub) ? globalThis.String(object.sub) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      tenant_id: isSet(object.tenant_id) ? globalThis.String(object.tenant_id) : "",
      roles: globalThis.Array.isArray(object?.roles) ? object.roles.map((e: any) => globalThis.String(e)) : [],
      permissions: globalThis.Array.isArray(object?.permissions)
        ? object.permissions.map((e: any) => globalThis.String(e))
        : [],
      departments: globalThis.Array.isArray(object?.departments)
        ? object.departments.map((e: any) => globalThis.String(e))
        : [],
      attributes: isObject(object.attributes)
        ? (globalThis.Object.entries(object.attributes) as [string, any][]).reduce(
          (acc: { [key: string]: Any }, [key, value]: [string, any]) => {
            acc[key] = Any.fromJSON(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: Tenant): unknown {
    const obj: any = {};
    if (message.sub !== "") {
      obj.sub = message.sub;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.tenant_id !== "") {
      obj.tenant_id = message.tenant_id;
    }
    if (message.roles?.length) {
      obj.roles = message.roles;
    }
    if (message.permissions?.length) {
      obj.permissions = message.permissions;
    }
    if (message.departments?.length) {
      obj.departments = message.departments;
    }
    if (message.attributes) {
      const entries = globalThis.Object.entries(message.attributes) as [string, Any][];
      if (entries.length > 0) {
        obj.attributes = {};
        entries.forEach(([k, v]) => {
          obj.attributes[k] = Any.toJSON(v);
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Tenant>, I>>(base?: I): Tenant {
    return Tenant.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Tenant>, I>>(object: I): Tenant {
    const message = createBaseTenant();
    message.sub = object.sub ?? "";
    message.name = object.name ?? "";
    message.tenant_id = object.tenant_id ?? "";
    message.roles = object.roles?.map((e) => e) || [];
    message.permissions = object.permissions?.map((e) => e) || [];
    message.departments = object.departments?.map((e) => e) || [];
    message.attributes = (globalThis.Object.entries(object.attributes ?? {}) as [string, Any][]).reduce(
      (acc: { [key: string]: Any }, [key, value]: [string, Any]) => {
        if (value !== undefined) {
          acc[key] = Any.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseTenant_AttributesEntry(): Tenant_AttributesEntry {
  return { key: "", value: undefined };
}

export const Tenant_AttributesEntry: MessageFns<Tenant_AttributesEntry> = {
  encode(message: Tenant_AttributesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      Any.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Tenant_AttributesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTenant_AttributesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = Any.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Tenant_AttributesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? Any.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: Tenant_AttributesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = Any.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Tenant_AttributesEntry>, I>>(base?: I): Tenant_AttributesEntry {
    return Tenant_AttributesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Tenant_AttributesEntry>, I>>(object: I): Tenant_AttributesEntry {
    const message = createBaseTenant_AttributesEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null) ? Any.fromPartial(object.value) : undefined;
    return message;
  },
};

function createBaseClientContext(): ClientContext {
  return {
    ip: "",
    device: "",
    location: "",
    token: "",
    tenant: undefined,
    ua: "",
    referer: "",
    origin: "",
    host: "",
    browser: "",
    os: "",
    country: "",
    region: "",
    additional: {},
  };
}

export const ClientContext: MessageFns<ClientContext> = {
  encode(message: ClientContext, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ip !== "") {
      writer.uint32(10).string(message.ip);
    }
    if (message.device !== "") {
      writer.uint32(18).string(message.device);
    }
    if (message.location !== "") {
      writer.uint32(26).string(message.location);
    }
    if (message.token !== "") {
      writer.uint32(34).string(message.token);
    }
    if (message.tenant !== undefined) {
      Tenant.encode(message.tenant, writer.uint32(42).fork()).join();
    }
    if (message.ua !== "") {
      writer.uint32(50).string(message.ua);
    }
    if (message.referer !== "") {
      writer.uint32(58).string(message.referer);
    }
    if (message.origin !== "") {
      writer.uint32(66).string(message.origin);
    }
    if (message.host !== "") {
      writer.uint32(74).string(message.host);
    }
    if (message.browser !== "") {
      writer.uint32(82).string(message.browser);
    }
    if (message.os !== "") {
      writer.uint32(90).string(message.os);
    }
    if (message.country !== "") {
      writer.uint32(98).string(message.country);
    }
    if (message.region !== "") {
      writer.uint32(106).string(message.region);
    }
    globalThis.Object.entries(message.additional).forEach(([key, value]: [string, Any]) => {
      ClientContext_AdditionalEntry.encode({ key: key as any, value }, writer.uint32(114).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClientContext {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClientContext();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ip = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.device = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.location = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.token = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.tenant = Tenant.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.ua = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.referer = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.origin = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.host = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.browser = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.os = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.country = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.region = reader.string();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          const entry14 = ClientContext_AdditionalEntry.decode(reader, reader.uint32());
          if (entry14.value !== undefined) {
            message.additional[entry14.key] = entry14.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClientContext {
    return {
      ip: isSet(object.ip) ? globalThis.String(object.ip) : "",
      device: isSet(object.device) ? globalThis.String(object.device) : "",
      location: isSet(object.location) ? globalThis.String(object.location) : "",
      token: isSet(object.token) ? globalThis.String(object.token) : "",
      tenant: isSet(object.tenant) ? Tenant.fromJSON(object.tenant) : undefined,
      ua: isSet(object.ua) ? globalThis.String(object.ua) : "",
      referer: isSet(object.referer) ? globalThis.String(object.referer) : "",
      origin: isSet(object.origin) ? globalThis.String(object.origin) : "",
      host: isSet(object.host) ? globalThis.String(object.host) : "",
      browser: isSet(object.browser) ? globalThis.String(object.browser) : "",
      os: isSet(object.os) ? globalThis.String(object.os) : "",
      country: isSet(object.country) ? globalThis.String(object.country) : "",
      region: isSet(object.region) ? globalThis.String(object.region) : "",
      additional: isObject(object.additional)
        ? (globalThis.Object.entries(object.additional) as [string, any][]).reduce(
          (acc: { [key: string]: Any }, [key, value]: [string, any]) => {
            acc[key] = Any.fromJSON(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: ClientContext): unknown {
    const obj: any = {};
    if (message.ip !== "") {
      obj.ip = message.ip;
    }
    if (message.device !== "") {
      obj.device = message.device;
    }
    if (message.location !== "") {
      obj.location = message.location;
    }
    if (message.token !== "") {
      obj.token = message.token;
    }
    if (message.tenant !== undefined) {
      obj.tenant = Tenant.toJSON(message.tenant);
    }
    if (message.ua !== "") {
      obj.ua = message.ua;
    }
    if (message.referer !== "") {
      obj.referer = message.referer;
    }
    if (message.origin !== "") {
      obj.origin = message.origin;
    }
    if (message.host !== "") {
      obj.host = message.host;
    }
    if (message.browser !== "") {
      obj.browser = message.browser;
    }
    if (message.os !== "") {
      obj.os = message.os;
    }
    if (message.country !== "") {
      obj.country = message.country;
    }
    if (message.region !== "") {
      obj.region = message.region;
    }
    if (message.additional) {
      const entries = globalThis.Object.entries(message.additional) as [string, Any][];
      if (entries.length > 0) {
        obj.additional = {};
        entries.forEach(([k, v]) => {
          obj.additional[k] = Any.toJSON(v);
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ClientContext>, I>>(base?: I): ClientContext {
    return ClientContext.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ClientContext>, I>>(object: I): ClientContext {
    const message = createBaseClientContext();
    message.ip = object.ip ?? "";
    message.device = object.device ?? "";
    message.location = object.location ?? "";
    message.token = object.token ?? "";
    message.tenant = (object.tenant !== undefined && object.tenant !== null)
      ? Tenant.fromPartial(object.tenant)
      : undefined;
    message.ua = object.ua ?? "";
    message.referer = object.referer ?? "";
    message.origin = object.origin ?? "";
    message.host = object.host ?? "";
    message.browser = object.browser ?? "";
    message.os = object.os ?? "";
    message.country = object.country ?? "";
    message.region = object.region ?? "";
    message.additional = (globalThis.Object.entries(object.additional ?? {}) as [string, Any][]).reduce(
      (acc: { [key: string]: Any }, [key, value]: [string, Any]) => {
        if (value !== undefined) {
          acc[key] = Any.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseClientContext_AdditionalEntry(): ClientContext_AdditionalEntry {
  return { key: "", value: undefined };
}

export const ClientContext_AdditionalEntry: MessageFns<ClientContext_AdditionalEntry> = {
  encode(message: ClientContext_AdditionalEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      Any.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClientContext_AdditionalEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClientContext_AdditionalEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = Any.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClientContext_AdditionalEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? Any.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: ClientContext_AdditionalEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = Any.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ClientContext_AdditionalEntry>, I>>(base?: I): ClientContext_AdditionalEntry {
    return ClientContext_AdditionalEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ClientContext_AdditionalEntry>, I>>(
    object: I,
  ): ClientContext_AdditionalEntry {
    const message = createBaseClientContext_AdditionalEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null) ? Any.fromPartial(object.value) : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
