// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v3.21.12
// source: file_storage.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Observable } from "rxjs";
import { map } from "rxjs/operators";
import { HttpBody } from "./google/api/httpbody";
import { Empty } from "./google/protobuf/empty";
import { Timestamp } from "./google/protobuf/timestamp";

export const protobufPackage = "stew.api.v1";

export enum UploadSessionStatus {
  UPLOAD_SESSION_STATUS_UNSPECIFIED = 0,
  UPLOAD_SESSION_STATUS_ACTIVE = 1,
  UPLOAD_SESSION_STATUS_COMPLETED = 2,
  UPLOAD_SESSION_STATUS_ABORTED = 3,
  UNRECOGNIZED = -1,
}

export function uploadSessionStatusFromJSON(object: any): UploadSessionStatus {
  switch (object) {
    case 0:
    case "UPLOAD_SESSION_STATUS_UNSPECIFIED":
      return UploadSessionStatus.UPLOAD_SESSION_STATUS_UNSPECIFIED;
    case 1:
    case "UPLOAD_SESSION_STATUS_ACTIVE":
      return UploadSessionStatus.UPLOAD_SESSION_STATUS_ACTIVE;
    case 2:
    case "UPLOAD_SESSION_STATUS_COMPLETED":
      return UploadSessionStatus.UPLOAD_SESSION_STATUS_COMPLETED;
    case 3:
    case "UPLOAD_SESSION_STATUS_ABORTED":
      return UploadSessionStatus.UPLOAD_SESSION_STATUS_ABORTED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return UploadSessionStatus.UNRECOGNIZED;
  }
}

export function uploadSessionStatusToJSON(object: UploadSessionStatus): string {
  switch (object) {
    case UploadSessionStatus.UPLOAD_SESSION_STATUS_UNSPECIFIED:
      return "UPLOAD_SESSION_STATUS_UNSPECIFIED";
    case UploadSessionStatus.UPLOAD_SESSION_STATUS_ACTIVE:
      return "UPLOAD_SESSION_STATUS_ACTIVE";
    case UploadSessionStatus.UPLOAD_SESSION_STATUS_COMPLETED:
      return "UPLOAD_SESSION_STATUS_COMPLETED";
    case UploadSessionStatus.UPLOAD_SESSION_STATUS_ABORTED:
      return "UPLOAD_SESSION_STATUS_ABORTED";
    case UploadSessionStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum PathEntryKind {
  PATH_ENTRY_KIND_UNSPECIFIED = 0,
  PATH_ENTRY_KIND_FILE = 1,
  PATH_ENTRY_KIND_DIRECTORY = 2,
  UNRECOGNIZED = -1,
}

export function pathEntryKindFromJSON(object: any): PathEntryKind {
  switch (object) {
    case 0:
    case "PATH_ENTRY_KIND_UNSPECIFIED":
      return PathEntryKind.PATH_ENTRY_KIND_UNSPECIFIED;
    case 1:
    case "PATH_ENTRY_KIND_FILE":
      return PathEntryKind.PATH_ENTRY_KIND_FILE;
    case 2:
    case "PATH_ENTRY_KIND_DIRECTORY":
      return PathEntryKind.PATH_ENTRY_KIND_DIRECTORY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PathEntryKind.UNRECOGNIZED;
  }
}

export function pathEntryKindToJSON(object: PathEntryKind): string {
  switch (object) {
    case PathEntryKind.PATH_ENTRY_KIND_UNSPECIFIED:
      return "PATH_ENTRY_KIND_UNSPECIFIED";
    case PathEntryKind.PATH_ENTRY_KIND_FILE:
      return "PATH_ENTRY_KIND_FILE";
    case PathEntryKind.PATH_ENTRY_KIND_DIRECTORY:
      return "PATH_ENTRY_KIND_DIRECTORY";
    case PathEntryKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface UploadFileRequest {
  payload:
    | //
    /** First message: file metadata. */
    { $case: "metadata"; metadata: UploadFileMetadata }
    | //
    /** Subsequent messages: raw file data chunks (recommended 64KB each). */
    { $case: "chunk_data"; chunk_data: Uint8Array }
    | undefined;
}

export interface UploadFileMetadata {
  /** Original filename. */
  filename: string;
  /** MIME content type (e.g. "image/png", "application/pdf"). */
  content_type: string;
  /** Virtual folder path (e.g. "/documents", "/images"). Defaults to "/". */
  folder: string;
  /**
   * Arbitrary business context (JSON string). Forwarded to the callback endpoint
   * to associate this file with a business entity (e.g. order ID, ticket ID).
   */
  business_context: string;
}

export interface UploadFileResponse {
  /** Metadata of the newly created file. */
  file_info:
    | FileInfo
    | undefined;
  /** Business callback result (if callback is configured and sync mode is enabled). */
  callback_result: CallbackResult | undefined;
}

export interface InitResumableUploadRequest {
  /** Original filename. */
  filename: string;
  /** MIME content type. */
  content_type: string;
  /** Virtual folder path. Defaults to "/". */
  folder: string;
  /** Total file size in bytes. Required for computing part layout. */
  total_size: number;
  /** Desired part size in bytes. Server may adjust. Default: 5MB (5242880). */
  part_size: number;
  /** Arbitrary business context (JSON string). Forwarded to the callback endpoint. */
  business_context: string;
  /** Expected SHA-256 checksum computed by the client before multipart upload starts. */
  checksum: string;
}

export interface InitResumableUploadResponse {
  /** Opaque upload session identifier. */
  upload_id: string;
  /** Actual part size chosen by the server (bytes). */
  part_size: number;
  /** Total number of parts to upload. */
  total_parts: number;
  /** Session expiration time. Client must complete before this. */
  expires_at: Date | undefined;
}

export interface UploadPartRequest {
  payload:
    | //
    /** First message: part header. */
    { $case: "header"; header: UploadPartHeader }
    | //
    /** Subsequent messages: raw part data chunks. */
    { $case: "chunk_data"; chunk_data: Uint8Array }
    | undefined;
}

export interface UploadPartHeader {
  /** Upload session ID from InitResumableUpload. */
  upload_id: string;
  /** 1-based part number. */
  part_number: number;
}

export interface UploadPartResponse {
  /** Confirmed part number. */
  part_number: number;
  /** ETag (content hash) of the uploaded part. Required for CompleteResumableUpload. */
  etag: string;
  /** Number of bytes written for this part. */
  bytes_written: number;
}

export interface CompleteResumableUploadRequest {
  /** Upload session ID. */
  upload_id: string;
  /** List of all uploaded parts with their ETags. */
  parts: PartEtag[];
}

export interface PartEtag {
  /** 1-based part number. */
  part_number: number;
  /** ETag returned from UploadPart. */
  etag: string;
}

export interface AbortResumableUploadRequest {
  /** Upload session ID to abort. */
  upload_id: string;
}

export interface GetUploadStatusRequest {
  /** Upload session ID to query. */
  upload_id: string;
}

export interface GetUploadStatusResponse {
  /** Upload session ID. */
  upload_id: string;
  /** Current session status. */
  status: UploadSessionStatus;
  /** Parts that have been successfully uploaded. */
  completed_parts: UploadedPartInfo[];
  /** Total number of parts expected. */
  total_parts: number;
  /** Session expiration time. */
  expires_at:
    | Date
    | undefined;
  /** Original filename. */
  filename: string;
  /** Total file size in bytes. */
  total_size: number;
}

export interface UploadedPartInfo {
  part_number: number;
  etag: string;
  size: number;
  completed_at: Date | undefined;
}

export interface DownloadFileRequest {
  /** File ID to download. */
  file_id: string;
  /** Expected SHA-256 checksum supplied by the client for post-download verification. */
  checksum: string;
  /** If true, the server verifies checksum and returns an empty body instead of file content. */
  verify_only: boolean;
}

/**
 * HTTP metadata projected from google.api.HttpBody.extensions for download responses.
 * The gateway renderer maps this metadata to concrete HTTP headers.
 */
export interface DownloadFileHttpMetadata {
  /** Original filename. Preserved in the extension payload for downstream use. */
  filename: string;
  /** Full Content-Disposition header value to expose on the HTTP response. */
  content_disposition: string;
  /** Full ETag header value to expose on the HTTP response. */
  etag: string;
}

export interface DownloadFileChunk {
  /** Raw file data chunk. */
  data: Uint8Array;
}

export interface DeleteFileRequest {
  /** File ID to delete. */
  file_id: string;
}

export interface ListFilesRequest {
  /** Virtual folder path to filter by. Empty or "/" lists root. */
  folder: string;
  /** Maximum number of files to return. Default: 100. */
  page_size: number;
  /** Pagination token from previous response. */
  page_token: string;
}

export interface ListFilesResponse {
  files: FileInfo[];
  /** Token for fetching the next page. Empty if no more results. */
  next_page_token: string;
  /** Total number of files matching the filter. */
  total_count: number;
}

export interface GetFileInfoRequest {
  /** File ID to query. */
  file_id: string;
}

/** Business callback result returned when sync callback mode is enabled. */
export interface CallbackResult {
  /** Whether the business side accepted the file. */
  accepted: boolean;
  /** Business-assigned reference ID (e.g. order attachment ID). */
  business_id: string;
  /** Optional message from the business callback. */
  message: string;
}

/** File metadata returned by upload, list, and info operations. */
export interface FileInfo {
  /** Unique file identifier (UUID). */
  id: string;
  /** Original filename. */
  filename: string;
  /** MIME content type. */
  content_type: string;
  /** File size in bytes. */
  file_size: number;
  /** Virtual folder path. */
  folder: string;
  /** Owner user ID (sub claim). */
  owner_id: string;
  /** SHA-256 checksum of the file content. */
  checksum: string;
  /** Storage backend type ("s3" or "local"). */
  storage_backend: string;
  created_at: Date | undefined;
  updated_at:
    | Date
    | undefined;
  /** Local filesystem path if local_cache is enabled (for business service access). */
  local_path: string;
  /** Storage key (object path in the storage backend). */
  storage_key: string;
}

/** Reference to a file or directory by virtual path. */
export interface VirtualPathRef {
  /** Base folder. Empty or "/" means root. */
  folder: string;
  /** Relative path below folder. May contain '/'. */
  relative_path: string;
}

/** Selects a target by file_id or virtual path. */
export interface PathSelector {
  selector: { $case: "file_id"; file_id: string } | { $case: "path"; path: VirtualPathRef } | undefined;
}

/** A single entry in a virtual directory listing. */
export interface PathEntry {
  kind: PathEntryKind;
  /** Direct child name only, e.g. "docs" or "README.md". */
  name: string;
  /** Canonical absolute virtual path, e.g. "/docs/README.md". */
  path: string;
  /** Canonical parent folder. */
  parent_folder: string;
  /** File-only fields. */
  file_id: string;
  content_type: string;
  size_bytes: number;
  checksum: string;
  created_at: Date | undefined;
  updated_at:
    | Date
    | undefined;
  /** Directory-only field. File nodes always return false. */
  has_children: boolean;
}

export interface ListFolderRequest {
  /** Folder to browse. Empty or "/" means root. */
  folder: string;
  /** Maximum number of mixed entries to return. Default 100, max 1000. */
  page_size: number;
  /** Opaque pagination token. */
  page_token: string;
  /** Whether to include file entries. Default true. */
  include_files: boolean;
  /** Whether to include directory entries. Default true. */
  include_directories: boolean;
}

export interface ListFolderResponse {
  entries: PathEntry[];
  next_page_token: string;
  total_count: number;
}

export interface GetPathInfoRequest {
  selector: PathSelector | undefined;
}

export interface GetPathInfoResponse {
  entry: PathEntry | undefined;
}

export interface ReadTextFileRequest {
  selector:
    | PathSelector
    | undefined;
  /** Requested preview length. Server will clamp to configured hard limit. */
  max_bytes: number;
  /** Reject binary or undecodable content when true. */
  fail_if_binary: boolean;
}

export interface ReadTextFileResponse {
  entry: PathEntry | undefined;
  text: string;
  content_type: string;
  size_bytes: number;
  checksum: string;
  updated_at: Date | undefined;
  truncated: boolean;
  lossy: boolean;
}

function createBaseUploadFileRequest(): UploadFileRequest {
  return { payload: undefined };
}

export const UploadFileRequest: MessageFns<UploadFileRequest> = {
  encode(message: UploadFileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.payload?.$case) {
      case "metadata":
        UploadFileMetadata.encode(message.payload.metadata, writer.uint32(10).fork()).join();
        break;
      case "chunk_data":
        writer.uint32(18).bytes(message.payload.chunk_data);
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadFileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadFileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = { $case: "metadata", metadata: UploadFileMetadata.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.payload = { $case: "chunk_data", chunk_data: reader.bytes() };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadFileRequest {
    return {
      payload: isSet(object.metadata)
        ? { $case: "metadata", metadata: UploadFileMetadata.fromJSON(object.metadata) }
        : isSet(object.chunkData)
        ? { $case: "chunk_data", chunk_data: bytesFromBase64(object.chunkData) }
        : isSet(object.chunk_data)
        ? { $case: "chunk_data", chunk_data: bytesFromBase64(object.chunk_data) }
        : undefined,
    };
  },

  toJSON(message: UploadFileRequest): unknown {
    const obj: any = {};
    if (message.payload?.$case === "metadata") {
      obj.metadata = UploadFileMetadata.toJSON(message.payload.metadata);
    } else if (message.payload?.$case === "chunk_data") {
      obj.chunkData = base64FromBytes(message.payload.chunk_data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UploadFileRequest>, I>>(base?: I): UploadFileRequest {
    return UploadFileRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UploadFileRequest>, I>>(object: I): UploadFileRequest {
    const message = createBaseUploadFileRequest();
    switch (object.payload?.$case) {
      case "metadata": {
        if (object.payload?.metadata !== undefined && object.payload?.metadata !== null) {
          message.payload = { $case: "metadata", metadata: UploadFileMetadata.fromPartial(object.payload.metadata) };
        }
        break;
      }
      case "chunk_data": {
        if (object.payload?.chunk_data !== undefined && object.payload?.chunk_data !== null) {
          message.payload = { $case: "chunk_data", chunk_data: object.payload.chunk_data };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseUploadFileMetadata(): UploadFileMetadata {
  return { filename: "", content_type: "", folder: "", business_context: "" };
}

export const UploadFileMetadata: MessageFns<UploadFileMetadata> = {
  encode(message: UploadFileMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.filename !== "") {
      writer.uint32(10).string(message.filename);
    }
    if (message.content_type !== "") {
      writer.uint32(18).string(message.content_type);
    }
    if (message.folder !== "") {
      writer.uint32(26).string(message.folder);
    }
    if (message.business_context !== "") {
      writer.uint32(34).string(message.business_context);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadFileMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadFileMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.filename = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content_type = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.folder = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.business_context = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadFileMetadata {
    return {
      filename: isSet(object.filename) ? globalThis.String(object.filename) : "",
      content_type: isSet(object.contentType)
        ? globalThis.String(object.contentType)
        : isSet(object.content_type)
        ? globalThis.String(object.content_type)
        : "",
      folder: isSet(object.folder) ? globalThis.String(object.folder) : "",
      business_context: isSet(object.businessContext)
        ? globalThis.String(object.businessContext)
        : isSet(object.business_context)
        ? globalThis.String(object.business_context)
        : "",
    };
  },

  toJSON(message: UploadFileMetadata): unknown {
    const obj: any = {};
    if (message.filename !== "") {
      obj.filename = message.filename;
    }
    if (message.content_type !== "") {
      obj.contentType = message.content_type;
    }
    if (message.folder !== "") {
      obj.folder = message.folder;
    }
    if (message.business_context !== "") {
      obj.businessContext = message.business_context;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UploadFileMetadata>, I>>(base?: I): UploadFileMetadata {
    return UploadFileMetadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UploadFileMetadata>, I>>(object: I): UploadFileMetadata {
    const message = createBaseUploadFileMetadata();
    message.filename = object.filename ?? "";
    message.content_type = object.content_type ?? "";
    message.folder = object.folder ?? "";
    message.business_context = object.business_context ?? "";
    return message;
  },
};

function createBaseUploadFileResponse(): UploadFileResponse {
  return { file_info: undefined, callback_result: undefined };
}

export const UploadFileResponse: MessageFns<UploadFileResponse> = {
  encode(message: UploadFileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.file_info !== undefined) {
      FileInfo.encode(message.file_info, writer.uint32(10).fork()).join();
    }
    if (message.callback_result !== undefined) {
      CallbackResult.encode(message.callback_result, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadFileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadFileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.file_info = FileInfo.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.callback_result = CallbackResult.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadFileResponse {
    return {
      file_info: isSet(object.fileInfo)
        ? FileInfo.fromJSON(object.fileInfo)
        : isSet(object.file_info)
        ? FileInfo.fromJSON(object.file_info)
        : undefined,
      callback_result: isSet(object.callbackResult)
        ? CallbackResult.fromJSON(object.callbackResult)
        : isSet(object.callback_result)
        ? CallbackResult.fromJSON(object.callback_result)
        : undefined,
    };
  },

  toJSON(message: UploadFileResponse): unknown {
    const obj: any = {};
    if (message.file_info !== undefined) {
      obj.fileInfo = FileInfo.toJSON(message.file_info);
    }
    if (message.callback_result !== undefined) {
      obj.callbackResult = CallbackResult.toJSON(message.callback_result);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UploadFileResponse>, I>>(base?: I): UploadFileResponse {
    return UploadFileResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UploadFileResponse>, I>>(object: I): UploadFileResponse {
    const message = createBaseUploadFileResponse();
    message.file_info = (object.file_info !== undefined && object.file_info !== null)
      ? FileInfo.fromPartial(object.file_info)
      : undefined;
    message.callback_result = (object.callback_result !== undefined && object.callback_result !== null)
      ? CallbackResult.fromPartial(object.callback_result)
      : undefined;
    return message;
  },
};

function createBaseInitResumableUploadRequest(): InitResumableUploadRequest {
  return {
    filename: "",
    content_type: "",
    folder: "",
    total_size: 0,
    part_size: 0,
    business_context: "",
    checksum: "",
  };
}

export const InitResumableUploadRequest: MessageFns<InitResumableUploadRequest> = {
  encode(message: InitResumableUploadRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.filename !== "") {
      writer.uint32(10).string(message.filename);
    }
    if (message.content_type !== "") {
      writer.uint32(18).string(message.content_type);
    }
    if (message.folder !== "") {
      writer.uint32(26).string(message.folder);
    }
    if (message.total_size !== 0) {
      writer.uint32(32).int64(message.total_size);
    }
    if (message.part_size !== 0) {
      writer.uint32(40).int32(message.part_size);
    }
    if (message.business_context !== "") {
      writer.uint32(50).string(message.business_context);
    }
    if (message.checksum !== "") {
      writer.uint32(58).string(message.checksum);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InitResumableUploadRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInitResumableUploadRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.filename = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content_type = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.folder = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.total_size = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.part_size = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.business_context = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.checksum = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InitResumableUploadRequest {
    return {
      filename: isSet(object.filename) ? globalThis.String(object.filename) : "",
      content_type: isSet(object.contentType)
        ? globalThis.String(object.contentType)
        : isSet(object.content_type)
        ? globalThis.String(object.content_type)
        : "",
      folder: isSet(object.folder) ? globalThis.String(object.folder) : "",
      total_size: isSet(object.totalSize)
        ? globalThis.Number(object.totalSize)
        : isSet(object.total_size)
        ? globalThis.Number(object.total_size)
        : 0,
      part_size: isSet(object.partSize)
        ? globalThis.Number(object.partSize)
        : isSet(object.part_size)
        ? globalThis.Number(object.part_size)
        : 0,
      business_context: isSet(object.businessContext)
        ? globalThis.String(object.businessContext)
        : isSet(object.business_context)
        ? globalThis.String(object.business_context)
        : "",
      checksum: isSet(object.checksum) ? globalThis.String(object.checksum) : "",
    };
  },

  toJSON(message: InitResumableUploadRequest): unknown {
    const obj: any = {};
    if (message.filename !== "") {
      obj.filename = message.filename;
    }
    if (message.content_type !== "") {
      obj.contentType = message.content_type;
    }
    if (message.folder !== "") {
      obj.folder = message.folder;
    }
    if (message.total_size !== 0) {
      obj.totalSize = Math.round(message.total_size);
    }
    if (message.part_size !== 0) {
      obj.partSize = Math.round(message.part_size);
    }
    if (message.business_context !== "") {
      obj.businessContext = message.business_context;
    }
    if (message.checksum !== "") {
      obj.checksum = message.checksum;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InitResumableUploadRequest>, I>>(base?: I): InitResumableUploadRequest {
    return InitResumableUploadRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InitResumableUploadRequest>, I>>(object: I): InitResumableUploadRequest {
    const message = createBaseInitResumableUploadRequest();
    message.filename = object.filename ?? "";
    message.content_type = object.content_type ?? "";
    message.folder = object.folder ?? "";
    message.total_size = object.total_size ?? 0;
    message.part_size = object.part_size ?? 0;
    message.business_context = object.business_context ?? "";
    message.checksum = object.checksum ?? "";
    return message;
  },
};

function createBaseInitResumableUploadResponse(): InitResumableUploadResponse {
  return { upload_id: "", part_size: 0, total_parts: 0, expires_at: undefined };
}

export const InitResumableUploadResponse: MessageFns<InitResumableUploadResponse> = {
  encode(message: InitResumableUploadResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.upload_id !== "") {
      writer.uint32(10).string(message.upload_id);
    }
    if (message.part_size !== 0) {
      writer.uint32(16).int32(message.part_size);
    }
    if (message.total_parts !== 0) {
      writer.uint32(24).int32(message.total_parts);
    }
    if (message.expires_at !== undefined) {
      Timestamp.encode(toTimestamp(message.expires_at), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InitResumableUploadResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInitResumableUploadResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.upload_id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.part_size = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.total_parts = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.expires_at = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InitResumableUploadResponse {
    return {
      upload_id: isSet(object.uploadId)
        ? globalThis.String(object.uploadId)
        : isSet(object.upload_id)
        ? globalThis.String(object.upload_id)
        : "",
      part_size: isSet(object.partSize)
        ? globalThis.Number(object.partSize)
        : isSet(object.part_size)
        ? globalThis.Number(object.part_size)
        : 0,
      total_parts: isSet(object.totalParts)
        ? globalThis.Number(object.totalParts)
        : isSet(object.total_parts)
        ? globalThis.Number(object.total_parts)
        : 0,
      expires_at: isSet(object.expiresAt)
        ? fromJsonTimestamp(object.expiresAt)
        : isSet(object.expires_at)
        ? fromJsonTimestamp(object.expires_at)
        : undefined,
    };
  },

  toJSON(message: InitResumableUploadResponse): unknown {
    const obj: any = {};
    if (message.upload_id !== "") {
      obj.uploadId = message.upload_id;
    }
    if (message.part_size !== 0) {
      obj.partSize = Math.round(message.part_size);
    }
    if (message.total_parts !== 0) {
      obj.totalParts = Math.round(message.total_parts);
    }
    if (message.expires_at !== undefined) {
      obj.expiresAt = message.expires_at.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InitResumableUploadResponse>, I>>(base?: I): InitResumableUploadResponse {
    return InitResumableUploadResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InitResumableUploadResponse>, I>>(object: I): InitResumableUploadResponse {
    const message = createBaseInitResumableUploadResponse();
    message.upload_id = object.upload_id ?? "";
    message.part_size = object.part_size ?? 0;
    message.total_parts = object.total_parts ?? 0;
    message.expires_at = object.expires_at ?? undefined;
    return message;
  },
};

function createBaseUploadPartRequest(): UploadPartRequest {
  return { payload: undefined };
}

export const UploadPartRequest: MessageFns<UploadPartRequest> = {
  encode(message: UploadPartRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.payload?.$case) {
      case "header":
        UploadPartHeader.encode(message.payload.header, writer.uint32(10).fork()).join();
        break;
      case "chunk_data":
        writer.uint32(18).bytes(message.payload.chunk_data);
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadPartRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadPartRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = { $case: "header", header: UploadPartHeader.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.payload = { $case: "chunk_data", chunk_data: reader.bytes() };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadPartRequest {
    return {
      payload: isSet(object.header)
        ? { $case: "header", header: UploadPartHeader.fromJSON(object.header) }
        : isSet(object.chunkData)
        ? { $case: "chunk_data", chunk_data: bytesFromBase64(object.chunkData) }
        : isSet(object.chunk_data)
        ? { $case: "chunk_data", chunk_data: bytesFromBase64(object.chunk_data) }
        : undefined,
    };
  },

  toJSON(message: UploadPartRequest): unknown {
    const obj: any = {};
    if (message.payload?.$case === "header") {
      obj.header = UploadPartHeader.toJSON(message.payload.header);
    } else if (message.payload?.$case === "chunk_data") {
      obj.chunkData = base64FromBytes(message.payload.chunk_data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UploadPartRequest>, I>>(base?: I): UploadPartRequest {
    return UploadPartRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UploadPartRequest>, I>>(object: I): UploadPartRequest {
    const message = createBaseUploadPartRequest();
    switch (object.payload?.$case) {
      case "header": {
        if (object.payload?.header !== undefined && object.payload?.header !== null) {
          message.payload = { $case: "header", header: UploadPartHeader.fromPartial(object.payload.header) };
        }
        break;
      }
      case "chunk_data": {
        if (object.payload?.chunk_data !== undefined && object.payload?.chunk_data !== null) {
          message.payload = { $case: "chunk_data", chunk_data: object.payload.chunk_data };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseUploadPartHeader(): UploadPartHeader {
  return { upload_id: "", part_number: 0 };
}

export const UploadPartHeader: MessageFns<UploadPartHeader> = {
  encode(message: UploadPartHeader, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.upload_id !== "") {
      writer.uint32(10).string(message.upload_id);
    }
    if (message.part_number !== 0) {
      writer.uint32(16).int32(message.part_number);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadPartHeader {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadPartHeader();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.upload_id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.part_number = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadPartHeader {
    return {
      upload_id: isSet(object.uploadId)
        ? globalThis.String(object.uploadId)
        : isSet(object.upload_id)
        ? globalThis.String(object.upload_id)
        : "",
      part_number: isSet(object.partNumber)
        ? globalThis.Number(object.partNumber)
        : isSet(object.part_number)
        ? globalThis.Number(object.part_number)
        : 0,
    };
  },

  toJSON(message: UploadPartHeader): unknown {
    const obj: any = {};
    if (message.upload_id !== "") {
      obj.uploadId = message.upload_id;
    }
    if (message.part_number !== 0) {
      obj.partNumber = Math.round(message.part_number);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UploadPartHeader>, I>>(base?: I): UploadPartHeader {
    return UploadPartHeader.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UploadPartHeader>, I>>(object: I): UploadPartHeader {
    const message = createBaseUploadPartHeader();
    message.upload_id = object.upload_id ?? "";
    message.part_number = object.part_number ?? 0;
    return message;
  },
};

function createBaseUploadPartResponse(): UploadPartResponse {
  return { part_number: 0, etag: "", bytes_written: 0 };
}

export const UploadPartResponse: MessageFns<UploadPartResponse> = {
  encode(message: UploadPartResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.part_number !== 0) {
      writer.uint32(8).int32(message.part_number);
    }
    if (message.etag !== "") {
      writer.uint32(18).string(message.etag);
    }
    if (message.bytes_written !== 0) {
      writer.uint32(24).int64(message.bytes_written);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadPartResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadPartResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.part_number = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.etag = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.bytes_written = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadPartResponse {
    return {
      part_number: isSet(object.partNumber)
        ? globalThis.Number(object.partNumber)
        : isSet(object.part_number)
        ? globalThis.Number(object.part_number)
        : 0,
      etag: isSet(object.etag) ? globalThis.String(object.etag) : "",
      bytes_written: isSet(object.bytesWritten)
        ? globalThis.Number(object.bytesWritten)
        : isSet(object.bytes_written)
        ? globalThis.Number(object.bytes_written)
        : 0,
    };
  },

  toJSON(message: UploadPartResponse): unknown {
    const obj: any = {};
    if (message.part_number !== 0) {
      obj.partNumber = Math.round(message.part_number);
    }
    if (message.etag !== "") {
      obj.etag = message.etag;
    }
    if (message.bytes_written !== 0) {
      obj.bytesWritten = Math.round(message.bytes_written);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UploadPartResponse>, I>>(base?: I): UploadPartResponse {
    return UploadPartResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UploadPartResponse>, I>>(object: I): UploadPartResponse {
    const message = createBaseUploadPartResponse();
    message.part_number = object.part_number ?? 0;
    message.etag = object.etag ?? "";
    message.bytes_written = object.bytes_written ?? 0;
    return message;
  },
};

function createBaseCompleteResumableUploadRequest(): CompleteResumableUploadRequest {
  return { upload_id: "", parts: [] };
}

export const CompleteResumableUploadRequest: MessageFns<CompleteResumableUploadRequest> = {
  encode(message: CompleteResumableUploadRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.upload_id !== "") {
      writer.uint32(10).string(message.upload_id);
    }
    for (const v of message.parts) {
      PartEtag.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CompleteResumableUploadRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCompleteResumableUploadRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.upload_id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.parts.push(PartEtag.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CompleteResumableUploadRequest {
    return {
      upload_id: isSet(object.uploadId)
        ? globalThis.String(object.uploadId)
        : isSet(object.upload_id)
        ? globalThis.String(object.upload_id)
        : "",
      parts: globalThis.Array.isArray(object?.parts) ? object.parts.map((e: any) => PartEtag.fromJSON(e)) : [],
    };
  },

  toJSON(message: CompleteResumableUploadRequest): unknown {
    const obj: any = {};
    if (message.upload_id !== "") {
      obj.uploadId = message.upload_id;
    }
    if (message.parts?.length) {
      obj.parts = message.parts.map((e) => PartEtag.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CompleteResumableUploadRequest>, I>>(base?: I): CompleteResumableUploadRequest {
    return CompleteResumableUploadRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CompleteResumableUploadRequest>, I>>(
    object: I,
  ): CompleteResumableUploadRequest {
    const message = createBaseCompleteResumableUploadRequest();
    message.upload_id = object.upload_id ?? "";
    message.parts = object.parts?.map((e) => PartEtag.fromPartial(e)) || [];
    return message;
  },
};

function createBasePartEtag(): PartEtag {
  return { part_number: 0, etag: "" };
}

export const PartEtag: MessageFns<PartEtag> = {
  encode(message: PartEtag, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.part_number !== 0) {
      writer.uint32(8).int32(message.part_number);
    }
    if (message.etag !== "") {
      writer.uint32(18).string(message.etag);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PartEtag {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePartEtag();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.part_number = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.etag = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PartEtag {
    return {
      part_number: isSet(object.partNumber)
        ? globalThis.Number(object.partNumber)
        : isSet(object.part_number)
        ? globalThis.Number(object.part_number)
        : 0,
      etag: isSet(object.etag) ? globalThis.String(object.etag) : "",
    };
  },

  toJSON(message: PartEtag): unknown {
    const obj: any = {};
    if (message.part_number !== 0) {
      obj.partNumber = Math.round(message.part_number);
    }
    if (message.etag !== "") {
      obj.etag = message.etag;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PartEtag>, I>>(base?: I): PartEtag {
    return PartEtag.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PartEtag>, I>>(object: I): PartEtag {
    const message = createBasePartEtag();
    message.part_number = object.part_number ?? 0;
    message.etag = object.etag ?? "";
    return message;
  },
};

function createBaseAbortResumableUploadRequest(): AbortResumableUploadRequest {
  return { upload_id: "" };
}

export const AbortResumableUploadRequest: MessageFns<AbortResumableUploadRequest> = {
  encode(message: AbortResumableUploadRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.upload_id !== "") {
      writer.uint32(10).string(message.upload_id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AbortResumableUploadRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAbortResumableUploadRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.upload_id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AbortResumableUploadRequest {
    return {
      upload_id: isSet(object.uploadId)
        ? globalThis.String(object.uploadId)
        : isSet(object.upload_id)
        ? globalThis.String(object.upload_id)
        : "",
    };
  },

  toJSON(message: AbortResumableUploadRequest): unknown {
    const obj: any = {};
    if (message.upload_id !== "") {
      obj.uploadId = message.upload_id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AbortResumableUploadRequest>, I>>(base?: I): AbortResumableUploadRequest {
    return AbortResumableUploadRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AbortResumableUploadRequest>, I>>(object: I): AbortResumableUploadRequest {
    const message = createBaseAbortResumableUploadRequest();
    message.upload_id = object.upload_id ?? "";
    return message;
  },
};

function createBaseGetUploadStatusRequest(): GetUploadStatusRequest {
  return { upload_id: "" };
}

export const GetUploadStatusRequest: MessageFns<GetUploadStatusRequest> = {
  encode(message: GetUploadStatusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.upload_id !== "") {
      writer.uint32(10).string(message.upload_id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUploadStatusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUploadStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.upload_id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUploadStatusRequest {
    return {
      upload_id: isSet(object.uploadId)
        ? globalThis.String(object.uploadId)
        : isSet(object.upload_id)
        ? globalThis.String(object.upload_id)
        : "",
    };
  },

  toJSON(message: GetUploadStatusRequest): unknown {
    const obj: any = {};
    if (message.upload_id !== "") {
      obj.uploadId = message.upload_id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetUploadStatusRequest>, I>>(base?: I): GetUploadStatusRequest {
    return GetUploadStatusRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetUploadStatusRequest>, I>>(object: I): GetUploadStatusRequest {
    const message = createBaseGetUploadStatusRequest();
    message.upload_id = object.upload_id ?? "";
    return message;
  },
};

function createBaseGetUploadStatusResponse(): GetUploadStatusResponse {
  return {
    upload_id: "",
    status: 0,
    completed_parts: [],
    total_parts: 0,
    expires_at: undefined,
    filename: "",
    total_size: 0,
  };
}

export const GetUploadStatusResponse: MessageFns<GetUploadStatusResponse> = {
  encode(message: GetUploadStatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.upload_id !== "") {
      writer.uint32(10).string(message.upload_id);
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    for (const v of message.completed_parts) {
      UploadedPartInfo.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.total_parts !== 0) {
      writer.uint32(32).int32(message.total_parts);
    }
    if (message.expires_at !== undefined) {
      Timestamp.encode(toTimestamp(message.expires_at), writer.uint32(42).fork()).join();
    }
    if (message.filename !== "") {
      writer.uint32(50).string(message.filename);
    }
    if (message.total_size !== 0) {
      writer.uint32(56).int64(message.total_size);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUploadStatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUploadStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.upload_id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.completed_parts.push(UploadedPartInfo.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.total_parts = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.expires_at = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.filename = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.total_size = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUploadStatusResponse {
    return {
      upload_id: isSet(object.uploadId)
        ? globalThis.String(object.uploadId)
        : isSet(object.upload_id)
        ? globalThis.String(object.upload_id)
        : "",
      status: isSet(object.status) ? uploadSessionStatusFromJSON(object.status) : 0,
      completed_parts: globalThis.Array.isArray(object?.completedParts)
        ? object.completedParts.map((e: any) => UploadedPartInfo.fromJSON(e))
        : globalThis.Array.isArray(object?.completed_parts)
        ? object.completed_parts.map((e: any) => UploadedPartInfo.fromJSON(e))
        : [],
      total_parts: isSet(object.totalParts)
        ? globalThis.Number(object.totalParts)
        : isSet(object.total_parts)
        ? globalThis.Number(object.total_parts)
        : 0,
      expires_at: isSet(object.expiresAt)
        ? fromJsonTimestamp(object.expiresAt)
        : isSet(object.expires_at)
        ? fromJsonTimestamp(object.expires_at)
        : undefined,
      filename: isSet(object.filename) ? globalThis.String(object.filename) : "",
      total_size: isSet(object.totalSize)
        ? globalThis.Number(object.totalSize)
        : isSet(object.total_size)
        ? globalThis.Number(object.total_size)
        : 0,
    };
  },

  toJSON(message: GetUploadStatusResponse): unknown {
    const obj: any = {};
    if (message.upload_id !== "") {
      obj.uploadId = message.upload_id;
    }
    if (message.status !== 0) {
      obj.status = uploadSessionStatusToJSON(message.status);
    }
    if (message.completed_parts?.length) {
      obj.completedParts = message.completed_parts.map((e) => UploadedPartInfo.toJSON(e));
    }
    if (message.total_parts !== 0) {
      obj.totalParts = Math.round(message.total_parts);
    }
    if (message.expires_at !== undefined) {
      obj.expiresAt = message.expires_at.toISOString();
    }
    if (message.filename !== "") {
      obj.filename = message.filename;
    }
    if (message.total_size !== 0) {
      obj.totalSize = Math.round(message.total_size);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetUploadStatusResponse>, I>>(base?: I): GetUploadStatusResponse {
    return GetUploadStatusResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetUploadStatusResponse>, I>>(object: I): GetUploadStatusResponse {
    const message = createBaseGetUploadStatusResponse();
    message.upload_id = object.upload_id ?? "";
    message.status = object.status ?? 0;
    message.completed_parts = object.completed_parts?.map((e) => UploadedPartInfo.fromPartial(e)) || [];
    message.total_parts = object.total_parts ?? 0;
    message.expires_at = object.expires_at ?? undefined;
    message.filename = object.filename ?? "";
    message.total_size = object.total_size ?? 0;
    return message;
  },
};

function createBaseUploadedPartInfo(): UploadedPartInfo {
  return { part_number: 0, etag: "", size: 0, completed_at: undefined };
}

export const UploadedPartInfo: MessageFns<UploadedPartInfo> = {
  encode(message: UploadedPartInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.part_number !== 0) {
      writer.uint32(8).int32(message.part_number);
    }
    if (message.etag !== "") {
      writer.uint32(18).string(message.etag);
    }
    if (message.size !== 0) {
      writer.uint32(24).int64(message.size);
    }
    if (message.completed_at !== undefined) {
      Timestamp.encode(toTimestamp(message.completed_at), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadedPartInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadedPartInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.part_number = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.etag = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.size = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.completed_at = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadedPartInfo {
    return {
      part_number: isSet(object.partNumber)
        ? globalThis.Number(object.partNumber)
        : isSet(object.part_number)
        ? globalThis.Number(object.part_number)
        : 0,
      etag: isSet(object.etag) ? globalThis.String(object.etag) : "",
      size: isSet(object.size) ? globalThis.Number(object.size) : 0,
      completed_at: isSet(object.completedAt)
        ? fromJsonTimestamp(object.completedAt)
        : isSet(object.completed_at)
        ? fromJsonTimestamp(object.completed_at)
        : undefined,
    };
  },

  toJSON(message: UploadedPartInfo): unknown {
    const obj: any = {};
    if (message.part_number !== 0) {
      obj.partNumber = Math.round(message.part_number);
    }
    if (message.etag !== "") {
      obj.etag = message.etag;
    }
    if (message.size !== 0) {
      obj.size = Math.round(message.size);
    }
    if (message.completed_at !== undefined) {
      obj.completedAt = message.completed_at.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UploadedPartInfo>, I>>(base?: I): UploadedPartInfo {
    return UploadedPartInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UploadedPartInfo>, I>>(object: I): UploadedPartInfo {
    const message = createBaseUploadedPartInfo();
    message.part_number = object.part_number ?? 0;
    message.etag = object.etag ?? "";
    message.size = object.size ?? 0;
    message.completed_at = object.completed_at ?? undefined;
    return message;
  },
};

function createBaseDownloadFileRequest(): DownloadFileRequest {
  return { file_id: "", checksum: "", verify_only: false };
}

export const DownloadFileRequest: MessageFns<DownloadFileRequest> = {
  encode(message: DownloadFileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.file_id !== "") {
      writer.uint32(10).string(message.file_id);
    }
    if (message.checksum !== "") {
      writer.uint32(18).string(message.checksum);
    }
    if (message.verify_only !== false) {
      writer.uint32(24).bool(message.verify_only);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DownloadFileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDownloadFileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.file_id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.checksum = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.verify_only = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DownloadFileRequest {
    return {
      file_id: isSet(object.fileId)
        ? globalThis.String(object.fileId)
        : isSet(object.file_id)
        ? globalThis.String(object.file_id)
        : "",
      checksum: isSet(object.checksum) ? globalThis.String(object.checksum) : "",
      verify_only: isSet(object.verifyOnly)
        ? globalThis.Boolean(object.verifyOnly)
        : isSet(object.verify_only)
        ? globalThis.Boolean(object.verify_only)
        : false,
    };
  },

  toJSON(message: DownloadFileRequest): unknown {
    const obj: any = {};
    if (message.file_id !== "") {
      obj.fileId = message.file_id;
    }
    if (message.checksum !== "") {
      obj.checksum = message.checksum;
    }
    if (message.verify_only !== false) {
      obj.verifyOnly = message.verify_only;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DownloadFileRequest>, I>>(base?: I): DownloadFileRequest {
    return DownloadFileRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DownloadFileRequest>, I>>(object: I): DownloadFileRequest {
    const message = createBaseDownloadFileRequest();
    message.file_id = object.file_id ?? "";
    message.checksum = object.checksum ?? "";
    message.verify_only = object.verify_only ?? false;
    return message;
  },
};

function createBaseDownloadFileHttpMetadata(): DownloadFileHttpMetadata {
  return { filename: "", content_disposition: "", etag: "" };
}

export const DownloadFileHttpMetadata: MessageFns<DownloadFileHttpMetadata> = {
  encode(message: DownloadFileHttpMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.filename !== "") {
      writer.uint32(10).string(message.filename);
    }
    if (message.content_disposition !== "") {
      writer.uint32(18).string(message.content_disposition);
    }
    if (message.etag !== "") {
      writer.uint32(26).string(message.etag);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DownloadFileHttpMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDownloadFileHttpMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.filename = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content_disposition = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.etag = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DownloadFileHttpMetadata {
    return {
      filename: isSet(object.filename) ? globalThis.String(object.filename) : "",
      content_disposition: isSet(object.contentDisposition)
        ? globalThis.String(object.contentDisposition)
        : isSet(object.content_disposition)
        ? globalThis.String(object.content_disposition)
        : "",
      etag: isSet(object.etag) ? globalThis.String(object.etag) : "",
    };
  },

  toJSON(message: DownloadFileHttpMetadata): unknown {
    const obj: any = {};
    if (message.filename !== "") {
      obj.filename = message.filename;
    }
    if (message.content_disposition !== "") {
      obj.contentDisposition = message.content_disposition;
    }
    if (message.etag !== "") {
      obj.etag = message.etag;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DownloadFileHttpMetadata>, I>>(base?: I): DownloadFileHttpMetadata {
    return DownloadFileHttpMetadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DownloadFileHttpMetadata>, I>>(object: I): DownloadFileHttpMetadata {
    const message = createBaseDownloadFileHttpMetadata();
    message.filename = object.filename ?? "";
    message.content_disposition = object.content_disposition ?? "";
    message.etag = object.etag ?? "";
    return message;
  },
};

function createBaseDownloadFileChunk(): DownloadFileChunk {
  return { data: new Uint8Array(0) };
}

export const DownloadFileChunk: MessageFns<DownloadFileChunk> = {
  encode(message: DownloadFileChunk, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data.length !== 0) {
      writer.uint32(10).bytes(message.data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DownloadFileChunk {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDownloadFileChunk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DownloadFileChunk {
    return { data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0) };
  },

  toJSON(message: DownloadFileChunk): unknown {
    const obj: any = {};
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DownloadFileChunk>, I>>(base?: I): DownloadFileChunk {
    return DownloadFileChunk.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DownloadFileChunk>, I>>(object: I): DownloadFileChunk {
    const message = createBaseDownloadFileChunk();
    message.data = object.data ?? new Uint8Array(0);
    return message;
  },
};

function createBaseDeleteFileRequest(): DeleteFileRequest {
  return { file_id: "" };
}

export const DeleteFileRequest: MessageFns<DeleteFileRequest> = {
  encode(message: DeleteFileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.file_id !== "") {
      writer.uint32(10).string(message.file_id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteFileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteFileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.file_id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteFileRequest {
    return {
      file_id: isSet(object.fileId)
        ? globalThis.String(object.fileId)
        : isSet(object.file_id)
        ? globalThis.String(object.file_id)
        : "",
    };
  },

  toJSON(message: DeleteFileRequest): unknown {
    const obj: any = {};
    if (message.file_id !== "") {
      obj.fileId = message.file_id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteFileRequest>, I>>(base?: I): DeleteFileRequest {
    return DeleteFileRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteFileRequest>, I>>(object: I): DeleteFileRequest {
    const message = createBaseDeleteFileRequest();
    message.file_id = object.file_id ?? "";
    return message;
  },
};

function createBaseListFilesRequest(): ListFilesRequest {
  return { folder: "", page_size: 0, page_token: "" };
}

export const ListFilesRequest: MessageFns<ListFilesRequest> = {
  encode(message: ListFilesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.folder !== "") {
      writer.uint32(10).string(message.folder);
    }
    if (message.page_size !== 0) {
      writer.uint32(16).int32(message.page_size);
    }
    if (message.page_token !== "") {
      writer.uint32(26).string(message.page_token);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListFilesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListFilesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.folder = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.page_size = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.page_token = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListFilesRequest {
    return {
      folder: isSet(object.folder) ? globalThis.String(object.folder) : "",
      page_size: isSet(object.pageSize)
        ? globalThis.Number(object.pageSize)
        : isSet(object.page_size)
        ? globalThis.Number(object.page_size)
        : 0,
      page_token: isSet(object.pageToken)
        ? globalThis.String(object.pageToken)
        : isSet(object.page_token)
        ? globalThis.String(object.page_token)
        : "",
    };
  },

  toJSON(message: ListFilesRequest): unknown {
    const obj: any = {};
    if (message.folder !== "") {
      obj.folder = message.folder;
    }
    if (message.page_size !== 0) {
      obj.pageSize = Math.round(message.page_size);
    }
    if (message.page_token !== "") {
      obj.pageToken = message.page_token;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListFilesRequest>, I>>(base?: I): ListFilesRequest {
    return ListFilesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListFilesRequest>, I>>(object: I): ListFilesRequest {
    const message = createBaseListFilesRequest();
    message.folder = object.folder ?? "";
    message.page_size = object.page_size ?? 0;
    message.page_token = object.page_token ?? "";
    return message;
  },
};

function createBaseListFilesResponse(): ListFilesResponse {
  return { files: [], next_page_token: "", total_count: 0 };
}

export const ListFilesResponse: MessageFns<ListFilesResponse> = {
  encode(message: ListFilesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.files) {
      FileInfo.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.next_page_token !== "") {
      writer.uint32(18).string(message.next_page_token);
    }
    if (message.total_count !== 0) {
      writer.uint32(24).int32(message.total_count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListFilesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListFilesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.files.push(FileInfo.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.next_page_token = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.total_count = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListFilesResponse {
    return {
      files: globalThis.Array.isArray(object?.files) ? object.files.map((e: any) => FileInfo.fromJSON(e)) : [],
      next_page_token: isSet(object.nextPageToken)
        ? globalThis.String(object.nextPageToken)
        : isSet(object.next_page_token)
        ? globalThis.String(object.next_page_token)
        : "",
      total_count: isSet(object.totalCount)
        ? globalThis.Number(object.totalCount)
        : isSet(object.total_count)
        ? globalThis.Number(object.total_count)
        : 0,
    };
  },

  toJSON(message: ListFilesResponse): unknown {
    const obj: any = {};
    if (message.files?.length) {
      obj.files = message.files.map((e) => FileInfo.toJSON(e));
    }
    if (message.next_page_token !== "") {
      obj.nextPageToken = message.next_page_token;
    }
    if (message.total_count !== 0) {
      obj.totalCount = Math.round(message.total_count);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListFilesResponse>, I>>(base?: I): ListFilesResponse {
    return ListFilesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListFilesResponse>, I>>(object: I): ListFilesResponse {
    const message = createBaseListFilesResponse();
    message.files = object.files?.map((e) => FileInfo.fromPartial(e)) || [];
    message.next_page_token = object.next_page_token ?? "";
    message.total_count = object.total_count ?? 0;
    return message;
  },
};

function createBaseGetFileInfoRequest(): GetFileInfoRequest {
  return { file_id: "" };
}

export const GetFileInfoRequest: MessageFns<GetFileInfoRequest> = {
  encode(message: GetFileInfoRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.file_id !== "") {
      writer.uint32(10).string(message.file_id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetFileInfoRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetFileInfoRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.file_id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetFileInfoRequest {
    return {
      file_id: isSet(object.fileId)
        ? globalThis.String(object.fileId)
        : isSet(object.file_id)
        ? globalThis.String(object.file_id)
        : "",
    };
  },

  toJSON(message: GetFileInfoRequest): unknown {
    const obj: any = {};
    if (message.file_id !== "") {
      obj.fileId = message.file_id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetFileInfoRequest>, I>>(base?: I): GetFileInfoRequest {
    return GetFileInfoRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetFileInfoRequest>, I>>(object: I): GetFileInfoRequest {
    const message = createBaseGetFileInfoRequest();
    message.file_id = object.file_id ?? "";
    return message;
  },
};

function createBaseCallbackResult(): CallbackResult {
  return { accepted: false, business_id: "", message: "" };
}

export const CallbackResult: MessageFns<CallbackResult> = {
  encode(message: CallbackResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accepted !== false) {
      writer.uint32(8).bool(message.accepted);
    }
    if (message.business_id !== "") {
      writer.uint32(18).string(message.business_id);
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CallbackResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCallbackResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.accepted = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.business_id = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CallbackResult {
    return {
      accepted: isSet(object.accepted) ? globalThis.Boolean(object.accepted) : false,
      business_id: isSet(object.businessId)
        ? globalThis.String(object.businessId)
        : isSet(object.business_id)
        ? globalThis.String(object.business_id)
        : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: CallbackResult): unknown {
    const obj: any = {};
    if (message.accepted !== false) {
      obj.accepted = message.accepted;
    }
    if (message.business_id !== "") {
      obj.businessId = message.business_id;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CallbackResult>, I>>(base?: I): CallbackResult {
    return CallbackResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CallbackResult>, I>>(object: I): CallbackResult {
    const message = createBaseCallbackResult();
    message.accepted = object.accepted ?? false;
    message.business_id = object.business_id ?? "";
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseFileInfo(): FileInfo {
  return {
    id: "",
    filename: "",
    content_type: "",
    file_size: 0,
    folder: "",
    owner_id: "",
    checksum: "",
    storage_backend: "",
    created_at: undefined,
    updated_at: undefined,
    local_path: "",
    storage_key: "",
  };
}

export const FileInfo: MessageFns<FileInfo> = {
  encode(message: FileInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.filename !== "") {
      writer.uint32(18).string(message.filename);
    }
    if (message.content_type !== "") {
      writer.uint32(26).string(message.content_type);
    }
    if (message.file_size !== 0) {
      writer.uint32(32).int64(message.file_size);
    }
    if (message.folder !== "") {
      writer.uint32(42).string(message.folder);
    }
    if (message.owner_id !== "") {
      writer.uint32(50).string(message.owner_id);
    }
    if (message.checksum !== "") {
      writer.uint32(58).string(message.checksum);
    }
    if (message.storage_backend !== "") {
      writer.uint32(66).string(message.storage_backend);
    }
    if (message.created_at !== undefined) {
      Timestamp.encode(toTimestamp(message.created_at), writer.uint32(74).fork()).join();
    }
    if (message.updated_at !== undefined) {
      Timestamp.encode(toTimestamp(message.updated_at), writer.uint32(82).fork()).join();
    }
    if (message.local_path !== "") {
      writer.uint32(90).string(message.local_path);
    }
    if (message.storage_key !== "") {
      writer.uint32(98).string(message.storage_key);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.filename = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.content_type = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.file_size = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.folder = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.owner_id = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.checksum = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.storage_backend = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.created_at = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.updated_at = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.local_path = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.storage_key = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileInfo {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      filename: isSet(object.filename) ? globalThis.String(object.filename) : "",
      content_type: isSet(object.contentType)
        ? globalThis.String(object.contentType)
        : isSet(object.content_type)
        ? globalThis.String(object.content_type)
        : "",
      file_size: isSet(object.fileSize)
        ? globalThis.Number(object.fileSize)
        : isSet(object.file_size)
        ? globalThis.Number(object.file_size)
        : 0,
      folder: isSet(object.folder) ? globalThis.String(object.folder) : "",
      owner_id: isSet(object.ownerId)
        ? globalThis.String(object.ownerId)
        : isSet(object.owner_id)
        ? globalThis.String(object.owner_id)
        : "",
      checksum: isSet(object.checksum) ? globalThis.String(object.checksum) : "",
      storage_backend: isSet(object.storageBackend)
        ? globalThis.String(object.storageBackend)
        : isSet(object.storage_backend)
        ? globalThis.String(object.storage_backend)
        : "",
      created_at: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
      updated_at: isSet(object.updatedAt)
        ? fromJsonTimestamp(object.updatedAt)
        : isSet(object.updated_at)
        ? fromJsonTimestamp(object.updated_at)
        : undefined,
      local_path: isSet(object.localPath)
        ? globalThis.String(object.localPath)
        : isSet(object.local_path)
        ? globalThis.String(object.local_path)
        : "",
      storage_key: isSet(object.storageKey)
        ? globalThis.String(object.storageKey)
        : isSet(object.storage_key)
        ? globalThis.String(object.storage_key)
        : "",
    };
  },

  toJSON(message: FileInfo): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.filename !== "") {
      obj.filename = message.filename;
    }
    if (message.content_type !== "") {
      obj.contentType = message.content_type;
    }
    if (message.file_size !== 0) {
      obj.fileSize = Math.round(message.file_size);
    }
    if (message.folder !== "") {
      obj.folder = message.folder;
    }
    if (message.owner_id !== "") {
      obj.ownerId = message.owner_id;
    }
    if (message.checksum !== "") {
      obj.checksum = message.checksum;
    }
    if (message.storage_backend !== "") {
      obj.storageBackend = message.storage_backend;
    }
    if (message.created_at !== undefined) {
      obj.createdAt = message.created_at.toISOString();
    }
    if (message.updated_at !== undefined) {
      obj.updatedAt = message.updated_at.toISOString();
    }
    if (message.local_path !== "") {
      obj.localPath = message.local_path;
    }
    if (message.storage_key !== "") {
      obj.storageKey = message.storage_key;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FileInfo>, I>>(base?: I): FileInfo {
    return FileInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FileInfo>, I>>(object: I): FileInfo {
    const message = createBaseFileInfo();
    message.id = object.id ?? "";
    message.filename = object.filename ?? "";
    message.content_type = object.content_type ?? "";
    message.file_size = object.file_size ?? 0;
    message.folder = object.folder ?? "";
    message.owner_id = object.owner_id ?? "";
    message.checksum = object.checksum ?? "";
    message.storage_backend = object.storage_backend ?? "";
    message.created_at = object.created_at ?? undefined;
    message.updated_at = object.updated_at ?? undefined;
    message.local_path = object.local_path ?? "";
    message.storage_key = object.storage_key ?? "";
    return message;
  },
};

function createBaseVirtualPathRef(): VirtualPathRef {
  return { folder: "", relative_path: "" };
}

export const VirtualPathRef: MessageFns<VirtualPathRef> = {
  encode(message: VirtualPathRef, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.folder !== "") {
      writer.uint32(10).string(message.folder);
    }
    if (message.relative_path !== "") {
      writer.uint32(18).string(message.relative_path);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VirtualPathRef {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVirtualPathRef();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.folder = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.relative_path = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VirtualPathRef {
    return {
      folder: isSet(object.folder) ? globalThis.String(object.folder) : "",
      relative_path: isSet(object.relativePath)
        ? globalThis.String(object.relativePath)
        : isSet(object.relative_path)
        ? globalThis.String(object.relative_path)
        : "",
    };
  },

  toJSON(message: VirtualPathRef): unknown {
    const obj: any = {};
    if (message.folder !== "") {
      obj.folder = message.folder;
    }
    if (message.relative_path !== "") {
      obj.relativePath = message.relative_path;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VirtualPathRef>, I>>(base?: I): VirtualPathRef {
    return VirtualPathRef.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VirtualPathRef>, I>>(object: I): VirtualPathRef {
    const message = createBaseVirtualPathRef();
    message.folder = object.folder ?? "";
    message.relative_path = object.relative_path ?? "";
    return message;
  },
};

function createBasePathSelector(): PathSelector {
  return { selector: undefined };
}

export const PathSelector: MessageFns<PathSelector> = {
  encode(message: PathSelector, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.selector?.$case) {
      case "file_id":
        writer.uint32(10).string(message.selector.file_id);
        break;
      case "path":
        VirtualPathRef.encode(message.selector.path, writer.uint32(18).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PathSelector {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePathSelector();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.selector = { $case: "file_id", file_id: reader.string() };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.selector = { $case: "path", path: VirtualPathRef.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PathSelector {
    return {
      selector: isSet(object.fileId)
        ? { $case: "file_id", file_id: globalThis.String(object.fileId) }
        : isSet(object.file_id)
        ? { $case: "file_id", file_id: globalThis.String(object.file_id) }
        : isSet(object.path)
        ? { $case: "path", path: VirtualPathRef.fromJSON(object.path) }
        : undefined,
    };
  },

  toJSON(message: PathSelector): unknown {
    const obj: any = {};
    if (message.selector?.$case === "file_id") {
      obj.fileId = message.selector.file_id;
    } else if (message.selector?.$case === "path") {
      obj.path = VirtualPathRef.toJSON(message.selector.path);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PathSelector>, I>>(base?: I): PathSelector {
    return PathSelector.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PathSelector>, I>>(object: I): PathSelector {
    const message = createBasePathSelector();
    switch (object.selector?.$case) {
      case "file_id": {
        if (object.selector?.file_id !== undefined && object.selector?.file_id !== null) {
          message.selector = { $case: "file_id", file_id: object.selector.file_id };
        }
        break;
      }
      case "path": {
        if (object.selector?.path !== undefined && object.selector?.path !== null) {
          message.selector = { $case: "path", path: VirtualPathRef.fromPartial(object.selector.path) };
        }
        break;
      }
    }
    return message;
  },
};

function createBasePathEntry(): PathEntry {
  return {
    kind: 0,
    name: "",
    path: "",
    parent_folder: "",
    file_id: "",
    content_type: "",
    size_bytes: 0,
    checksum: "",
    created_at: undefined,
    updated_at: undefined,
    has_children: false,
  };
}

export const PathEntry: MessageFns<PathEntry> = {
  encode(message: PathEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.kind !== 0) {
      writer.uint32(8).int32(message.kind);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.path !== "") {
      writer.uint32(26).string(message.path);
    }
    if (message.parent_folder !== "") {
      writer.uint32(34).string(message.parent_folder);
    }
    if (message.file_id !== "") {
      writer.uint32(42).string(message.file_id);
    }
    if (message.content_type !== "") {
      writer.uint32(50).string(message.content_type);
    }
    if (message.size_bytes !== 0) {
      writer.uint32(56).int64(message.size_bytes);
    }
    if (message.checksum !== "") {
      writer.uint32(66).string(message.checksum);
    }
    if (message.created_at !== undefined) {
      Timestamp.encode(toTimestamp(message.created_at), writer.uint32(74).fork()).join();
    }
    if (message.updated_at !== undefined) {
      Timestamp.encode(toTimestamp(message.updated_at), writer.uint32(82).fork()).join();
    }
    if (message.has_children !== false) {
      writer.uint32(88).bool(message.has_children);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PathEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePathEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.parent_folder = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.file_id = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.content_type = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.size_bytes = longToNumber(reader.int64());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.checksum = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.created_at = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.updated_at = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.has_children = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PathEntry {
    return {
      kind: isSet(object.kind) ? pathEntryKindFromJSON(object.kind) : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      parent_folder: isSet(object.parentFolder)
        ? globalThis.String(object.parentFolder)
        : isSet(object.parent_folder)
        ? globalThis.String(object.parent_folder)
        : "",
      file_id: isSet(object.fileId)
        ? globalThis.String(object.fileId)
        : isSet(object.file_id)
        ? globalThis.String(object.file_id)
        : "",
      content_type: isSet(object.contentType)
        ? globalThis.String(object.contentType)
        : isSet(object.content_type)
        ? globalThis.String(object.content_type)
        : "",
      size_bytes: isSet(object.sizeBytes)
        ? globalThis.Number(object.sizeBytes)
        : isSet(object.size_bytes)
        ? globalThis.Number(object.size_bytes)
        : 0,
      checksum: isSet(object.checksum) ? globalThis.String(object.checksum) : "",
      created_at: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
      updated_at: isSet(object.updatedAt)
        ? fromJsonTimestamp(object.updatedAt)
        : isSet(object.updated_at)
        ? fromJsonTimestamp(object.updated_at)
        : undefined,
      has_children: isSet(object.hasChildren)
        ? globalThis.Boolean(object.hasChildren)
        : isSet(object.has_children)
        ? globalThis.Boolean(object.has_children)
        : false,
    };
  },

  toJSON(message: PathEntry): unknown {
    const obj: any = {};
    if (message.kind !== 0) {
      obj.kind = pathEntryKindToJSON(message.kind);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.parent_folder !== "") {
      obj.parentFolder = message.parent_folder;
    }
    if (message.file_id !== "") {
      obj.fileId = message.file_id;
    }
    if (message.content_type !== "") {
      obj.contentType = message.content_type;
    }
    if (message.size_bytes !== 0) {
      obj.sizeBytes = Math.round(message.size_bytes);
    }
    if (message.checksum !== "") {
      obj.checksum = message.checksum;
    }
    if (message.created_at !== undefined) {
      obj.createdAt = message.created_at.toISOString();
    }
    if (message.updated_at !== undefined) {
      obj.updatedAt = message.updated_at.toISOString();
    }
    if (message.has_children !== false) {
      obj.hasChildren = message.has_children;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PathEntry>, I>>(base?: I): PathEntry {
    return PathEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PathEntry>, I>>(object: I): PathEntry {
    const message = createBasePathEntry();
    message.kind = object.kind ?? 0;
    message.name = object.name ?? "";
    message.path = object.path ?? "";
    message.parent_folder = object.parent_folder ?? "";
    message.file_id = object.file_id ?? "";
    message.content_type = object.content_type ?? "";
    message.size_bytes = object.size_bytes ?? 0;
    message.checksum = object.checksum ?? "";
    message.created_at = object.created_at ?? undefined;
    message.updated_at = object.updated_at ?? undefined;
    message.has_children = object.has_children ?? false;
    return message;
  },
};

function createBaseListFolderRequest(): ListFolderRequest {
  return { folder: "", page_size: 0, page_token: "", include_files: false, include_directories: false };
}

export const ListFolderRequest: MessageFns<ListFolderRequest> = {
  encode(message: ListFolderRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.folder !== "") {
      writer.uint32(10).string(message.folder);
    }
    if (message.page_size !== 0) {
      writer.uint32(16).int32(message.page_size);
    }
    if (message.page_token !== "") {
      writer.uint32(26).string(message.page_token);
    }
    if (message.include_files !== false) {
      writer.uint32(32).bool(message.include_files);
    }
    if (message.include_directories !== false) {
      writer.uint32(40).bool(message.include_directories);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListFolderRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListFolderRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.folder = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.page_size = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.page_token = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.include_files = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.include_directories = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListFolderRequest {
    return {
      folder: isSet(object.folder) ? globalThis.String(object.folder) : "",
      page_size: isSet(object.pageSize)
        ? globalThis.Number(object.pageSize)
        : isSet(object.page_size)
        ? globalThis.Number(object.page_size)
        : 0,
      page_token: isSet(object.pageToken)
        ? globalThis.String(object.pageToken)
        : isSet(object.page_token)
        ? globalThis.String(object.page_token)
        : "",
      include_files: isSet(object.includeFiles)
        ? globalThis.Boolean(object.includeFiles)
        : isSet(object.include_files)
        ? globalThis.Boolean(object.include_files)
        : false,
      include_directories: isSet(object.includeDirectories)
        ? globalThis.Boolean(object.includeDirectories)
        : isSet(object.include_directories)
        ? globalThis.Boolean(object.include_directories)
        : false,
    };
  },

  toJSON(message: ListFolderRequest): unknown {
    const obj: any = {};
    if (message.folder !== "") {
      obj.folder = message.folder;
    }
    if (message.page_size !== 0) {
      obj.pageSize = Math.round(message.page_size);
    }
    if (message.page_token !== "") {
      obj.pageToken = message.page_token;
    }
    if (message.include_files !== false) {
      obj.includeFiles = message.include_files;
    }
    if (message.include_directories !== false) {
      obj.includeDirectories = message.include_directories;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListFolderRequest>, I>>(base?: I): ListFolderRequest {
    return ListFolderRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListFolderRequest>, I>>(object: I): ListFolderRequest {
    const message = createBaseListFolderRequest();
    message.folder = object.folder ?? "";
    message.page_size = object.page_size ?? 0;
    message.page_token = object.page_token ?? "";
    message.include_files = object.include_files ?? false;
    message.include_directories = object.include_directories ?? false;
    return message;
  },
};

function createBaseListFolderResponse(): ListFolderResponse {
  return { entries: [], next_page_token: "", total_count: 0 };
}

export const ListFolderResponse: MessageFns<ListFolderResponse> = {
  encode(message: ListFolderResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.entries) {
      PathEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.next_page_token !== "") {
      writer.uint32(18).string(message.next_page_token);
    }
    if (message.total_count !== 0) {
      writer.uint32(24).int32(message.total_count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListFolderResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListFolderResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entries.push(PathEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.next_page_token = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.total_count = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListFolderResponse {
    return {
      entries: globalThis.Array.isArray(object?.entries) ? object.entries.map((e: any) => PathEntry.fromJSON(e)) : [],
      next_page_token: isSet(object.nextPageToken)
        ? globalThis.String(object.nextPageToken)
        : isSet(object.next_page_token)
        ? globalThis.String(object.next_page_token)
        : "",
      total_count: isSet(object.totalCount)
        ? globalThis.Number(object.totalCount)
        : isSet(object.total_count)
        ? globalThis.Number(object.total_count)
        : 0,
    };
  },

  toJSON(message: ListFolderResponse): unknown {
    const obj: any = {};
    if (message.entries?.length) {
      obj.entries = message.entries.map((e) => PathEntry.toJSON(e));
    }
    if (message.next_page_token !== "") {
      obj.nextPageToken = message.next_page_token;
    }
    if (message.total_count !== 0) {
      obj.totalCount = Math.round(message.total_count);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListFolderResponse>, I>>(base?: I): ListFolderResponse {
    return ListFolderResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListFolderResponse>, I>>(object: I): ListFolderResponse {
    const message = createBaseListFolderResponse();
    message.entries = object.entries?.map((e) => PathEntry.fromPartial(e)) || [];
    message.next_page_token = object.next_page_token ?? "";
    message.total_count = object.total_count ?? 0;
    return message;
  },
};

function createBaseGetPathInfoRequest(): GetPathInfoRequest {
  return { selector: undefined };
}

export const GetPathInfoRequest: MessageFns<GetPathInfoRequest> = {
  encode(message: GetPathInfoRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.selector !== undefined) {
      PathSelector.encode(message.selector, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPathInfoRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPathInfoRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.selector = PathSelector.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPathInfoRequest {
    return { selector: isSet(object.selector) ? PathSelector.fromJSON(object.selector) : undefined };
  },

  toJSON(message: GetPathInfoRequest): unknown {
    const obj: any = {};
    if (message.selector !== undefined) {
      obj.selector = PathSelector.toJSON(message.selector);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPathInfoRequest>, I>>(base?: I): GetPathInfoRequest {
    return GetPathInfoRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPathInfoRequest>, I>>(object: I): GetPathInfoRequest {
    const message = createBaseGetPathInfoRequest();
    message.selector = (object.selector !== undefined && object.selector !== null)
      ? PathSelector.fromPartial(object.selector)
      : undefined;
    return message;
  },
};

function createBaseGetPathInfoResponse(): GetPathInfoResponse {
  return { entry: undefined };
}

export const GetPathInfoResponse: MessageFns<GetPathInfoResponse> = {
  encode(message: GetPathInfoResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.entry !== undefined) {
      PathEntry.encode(message.entry, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPathInfoResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPathInfoResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entry = PathEntry.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPathInfoResponse {
    return { entry: isSet(object.entry) ? PathEntry.fromJSON(object.entry) : undefined };
  },

  toJSON(message: GetPathInfoResponse): unknown {
    const obj: any = {};
    if (message.entry !== undefined) {
      obj.entry = PathEntry.toJSON(message.entry);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPathInfoResponse>, I>>(base?: I): GetPathInfoResponse {
    return GetPathInfoResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPathInfoResponse>, I>>(object: I): GetPathInfoResponse {
    const message = createBaseGetPathInfoResponse();
    message.entry = (object.entry !== undefined && object.entry !== null)
      ? PathEntry.fromPartial(object.entry)
      : undefined;
    return message;
  },
};

function createBaseReadTextFileRequest(): ReadTextFileRequest {
  return { selector: undefined, max_bytes: 0, fail_if_binary: false };
}

export const ReadTextFileRequest: MessageFns<ReadTextFileRequest> = {
  encode(message: ReadTextFileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.selector !== undefined) {
      PathSelector.encode(message.selector, writer.uint32(10).fork()).join();
    }
    if (message.max_bytes !== 0) {
      writer.uint32(16).int32(message.max_bytes);
    }
    if (message.fail_if_binary !== false) {
      writer.uint32(24).bool(message.fail_if_binary);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReadTextFileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReadTextFileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.selector = PathSelector.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.max_bytes = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.fail_if_binary = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReadTextFileRequest {
    return {
      selector: isSet(object.selector) ? PathSelector.fromJSON(object.selector) : undefined,
      max_bytes: isSet(object.maxBytes)
        ? globalThis.Number(object.maxBytes)
        : isSet(object.max_bytes)
        ? globalThis.Number(object.max_bytes)
        : 0,
      fail_if_binary: isSet(object.failIfBinary)
        ? globalThis.Boolean(object.failIfBinary)
        : isSet(object.fail_if_binary)
        ? globalThis.Boolean(object.fail_if_binary)
        : false,
    };
  },

  toJSON(message: ReadTextFileRequest): unknown {
    const obj: any = {};
    if (message.selector !== undefined) {
      obj.selector = PathSelector.toJSON(message.selector);
    }
    if (message.max_bytes !== 0) {
      obj.maxBytes = Math.round(message.max_bytes);
    }
    if (message.fail_if_binary !== false) {
      obj.failIfBinary = message.fail_if_binary;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReadTextFileRequest>, I>>(base?: I): ReadTextFileRequest {
    return ReadTextFileRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReadTextFileRequest>, I>>(object: I): ReadTextFileRequest {
    const message = createBaseReadTextFileRequest();
    message.selector = (object.selector !== undefined && object.selector !== null)
      ? PathSelector.fromPartial(object.selector)
      : undefined;
    message.max_bytes = object.max_bytes ?? 0;
    message.fail_if_binary = object.fail_if_binary ?? false;
    return message;
  },
};

function createBaseReadTextFileResponse(): ReadTextFileResponse {
  return {
    entry: undefined,
    text: "",
    content_type: "",
    size_bytes: 0,
    checksum: "",
    updated_at: undefined,
    truncated: false,
    lossy: false,
  };
}

export const ReadTextFileResponse: MessageFns<ReadTextFileResponse> = {
  encode(message: ReadTextFileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.entry !== undefined) {
      PathEntry.encode(message.entry, writer.uint32(10).fork()).join();
    }
    if (message.text !== "") {
      writer.uint32(18).string(message.text);
    }
    if (message.content_type !== "") {
      writer.uint32(26).string(message.content_type);
    }
    if (message.size_bytes !== 0) {
      writer.uint32(32).int64(message.size_bytes);
    }
    if (message.checksum !== "") {
      writer.uint32(42).string(message.checksum);
    }
    if (message.updated_at !== undefined) {
      Timestamp.encode(toTimestamp(message.updated_at), writer.uint32(50).fork()).join();
    }
    if (message.truncated !== false) {
      writer.uint32(56).bool(message.truncated);
    }
    if (message.lossy !== false) {
      writer.uint32(64).bool(message.lossy);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReadTextFileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReadTextFileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entry = PathEntry.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.content_type = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.size_bytes = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.checksum = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.updated_at = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.truncated = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.lossy = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReadTextFileResponse {
    return {
      entry: isSet(object.entry) ? PathEntry.fromJSON(object.entry) : undefined,
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      content_type: isSet(object.contentType)
        ? globalThis.String(object.contentType)
        : isSet(object.content_type)
        ? globalThis.String(object.content_type)
        : "",
      size_bytes: isSet(object.sizeBytes)
        ? globalThis.Number(object.sizeBytes)
        : isSet(object.size_bytes)
        ? globalThis.Number(object.size_bytes)
        : 0,
      checksum: isSet(object.checksum) ? globalThis.String(object.checksum) : "",
      updated_at: isSet(object.updatedAt)
        ? fromJsonTimestamp(object.updatedAt)
        : isSet(object.updated_at)
        ? fromJsonTimestamp(object.updated_at)
        : undefined,
      truncated: isSet(object.truncated) ? globalThis.Boolean(object.truncated) : false,
      lossy: isSet(object.lossy) ? globalThis.Boolean(object.lossy) : false,
    };
  },

  toJSON(message: ReadTextFileResponse): unknown {
    const obj: any = {};
    if (message.entry !== undefined) {
      obj.entry = PathEntry.toJSON(message.entry);
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.content_type !== "") {
      obj.contentType = message.content_type;
    }
    if (message.size_bytes !== 0) {
      obj.sizeBytes = Math.round(message.size_bytes);
    }
    if (message.checksum !== "") {
      obj.checksum = message.checksum;
    }
    if (message.updated_at !== undefined) {
      obj.updatedAt = message.updated_at.toISOString();
    }
    if (message.truncated !== false) {
      obj.truncated = message.truncated;
    }
    if (message.lossy !== false) {
      obj.lossy = message.lossy;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReadTextFileResponse>, I>>(base?: I): ReadTextFileResponse {
    return ReadTextFileResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReadTextFileResponse>, I>>(object: I): ReadTextFileResponse {
    const message = createBaseReadTextFileResponse();
    message.entry = (object.entry !== undefined && object.entry !== null)
      ? PathEntry.fromPartial(object.entry)
      : undefined;
    message.text = object.text ?? "";
    message.content_type = object.content_type ?? "";
    message.size_bytes = object.size_bytes ?? 0;
    message.checksum = object.checksum ?? "";
    message.updated_at = object.updated_at ?? undefined;
    message.truncated = object.truncated ?? false;
    message.lossy = object.lossy ?? false;
    return message;
  },
};

/**
 * File storage service with per-user isolation and resumable upload support.
 * All methods require authentication; user identity is extracted from auth metadata.
 */
export interface FileStorageService {
  /**
   * Simple file upload via client streaming.
   * First message must contain metadata (filename, content_type, folder).
   * Subsequent messages carry raw file data chunks.
   */
  UploadFile(request: Observable<UploadFileRequest>): Promise<UploadFileResponse>;
  /**
   * Initialize a resumable (multipart) upload session.
   * Returns an upload_id and part layout for the client to upload parts independently.
   */
  InitResumableUpload(request: InitResumableUploadRequest): Promise<InitResumableUploadResponse>;
  /**
   * Upload a single part of a resumable upload via client streaming.
   * First message must contain the part header (upload_id, part_number).
   * Subsequent messages carry raw part data chunks.
   */
  UploadPart(request: Observable<UploadPartRequest>): Promise<UploadPartResponse>;
  /** Complete a resumable upload by assembling all uploaded parts. */
  CompleteResumableUpload(request: CompleteResumableUploadRequest): Promise<UploadFileResponse>;
  /** Abort a resumable upload session and clean up all uploaded parts. */
  AbortResumableUpload(request: AbortResumableUploadRequest): Promise<Empty>;
  /**
   * Query the status of a resumable upload session, including completed parts.
   * Clients use this to determine which parts still need to be uploaded (resume).
   */
  GetUploadStatus(request: GetUploadStatusRequest): Promise<GetUploadStatusResponse>;
  /** Download a file via server streaming. */
  DownloadFile(request: DownloadFileRequest): Promise<HttpBody>;
  /** Delete a file owned by the current user. */
  DeleteFile(request: DeleteFileRequest): Promise<Empty>;
  /** List files owned by the current user, with optional folder filter. */
  ListFiles(request: ListFilesRequest): Promise<ListFilesResponse>;
  /** Get metadata for a single file owned by the current user. */
  GetFileInfo(request: GetFileInfoRequest): Promise<FileInfo>;
  /** Browse direct child entries of a virtual folder. */
  ListFolder(request: ListFolderRequest): Promise<ListFolderResponse>;
  /** Resolve a file or directory by file_id or virtual path. */
  GetPathInfo(request: GetPathInfoRequest): Promise<GetPathInfoResponse>;
  /** Preview a small UTF-8 text file safely. */
  ReadTextFile(request: ReadTextFileRequest): Promise<ReadTextFileResponse>;
}

export const FileStorageServiceServiceName = "stew.api.v1.FileStorageService";
export class FileStorageServiceClientImpl implements FileStorageService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || FileStorageServiceServiceName;
    this.rpc = rpc;
    this.UploadFile = this.UploadFile.bind(this);
    this.InitResumableUpload = this.InitResumableUpload.bind(this);
    this.UploadPart = this.UploadPart.bind(this);
    this.CompleteResumableUpload = this.CompleteResumableUpload.bind(this);
    this.AbortResumableUpload = this.AbortResumableUpload.bind(this);
    this.GetUploadStatus = this.GetUploadStatus.bind(this);
    this.DownloadFile = this.DownloadFile.bind(this);
    this.DeleteFile = this.DeleteFile.bind(this);
    this.ListFiles = this.ListFiles.bind(this);
    this.GetFileInfo = this.GetFileInfo.bind(this);
    this.ListFolder = this.ListFolder.bind(this);
    this.GetPathInfo = this.GetPathInfo.bind(this);
    this.ReadTextFile = this.ReadTextFile.bind(this);
  }
  UploadFile(request: Observable<UploadFileRequest>): Promise<UploadFileResponse> {
    const data = request.pipe(map((request) => UploadFileRequest.encode(request).finish()));
    const promise = this.rpc.clientStreamingRequest(this.service, "UploadFile", data);
    return promise.then((data) => UploadFileResponse.decode(new BinaryReader(data)));
  }

  InitResumableUpload(request: InitResumableUploadRequest): Promise<InitResumableUploadResponse> {
    const data = InitResumableUploadRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "InitResumableUpload", data);
    return promise.then((data) => InitResumableUploadResponse.decode(new BinaryReader(data)));
  }

  UploadPart(request: Observable<UploadPartRequest>): Promise<UploadPartResponse> {
    const data = request.pipe(map((request) => UploadPartRequest.encode(request).finish()));
    const promise = this.rpc.clientStreamingRequest(this.service, "UploadPart", data);
    return promise.then((data) => UploadPartResponse.decode(new BinaryReader(data)));
  }

  CompleteResumableUpload(request: CompleteResumableUploadRequest): Promise<UploadFileResponse> {
    const data = CompleteResumableUploadRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "CompleteResumableUpload", data);
    return promise.then((data) => UploadFileResponse.decode(new BinaryReader(data)));
  }

  AbortResumableUpload(request: AbortResumableUploadRequest): Promise<Empty> {
    const data = AbortResumableUploadRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "AbortResumableUpload", data);
    return promise.then((data) => Empty.decode(new BinaryReader(data)));
  }

  GetUploadStatus(request: GetUploadStatusRequest): Promise<GetUploadStatusResponse> {
    const data = GetUploadStatusRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetUploadStatus", data);
    return promise.then((data) => GetUploadStatusResponse.decode(new BinaryReader(data)));
  }

  DownloadFile(request: DownloadFileRequest): Promise<HttpBody> {
    const data = DownloadFileRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "DownloadFile", data);
    return promise.then((data) => HttpBody.decode(new BinaryReader(data)));
  }

  DeleteFile(request: DeleteFileRequest): Promise<Empty> {
    const data = DeleteFileRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "DeleteFile", data);
    return promise.then((data) => Empty.decode(new BinaryReader(data)));
  }

  ListFiles(request: ListFilesRequest): Promise<ListFilesResponse> {
    const data = ListFilesRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ListFiles", data);
    return promise.then((data) => ListFilesResponse.decode(new BinaryReader(data)));
  }

  GetFileInfo(request: GetFileInfoRequest): Promise<FileInfo> {
    const data = GetFileInfoRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetFileInfo", data);
    return promise.then((data) => FileInfo.decode(new BinaryReader(data)));
  }

  ListFolder(request: ListFolderRequest): Promise<ListFolderResponse> {
    const data = ListFolderRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ListFolder", data);
    return promise.then((data) => ListFolderResponse.decode(new BinaryReader(data)));
  }

  GetPathInfo(request: GetPathInfoRequest): Promise<GetPathInfoResponse> {
    const data = GetPathInfoRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetPathInfo", data);
    return promise.then((data) => GetPathInfoResponse.decode(new BinaryReader(data)));
  }

  ReadTextFile(request: ReadTextFileRequest): Promise<ReadTextFileResponse> {
    const data = ReadTextFileRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ReadTextFile", data);
    return promise.then((data) => ReadTextFileResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
  clientStreamingRequest(service: string, method: string, data: Observable<Uint8Array>): Promise<Uint8Array>;
  serverStreamingRequest(service: string, method: string, data: Uint8Array): Observable<Uint8Array>;
  bidirectionalStreamingRequest(service: string, method: string, data: Observable<Uint8Array>): Observable<Uint8Array>;
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
