// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v3.21.12
// source: authorization.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Struct } from "./google/protobuf/struct";
import { Timestamp } from "./google/protobuf/timestamp";
import { User } from "./user";

export const protobufPackage = "stew.api.v1";

/** OIDC 令牌请求 */
export interface TokenRequest {
  code: string;
  redirect_uri: string;
  state: string;
}

/** JWT 令牌响应 */
export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  id_token: string;
  user_info: User | undefined;
}

/** 令牌验证请求 */
export interface ValidateTokenRequest {
  token: string;
}

/** 令牌验证响应 */
export interface ValidateTokenResponse {
  valid: boolean;
  user: User | undefined;
  claims: { [key: string]: any } | undefined;
  expires_at: number;
}

/** 授权检查请求 */
export interface AuthorizationRequest {
  /** 主体信息（用户或 API Key） */
  subject: string;
  /** 操作（动作） */
  action: string;
  /** 资源（对象） */
  resource: string;
  /** 上下文信息（用于复杂决策） */
  context:
    | { [key: string]: any }
    | undefined;
  /** 域/租户信息（多租户场景） */
  domain: string;
  /** 是否强制使用 OPA（跳过 Casbin 缓存） */
  force_opa: boolean;
  /** 主体类型（user, apikey, service） */
  subject_type: string;
}

/** 授权检查响应 */
export interface AuthorizationResponse {
  /** 是否允许 */
  allowed: boolean;
  /** 决策原因 */
  reason: string;
  /** 决策来源（casbin 或 opa） */
  decision_source: string;
  /** 决策时间（毫秒） */
  decision_time_ms: number;
  /** 建议（可选的补充信息） */
  recommendations: string[];
  /** 审计追踪 ID */
  trace_id: string;
}

/** 批量授权检查请求 */
export interface BatchAuthorizationRequest {
  requests: AuthorizationRequest[];
}

/** 批量授权检查响应 */
export interface BatchAuthorizationResponse {
  responses: AuthorizationResponse[];
}

/** 策略规则（Casbin 格式） */
export interface PolicyRule {
  ptype: string;
  rule: string[];
}

/** 策略同步请求 */
export interface SyncPolicyRequest {
  domain: string;
  policies: PolicyRule[];
  version: number;
}

/** 策略同步响应 */
export interface SyncPolicyResponse {
  success: boolean;
  message: string;
  synced_count: number;
  synced_at: Date | undefined;
}

/** 获取策略请求 */
export interface GetPolicyRequest {
  domain: string;
  subject: string;
}

/** 获取策略响应 */
export interface GetPolicyResponse {
  policies: PolicyRule[];
  version: number;
  updated_at: Date | undefined;
}

/** 添加策略请求 */
export interface AddPolicyRequest {
  domain: string;
  policy: PolicyRule | undefined;
}

/** 删除策略请求 */
export interface RemovePolicyRequest {
  domain: string;
  policy: PolicyRule | undefined;
}

/** 策略操作响应 */
export interface PolicyOperationResponse {
  success: boolean;
  message: string;
}

/** 审计日志条目 */
export interface AuditLog {
  trace_id: string;
  timestamp: Date | undefined;
  subject: string;
  action: string;
  resource: string;
  allowed: boolean;
  decision_source: string;
  reason: string;
  context: { [key: string]: any } | undefined;
  ip_address: string;
  user_agent: string;
}

/** 查询审计日志请求 */
export interface QueryAuditLogsRequest {
  subject: string;
  resource: string;
  start_time: Date | undefined;
  end_time: Date | undefined;
  page_size: number;
  page_token: string;
}

/** 查询审计日志响应 */
export interface QueryAuditLogsResponse {
  logs: AuditLog[];
  next_page_token: string;
  total_count: number;
}

/** 健康检查请求 */
export interface HealthCheckRequest {
  service: string;
}

/** 健康检查响应 */
export interface HealthCheckResponse {
  status: HealthCheckResponse_ServingStatus;
  components: { [key: string]: any } | undefined;
  version: string;
}

export enum HealthCheckResponse_ServingStatus {
  UNKNOWN = 0,
  SERVING = 1,
  NOT_SERVING = 2,
  SERVICE_UNKNOWN = 3,
  UNRECOGNIZED = -1,
}

export function healthCheckResponse_ServingStatusFromJSON(object: any): HealthCheckResponse_ServingStatus {
  switch (object) {
    case 0:
    case "UNKNOWN":
      return HealthCheckResponse_ServingStatus.UNKNOWN;
    case 1:
    case "SERVING":
      return HealthCheckResponse_ServingStatus.SERVING;
    case 2:
    case "NOT_SERVING":
      return HealthCheckResponse_ServingStatus.NOT_SERVING;
    case 3:
    case "SERVICE_UNKNOWN":
      return HealthCheckResponse_ServingStatus.SERVICE_UNKNOWN;
    case -1:
    case "UNRECOGNIZED":
    default:
      return HealthCheckResponse_ServingStatus.UNRECOGNIZED;
  }
}

export function healthCheckResponse_ServingStatusToJSON(object: HealthCheckResponse_ServingStatus): string {
  switch (object) {
    case HealthCheckResponse_ServingStatus.UNKNOWN:
      return "UNKNOWN";
    case HealthCheckResponse_ServingStatus.SERVING:
      return "SERVING";
    case HealthCheckResponse_ServingStatus.NOT_SERVING:
      return "NOT_SERVING";
    case HealthCheckResponse_ServingStatus.SERVICE_UNKNOWN:
      return "SERVICE_UNKNOWN";
    case HealthCheckResponse_ServingStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

function createBaseTokenRequest(): TokenRequest {
  return { code: "", redirect_uri: "", state: "" };
}

export const TokenRequest: MessageFns<TokenRequest> = {
  encode(message: TokenRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.redirect_uri !== "") {
      writer.uint32(18).string(message.redirect_uri);
    }
    if (message.state !== "") {
      writer.uint32(26).string(message.state);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TokenRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTokenRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.redirect_uri = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.state = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TokenRequest {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      redirect_uri: isSet(object.redirect_uri) ? globalThis.String(object.redirect_uri) : "",
      state: isSet(object.state) ? globalThis.String(object.state) : "",
    };
  },

  toJSON(message: TokenRequest): unknown {
    const obj: any = {};
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.redirect_uri !== "") {
      obj.redirect_uri = message.redirect_uri;
    }
    if (message.state !== "") {
      obj.state = message.state;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TokenRequest>, I>>(base?: I): TokenRequest {
    return TokenRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TokenRequest>, I>>(object: I): TokenRequest {
    const message = createBaseTokenRequest();
    message.code = object.code ?? "";
    message.redirect_uri = object.redirect_uri ?? "";
    message.state = object.state ?? "";
    return message;
  },
};

function createBaseTokenResponse(): TokenResponse {
  return { access_token: "", token_type: "", expires_in: 0, refresh_token: "", id_token: "", user_info: undefined };
}

export const TokenResponse: MessageFns<TokenResponse> = {
  encode(message: TokenResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.access_token !== "") {
      writer.uint32(10).string(message.access_token);
    }
    if (message.token_type !== "") {
      writer.uint32(18).string(message.token_type);
    }
    if (message.expires_in !== 0) {
      writer.uint32(24).int64(message.expires_in);
    }
    if (message.refresh_token !== "") {
      writer.uint32(34).string(message.refresh_token);
    }
    if (message.id_token !== "") {
      writer.uint32(42).string(message.id_token);
    }
    if (message.user_info !== undefined) {
      User.encode(message.user_info, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TokenResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTokenResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.access_token = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.token_type = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.expires_in = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.refresh_token = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.id_token = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.user_info = User.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TokenResponse {
    return {
      access_token: isSet(object.access_token) ? globalThis.String(object.access_token) : "",
      token_type: isSet(object.token_type) ? globalThis.String(object.token_type) : "",
      expires_in: isSet(object.expires_in) ? globalThis.Number(object.expires_in) : 0,
      refresh_token: isSet(object.refresh_token) ? globalThis.String(object.refresh_token) : "",
      id_token: isSet(object.id_token) ? globalThis.String(object.id_token) : "",
      user_info: isSet(object.user_info) ? User.fromJSON(object.user_info) : undefined,
    };
  },

  toJSON(message: TokenResponse): unknown {
    const obj: any = {};
    if (message.access_token !== "") {
      obj.access_token = message.access_token;
    }
    if (message.token_type !== "") {
      obj.token_type = message.token_type;
    }
    if (message.expires_in !== 0) {
      obj.expires_in = Math.round(message.expires_in);
    }
    if (message.refresh_token !== "") {
      obj.refresh_token = message.refresh_token;
    }
    if (message.id_token !== "") {
      obj.id_token = message.id_token;
    }
    if (message.user_info !== undefined) {
      obj.user_info = User.toJSON(message.user_info);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TokenResponse>, I>>(base?: I): TokenResponse {
    return TokenResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TokenResponse>, I>>(object: I): TokenResponse {
    const message = createBaseTokenResponse();
    message.access_token = object.access_token ?? "";
    message.token_type = object.token_type ?? "";
    message.expires_in = object.expires_in ?? 0;
    message.refresh_token = object.refresh_token ?? "";
    message.id_token = object.id_token ?? "";
    message.user_info = (object.user_info !== undefined && object.user_info !== null)
      ? User.fromPartial(object.user_info)
      : undefined;
    return message;
  },
};

function createBaseValidateTokenRequest(): ValidateTokenRequest {
  return { token: "" };
}

export const ValidateTokenRequest: MessageFns<ValidateTokenRequest> = {
  encode(message: ValidateTokenRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== "") {
      writer.uint32(10).string(message.token);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ValidateTokenRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseValidateTokenRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ValidateTokenRequest {
    return { token: isSet(object.token) ? globalThis.String(object.token) : "" };
  },

  toJSON(message: ValidateTokenRequest): unknown {
    const obj: any = {};
    if (message.token !== "") {
      obj.token = message.token;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ValidateTokenRequest>, I>>(base?: I): ValidateTokenRequest {
    return ValidateTokenRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ValidateTokenRequest>, I>>(object: I): ValidateTokenRequest {
    const message = createBaseValidateTokenRequest();
    message.token = object.token ?? "";
    return message;
  },
};

function createBaseValidateTokenResponse(): ValidateTokenResponse {
  return { valid: false, user: undefined, claims: undefined, expires_at: 0 };
}

export const ValidateTokenResponse: MessageFns<ValidateTokenResponse> = {
  encode(message: ValidateTokenResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.valid !== false) {
      writer.uint32(8).bool(message.valid);
    }
    if (message.user !== undefined) {
      User.encode(message.user, writer.uint32(18).fork()).join();
    }
    if (message.claims !== undefined) {
      Struct.encode(Struct.wrap(message.claims), writer.uint32(26).fork()).join();
    }
    if (message.expires_at !== 0) {
      writer.uint32(32).int64(message.expires_at);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ValidateTokenResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseValidateTokenResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.valid = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.user = User.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.claims = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.expires_at = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ValidateTokenResponse {
    return {
      valid: isSet(object.valid) ? globalThis.Boolean(object.valid) : false,
      user: isSet(object.user) ? User.fromJSON(object.user) : undefined,
      claims: isObject(object.claims) ? object.claims : undefined,
      expires_at: isSet(object.expires_at) ? globalThis.Number(object.expires_at) : 0,
    };
  },

  toJSON(message: ValidateTokenResponse): unknown {
    const obj: any = {};
    if (message.valid !== false) {
      obj.valid = message.valid;
    }
    if (message.user !== undefined) {
      obj.user = User.toJSON(message.user);
    }
    if (message.claims !== undefined) {
      obj.claims = message.claims;
    }
    if (message.expires_at !== 0) {
      obj.expires_at = Math.round(message.expires_at);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ValidateTokenResponse>, I>>(base?: I): ValidateTokenResponse {
    return ValidateTokenResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ValidateTokenResponse>, I>>(object: I): ValidateTokenResponse {
    const message = createBaseValidateTokenResponse();
    message.valid = object.valid ?? false;
    message.user = (object.user !== undefined && object.user !== null) ? User.fromPartial(object.user) : undefined;
    message.claims = object.claims ?? undefined;
    message.expires_at = object.expires_at ?? 0;
    return message;
  },
};

function createBaseAuthorizationRequest(): AuthorizationRequest {
  return { subject: "", action: "", resource: "", context: undefined, domain: "", force_opa: false, subject_type: "" };
}

export const AuthorizationRequest: MessageFns<AuthorizationRequest> = {
  encode(message: AuthorizationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.subject !== "") {
      writer.uint32(10).string(message.subject);
    }
    if (message.action !== "") {
      writer.uint32(18).string(message.action);
    }
    if (message.resource !== "") {
      writer.uint32(26).string(message.resource);
    }
    if (message.context !== undefined) {
      Struct.encode(Struct.wrap(message.context), writer.uint32(34).fork()).join();
    }
    if (message.domain !== "") {
      writer.uint32(42).string(message.domain);
    }
    if (message.force_opa !== false) {
      writer.uint32(48).bool(message.force_opa);
    }
    if (message.subject_type !== "") {
      writer.uint32(58).string(message.subject_type);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthorizationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthorizationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.subject = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.action = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.resource = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.context = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.force_opa = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.subject_type = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuthorizationRequest {
    return {
      subject: isSet(object.subject) ? globalThis.String(object.subject) : "",
      action: isSet(object.action) ? globalThis.String(object.action) : "",
      resource: isSet(object.resource) ? globalThis.String(object.resource) : "",
      context: isObject(object.context) ? object.context : undefined,
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      force_opa: isSet(object.force_opa) ? globalThis.Boolean(object.force_opa) : false,
      subject_type: isSet(object.subject_type) ? globalThis.String(object.subject_type) : "",
    };
  },

  toJSON(message: AuthorizationRequest): unknown {
    const obj: any = {};
    if (message.subject !== "") {
      obj.subject = message.subject;
    }
    if (message.action !== "") {
      obj.action = message.action;
    }
    if (message.resource !== "") {
      obj.resource = message.resource;
    }
    if (message.context !== undefined) {
      obj.context = message.context;
    }
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.force_opa !== false) {
      obj.force_opa = message.force_opa;
    }
    if (message.subject_type !== "") {
      obj.subject_type = message.subject_type;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AuthorizationRequest>, I>>(base?: I): AuthorizationRequest {
    return AuthorizationRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AuthorizationRequest>, I>>(object: I): AuthorizationRequest {
    const message = createBaseAuthorizationRequest();
    message.subject = object.subject ?? "";
    message.action = object.action ?? "";
    message.resource = object.resource ?? "";
    message.context = object.context ?? undefined;
    message.domain = object.domain ?? "";
    message.force_opa = object.force_opa ?? false;
    message.subject_type = object.subject_type ?? "";
    return message;
  },
};

function createBaseAuthorizationResponse(): AuthorizationResponse {
  return { allowed: false, reason: "", decision_source: "", decision_time_ms: 0, recommendations: [], trace_id: "" };
}

export const AuthorizationResponse: MessageFns<AuthorizationResponse> = {
  encode(message: AuthorizationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.allowed !== false) {
      writer.uint32(8).bool(message.allowed);
    }
    if (message.reason !== "") {
      writer.uint32(18).string(message.reason);
    }
    if (message.decision_source !== "") {
      writer.uint32(26).string(message.decision_source);
    }
    if (message.decision_time_ms !== 0) {
      writer.uint32(32).int64(message.decision_time_ms);
    }
    for (const v of message.recommendations) {
      writer.uint32(42).string(v!);
    }
    if (message.trace_id !== "") {
      writer.uint32(50).string(message.trace_id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthorizationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthorizationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.allowed = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.decision_source = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.decision_time_ms = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.recommendations.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.trace_id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuthorizationResponse {
    return {
      allowed: isSet(object.allowed) ? globalThis.Boolean(object.allowed) : false,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      decision_source: isSet(object.decision_source) ? globalThis.String(object.decision_source) : "",
      decision_time_ms: isSet(object.decision_time_ms) ? globalThis.Number(object.decision_time_ms) : 0,
      recommendations: globalThis.Array.isArray(object?.recommendations)
        ? object.recommendations.map((e: any) => globalThis.String(e))
        : [],
      trace_id: isSet(object.trace_id) ? globalThis.String(object.trace_id) : "",
    };
  },

  toJSON(message: AuthorizationResponse): unknown {
    const obj: any = {};
    if (message.allowed !== false) {
      obj.allowed = message.allowed;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.decision_source !== "") {
      obj.decision_source = message.decision_source;
    }
    if (message.decision_time_ms !== 0) {
      obj.decision_time_ms = Math.round(message.decision_time_ms);
    }
    if (message.recommendations?.length) {
      obj.recommendations = message.recommendations;
    }
    if (message.trace_id !== "") {
      obj.trace_id = message.trace_id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AuthorizationResponse>, I>>(base?: I): AuthorizationResponse {
    return AuthorizationResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AuthorizationResponse>, I>>(object: I): AuthorizationResponse {
    const message = createBaseAuthorizationResponse();
    message.allowed = object.allowed ?? false;
    message.reason = object.reason ?? "";
    message.decision_source = object.decision_source ?? "";
    message.decision_time_ms = object.decision_time_ms ?? 0;
    message.recommendations = object.recommendations?.map((e) => e) || [];
    message.trace_id = object.trace_id ?? "";
    return message;
  },
};

function createBaseBatchAuthorizationRequest(): BatchAuthorizationRequest {
  return { requests: [] };
}

export const BatchAuthorizationRequest: MessageFns<BatchAuthorizationRequest> = {
  encode(message: BatchAuthorizationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.requests) {
      AuthorizationRequest.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BatchAuthorizationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchAuthorizationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requests.push(AuthorizationRequest.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BatchAuthorizationRequest {
    return {
      requests: globalThis.Array.isArray(object?.requests)
        ? object.requests.map((e: any) => AuthorizationRequest.fromJSON(e))
        : [],
    };
  },

  toJSON(message: BatchAuthorizationRequest): unknown {
    const obj: any = {};
    if (message.requests?.length) {
      obj.requests = message.requests.map((e) => AuthorizationRequest.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BatchAuthorizationRequest>, I>>(base?: I): BatchAuthorizationRequest {
    return BatchAuthorizationRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BatchAuthorizationRequest>, I>>(object: I): BatchAuthorizationRequest {
    const message = createBaseBatchAuthorizationRequest();
    message.requests = object.requests?.map((e) => AuthorizationRequest.fromPartial(e)) || [];
    return message;
  },
};

function createBaseBatchAuthorizationResponse(): BatchAuthorizationResponse {
  return { responses: [] };
}

export const BatchAuthorizationResponse: MessageFns<BatchAuthorizationResponse> = {
  encode(message: BatchAuthorizationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.responses) {
      AuthorizationResponse.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BatchAuthorizationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchAuthorizationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responses.push(AuthorizationResponse.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BatchAuthorizationResponse {
    return {
      responses: globalThis.Array.isArray(object?.responses)
        ? object.responses.map((e: any) => AuthorizationResponse.fromJSON(e))
        : [],
    };
  },

  toJSON(message: BatchAuthorizationResponse): unknown {
    const obj: any = {};
    if (message.responses?.length) {
      obj.responses = message.responses.map((e) => AuthorizationResponse.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BatchAuthorizationResponse>, I>>(base?: I): BatchAuthorizationResponse {
    return BatchAuthorizationResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BatchAuthorizationResponse>, I>>(object: I): BatchAuthorizationResponse {
    const message = createBaseBatchAuthorizationResponse();
    message.responses = object.responses?.map((e) => AuthorizationResponse.fromPartial(e)) || [];
    return message;
  },
};

function createBasePolicyRule(): PolicyRule {
  return { ptype: "", rule: [] };
}

export const PolicyRule: MessageFns<PolicyRule> = {
  encode(message: PolicyRule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ptype !== "") {
      writer.uint32(10).string(message.ptype);
    }
    for (const v of message.rule) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PolicyRule {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePolicyRule();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ptype = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.rule.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PolicyRule {
    return {
      ptype: isSet(object.ptype) ? globalThis.String(object.ptype) : "",
      rule: globalThis.Array.isArray(object?.rule) ? object.rule.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: PolicyRule): unknown {
    const obj: any = {};
    if (message.ptype !== "") {
      obj.ptype = message.ptype;
    }
    if (message.rule?.length) {
      obj.rule = message.rule;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PolicyRule>, I>>(base?: I): PolicyRule {
    return PolicyRule.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PolicyRule>, I>>(object: I): PolicyRule {
    const message = createBasePolicyRule();
    message.ptype = object.ptype ?? "";
    message.rule = object.rule?.map((e) => e) || [];
    return message;
  },
};

function createBaseSyncPolicyRequest(): SyncPolicyRequest {
  return { domain: "", policies: [], version: 0 };
}

export const SyncPolicyRequest: MessageFns<SyncPolicyRequest> = {
  encode(message: SyncPolicyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.domain !== "") {
      writer.uint32(10).string(message.domain);
    }
    for (const v of message.policies) {
      PolicyRule.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.version !== 0) {
      writer.uint32(24).int64(message.version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SyncPolicyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSyncPolicyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.policies.push(PolicyRule.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.version = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SyncPolicyRequest {
    return {
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      policies: globalThis.Array.isArray(object?.policies)
        ? object.policies.map((e: any) => PolicyRule.fromJSON(e))
        : [],
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
    };
  },

  toJSON(message: SyncPolicyRequest): unknown {
    const obj: any = {};
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.policies?.length) {
      obj.policies = message.policies.map((e) => PolicyRule.toJSON(e));
    }
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SyncPolicyRequest>, I>>(base?: I): SyncPolicyRequest {
    return SyncPolicyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SyncPolicyRequest>, I>>(object: I): SyncPolicyRequest {
    const message = createBaseSyncPolicyRequest();
    message.domain = object.domain ?? "";
    message.policies = object.policies?.map((e) => PolicyRule.fromPartial(e)) || [];
    message.version = object.version ?? 0;
    return message;
  },
};

function createBaseSyncPolicyResponse(): SyncPolicyResponse {
  return { success: false, message: "", synced_count: 0, synced_at: undefined };
}

export const SyncPolicyResponse: MessageFns<SyncPolicyResponse> = {
  encode(message: SyncPolicyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.synced_count !== 0) {
      writer.uint32(24).int64(message.synced_count);
    }
    if (message.synced_at !== undefined) {
      Timestamp.encode(toTimestamp(message.synced_at), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SyncPolicyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSyncPolicyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.synced_count = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.synced_at = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SyncPolicyResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      synced_count: isSet(object.synced_count) ? globalThis.Number(object.synced_count) : 0,
      synced_at: isSet(object.synced_at) ? fromJsonTimestamp(object.synced_at) : undefined,
    };
  },

  toJSON(message: SyncPolicyResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.synced_count !== 0) {
      obj.synced_count = Math.round(message.synced_count);
    }
    if (message.synced_at !== undefined) {
      obj.synced_at = message.synced_at.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SyncPolicyResponse>, I>>(base?: I): SyncPolicyResponse {
    return SyncPolicyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SyncPolicyResponse>, I>>(object: I): SyncPolicyResponse {
    const message = createBaseSyncPolicyResponse();
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    message.synced_count = object.synced_count ?? 0;
    message.synced_at = object.synced_at ?? undefined;
    return message;
  },
};

function createBaseGetPolicyRequest(): GetPolicyRequest {
  return { domain: "", subject: "" };
}

export const GetPolicyRequest: MessageFns<GetPolicyRequest> = {
  encode(message: GetPolicyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.domain !== "") {
      writer.uint32(10).string(message.domain);
    }
    if (message.subject !== "") {
      writer.uint32(18).string(message.subject);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPolicyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPolicyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.subject = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPolicyRequest {
    return {
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      subject: isSet(object.subject) ? globalThis.String(object.subject) : "",
    };
  },

  toJSON(message: GetPolicyRequest): unknown {
    const obj: any = {};
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.subject !== "") {
      obj.subject = message.subject;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPolicyRequest>, I>>(base?: I): GetPolicyRequest {
    return GetPolicyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPolicyRequest>, I>>(object: I): GetPolicyRequest {
    const message = createBaseGetPolicyRequest();
    message.domain = object.domain ?? "";
    message.subject = object.subject ?? "";
    return message;
  },
};

function createBaseGetPolicyResponse(): GetPolicyResponse {
  return { policies: [], version: 0, updated_at: undefined };
}

export const GetPolicyResponse: MessageFns<GetPolicyResponse> = {
  encode(message: GetPolicyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.policies) {
      PolicyRule.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.version !== 0) {
      writer.uint32(16).int64(message.version);
    }
    if (message.updated_at !== undefined) {
      Timestamp.encode(toTimestamp(message.updated_at), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPolicyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPolicyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.policies.push(PolicyRule.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.version = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updated_at = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPolicyResponse {
    return {
      policies: globalThis.Array.isArray(object?.policies)
        ? object.policies.map((e: any) => PolicyRule.fromJSON(e))
        : [],
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
      updated_at: isSet(object.updated_at) ? fromJsonTimestamp(object.updated_at) : undefined,
    };
  },

  toJSON(message: GetPolicyResponse): unknown {
    const obj: any = {};
    if (message.policies?.length) {
      obj.policies = message.policies.map((e) => PolicyRule.toJSON(e));
    }
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    if (message.updated_at !== undefined) {
      obj.updated_at = message.updated_at.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPolicyResponse>, I>>(base?: I): GetPolicyResponse {
    return GetPolicyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPolicyResponse>, I>>(object: I): GetPolicyResponse {
    const message = createBaseGetPolicyResponse();
    message.policies = object.policies?.map((e) => PolicyRule.fromPartial(e)) || [];
    message.version = object.version ?? 0;
    message.updated_at = object.updated_at ?? undefined;
    return message;
  },
};

function createBaseAddPolicyRequest(): AddPolicyRequest {
  return { domain: "", policy: undefined };
}

export const AddPolicyRequest: MessageFns<AddPolicyRequest> = {
  encode(message: AddPolicyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.domain !== "") {
      writer.uint32(10).string(message.domain);
    }
    if (message.policy !== undefined) {
      PolicyRule.encode(message.policy, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AddPolicyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAddPolicyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.policy = PolicyRule.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AddPolicyRequest {
    return {
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      policy: isSet(object.policy) ? PolicyRule.fromJSON(object.policy) : undefined,
    };
  },

  toJSON(message: AddPolicyRequest): unknown {
    const obj: any = {};
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.policy !== undefined) {
      obj.policy = PolicyRule.toJSON(message.policy);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AddPolicyRequest>, I>>(base?: I): AddPolicyRequest {
    return AddPolicyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AddPolicyRequest>, I>>(object: I): AddPolicyRequest {
    const message = createBaseAddPolicyRequest();
    message.domain = object.domain ?? "";
    message.policy = (object.policy !== undefined && object.policy !== null)
      ? PolicyRule.fromPartial(object.policy)
      : undefined;
    return message;
  },
};

function createBaseRemovePolicyRequest(): RemovePolicyRequest {
  return { domain: "", policy: undefined };
}

export const RemovePolicyRequest: MessageFns<RemovePolicyRequest> = {
  encode(message: RemovePolicyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.domain !== "") {
      writer.uint32(10).string(message.domain);
    }
    if (message.policy !== undefined) {
      PolicyRule.encode(message.policy, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemovePolicyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemovePolicyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.policy = PolicyRule.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemovePolicyRequest {
    return {
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      policy: isSet(object.policy) ? PolicyRule.fromJSON(object.policy) : undefined,
    };
  },

  toJSON(message: RemovePolicyRequest): unknown {
    const obj: any = {};
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.policy !== undefined) {
      obj.policy = PolicyRule.toJSON(message.policy);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RemovePolicyRequest>, I>>(base?: I): RemovePolicyRequest {
    return RemovePolicyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RemovePolicyRequest>, I>>(object: I): RemovePolicyRequest {
    const message = createBaseRemovePolicyRequest();
    message.domain = object.domain ?? "";
    message.policy = (object.policy !== undefined && object.policy !== null)
      ? PolicyRule.fromPartial(object.policy)
      : undefined;
    return message;
  },
};

function createBasePolicyOperationResponse(): PolicyOperationResponse {
  return { success: false, message: "" };
}

export const PolicyOperationResponse: MessageFns<PolicyOperationResponse> = {
  encode(message: PolicyOperationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PolicyOperationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePolicyOperationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PolicyOperationResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: PolicyOperationResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PolicyOperationResponse>, I>>(base?: I): PolicyOperationResponse {
    return PolicyOperationResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PolicyOperationResponse>, I>>(object: I): PolicyOperationResponse {
    const message = createBasePolicyOperationResponse();
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseAuditLog(): AuditLog {
  return {
    trace_id: "",
    timestamp: undefined,
    subject: "",
    action: "",
    resource: "",
    allowed: false,
    decision_source: "",
    reason: "",
    context: undefined,
    ip_address: "",
    user_agent: "",
  };
}

export const AuditLog: MessageFns<AuditLog> = {
  encode(message: AuditLog, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.trace_id !== "") {
      writer.uint32(10).string(message.trace_id);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(18).fork()).join();
    }
    if (message.subject !== "") {
      writer.uint32(26).string(message.subject);
    }
    if (message.action !== "") {
      writer.uint32(34).string(message.action);
    }
    if (message.resource !== "") {
      writer.uint32(42).string(message.resource);
    }
    if (message.allowed !== false) {
      writer.uint32(48).bool(message.allowed);
    }
    if (message.decision_source !== "") {
      writer.uint32(58).string(message.decision_source);
    }
    if (message.reason !== "") {
      writer.uint32(66).string(message.reason);
    }
    if (message.context !== undefined) {
      Struct.encode(Struct.wrap(message.context), writer.uint32(74).fork()).join();
    }
    if (message.ip_address !== "") {
      writer.uint32(82).string(message.ip_address);
    }
    if (message.user_agent !== "") {
      writer.uint32(90).string(message.user_agent);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuditLog {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuditLog();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.trace_id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.subject = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.action = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.resource = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.allowed = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.decision_source = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.context = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.ip_address = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.user_agent = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuditLog {
    return {
      trace_id: isSet(object.trace_id) ? globalThis.String(object.trace_id) : "",
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      subject: isSet(object.subject) ? globalThis.String(object.subject) : "",
      action: isSet(object.action) ? globalThis.String(object.action) : "",
      resource: isSet(object.resource) ? globalThis.String(object.resource) : "",
      allowed: isSet(object.allowed) ? globalThis.Boolean(object.allowed) : false,
      decision_source: isSet(object.decision_source) ? globalThis.String(object.decision_source) : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      context: isObject(object.context) ? object.context : undefined,
      ip_address: isSet(object.ip_address) ? globalThis.String(object.ip_address) : "",
      user_agent: isSet(object.user_agent) ? globalThis.String(object.user_agent) : "",
    };
  },

  toJSON(message: AuditLog): unknown {
    const obj: any = {};
    if (message.trace_id !== "") {
      obj.trace_id = message.trace_id;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.subject !== "") {
      obj.subject = message.subject;
    }
    if (message.action !== "") {
      obj.action = message.action;
    }
    if (message.resource !== "") {
      obj.resource = message.resource;
    }
    if (message.allowed !== false) {
      obj.allowed = message.allowed;
    }
    if (message.decision_source !== "") {
      obj.decision_source = message.decision_source;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.context !== undefined) {
      obj.context = message.context;
    }
    if (message.ip_address !== "") {
      obj.ip_address = message.ip_address;
    }
    if (message.user_agent !== "") {
      obj.user_agent = message.user_agent;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AuditLog>, I>>(base?: I): AuditLog {
    return AuditLog.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AuditLog>, I>>(object: I): AuditLog {
    const message = createBaseAuditLog();
    message.trace_id = object.trace_id ?? "";
    message.timestamp = object.timestamp ?? undefined;
    message.subject = object.subject ?? "";
    message.action = object.action ?? "";
    message.resource = object.resource ?? "";
    message.allowed = object.allowed ?? false;
    message.decision_source = object.decision_source ?? "";
    message.reason = object.reason ?? "";
    message.context = object.context ?? undefined;
    message.ip_address = object.ip_address ?? "";
    message.user_agent = object.user_agent ?? "";
    return message;
  },
};

function createBaseQueryAuditLogsRequest(): QueryAuditLogsRequest {
  return { subject: "", resource: "", start_time: undefined, end_time: undefined, page_size: 0, page_token: "" };
}

export const QueryAuditLogsRequest: MessageFns<QueryAuditLogsRequest> = {
  encode(message: QueryAuditLogsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.subject !== "") {
      writer.uint32(10).string(message.subject);
    }
    if (message.resource !== "") {
      writer.uint32(18).string(message.resource);
    }
    if (message.start_time !== undefined) {
      Timestamp.encode(toTimestamp(message.start_time), writer.uint32(26).fork()).join();
    }
    if (message.end_time !== undefined) {
      Timestamp.encode(toTimestamp(message.end_time), writer.uint32(34).fork()).join();
    }
    if (message.page_size !== 0) {
      writer.uint32(40).int32(message.page_size);
    }
    if (message.page_token !== "") {
      writer.uint32(50).string(message.page_token);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryAuditLogsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryAuditLogsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.subject = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.resource = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.start_time = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.end_time = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.page_size = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.page_token = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryAuditLogsRequest {
    return {
      subject: isSet(object.subject) ? globalThis.String(object.subject) : "",
      resource: isSet(object.resource) ? globalThis.String(object.resource) : "",
      start_time: isSet(object.start_time) ? fromJsonTimestamp(object.start_time) : undefined,
      end_time: isSet(object.end_time) ? fromJsonTimestamp(object.end_time) : undefined,
      page_size: isSet(object.page_size) ? globalThis.Number(object.page_size) : 0,
      page_token: isSet(object.page_token) ? globalThis.String(object.page_token) : "",
    };
  },

  toJSON(message: QueryAuditLogsRequest): unknown {
    const obj: any = {};
    if (message.subject !== "") {
      obj.subject = message.subject;
    }
    if (message.resource !== "") {
      obj.resource = message.resource;
    }
    if (message.start_time !== undefined) {
      obj.start_time = message.start_time.toISOString();
    }
    if (message.end_time !== undefined) {
      obj.end_time = message.end_time.toISOString();
    }
    if (message.page_size !== 0) {
      obj.page_size = Math.round(message.page_size);
    }
    if (message.page_token !== "") {
      obj.page_token = message.page_token;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryAuditLogsRequest>, I>>(base?: I): QueryAuditLogsRequest {
    return QueryAuditLogsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryAuditLogsRequest>, I>>(object: I): QueryAuditLogsRequest {
    const message = createBaseQueryAuditLogsRequest();
    message.subject = object.subject ?? "";
    message.resource = object.resource ?? "";
    message.start_time = object.start_time ?? undefined;
    message.end_time = object.end_time ?? undefined;
    message.page_size = object.page_size ?? 0;
    message.page_token = object.page_token ?? "";
    return message;
  },
};

function createBaseQueryAuditLogsResponse(): QueryAuditLogsResponse {
  return { logs: [], next_page_token: "", total_count: 0 };
}

export const QueryAuditLogsResponse: MessageFns<QueryAuditLogsResponse> = {
  encode(message: QueryAuditLogsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.logs) {
      AuditLog.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.next_page_token !== "") {
      writer.uint32(18).string(message.next_page_token);
    }
    if (message.total_count !== 0) {
      writer.uint32(24).int64(message.total_count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryAuditLogsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryAuditLogsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.logs.push(AuditLog.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.next_page_token = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.total_count = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryAuditLogsResponse {
    return {
      logs: globalThis.Array.isArray(object?.logs) ? object.logs.map((e: any) => AuditLog.fromJSON(e)) : [],
      next_page_token: isSet(object.next_page_token) ? globalThis.String(object.next_page_token) : "",
      total_count: isSet(object.total_count) ? globalThis.Number(object.total_count) : 0,
    };
  },

  toJSON(message: QueryAuditLogsResponse): unknown {
    const obj: any = {};
    if (message.logs?.length) {
      obj.logs = message.logs.map((e) => AuditLog.toJSON(e));
    }
    if (message.next_page_token !== "") {
      obj.next_page_token = message.next_page_token;
    }
    if (message.total_count !== 0) {
      obj.total_count = Math.round(message.total_count);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryAuditLogsResponse>, I>>(base?: I): QueryAuditLogsResponse {
    return QueryAuditLogsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryAuditLogsResponse>, I>>(object: I): QueryAuditLogsResponse {
    const message = createBaseQueryAuditLogsResponse();
    message.logs = object.logs?.map((e) => AuditLog.fromPartial(e)) || [];
    message.next_page_token = object.next_page_token ?? "";
    message.total_count = object.total_count ?? 0;
    return message;
  },
};

function createBaseHealthCheckRequest(): HealthCheckRequest {
  return { service: "" };
}

export const HealthCheckRequest: MessageFns<HealthCheckRequest> = {
  encode(message: HealthCheckRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service !== "") {
      writer.uint32(10).string(message.service);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthCheckRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthCheckRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HealthCheckRequest {
    return { service: isSet(object.service) ? globalThis.String(object.service) : "" };
  },

  toJSON(message: HealthCheckRequest): unknown {
    const obj: any = {};
    if (message.service !== "") {
      obj.service = message.service;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HealthCheckRequest>, I>>(base?: I): HealthCheckRequest {
    return HealthCheckRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HealthCheckRequest>, I>>(object: I): HealthCheckRequest {
    const message = createBaseHealthCheckRequest();
    message.service = object.service ?? "";
    return message;
  },
};

function createBaseHealthCheckResponse(): HealthCheckResponse {
  return { status: 0, components: undefined, version: "" };
}

export const HealthCheckResponse: MessageFns<HealthCheckResponse> = {
  encode(message: HealthCheckResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    if (message.components !== undefined) {
      Struct.encode(Struct.wrap(message.components), writer.uint32(18).fork()).join();
    }
    if (message.version !== "") {
      writer.uint32(26).string(message.version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthCheckResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthCheckResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.components = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.version = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HealthCheckResponse {
    return {
      status: isSet(object.status) ? healthCheckResponse_ServingStatusFromJSON(object.status) : 0,
      components: isObject(object.components) ? object.components : undefined,
      version: isSet(object.version) ? globalThis.String(object.version) : "",
    };
  },

  toJSON(message: HealthCheckResponse): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = healthCheckResponse_ServingStatusToJSON(message.status);
    }
    if (message.components !== undefined) {
      obj.components = message.components;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HealthCheckResponse>, I>>(base?: I): HealthCheckResponse {
    return HealthCheckResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HealthCheckResponse>, I>>(object: I): HealthCheckResponse {
    const message = createBaseHealthCheckResponse();
    message.status = object.status ?? 0;
    message.components = object.components ?? undefined;
    message.version = object.version ?? "";
    return message;
  },
};

/** 授权服务（OPA + Casbin 混合决策） */
export interface AuthorizationService {
  /** 单次授权检查 */
  CheckAuthorization(request: AuthorizationRequest): Promise<AuthorizationResponse>;
  /** 批量授权检查 */
  BatchCheckAuthorization(request: BatchAuthorizationRequest): Promise<BatchAuthorizationResponse>;
  /** 同步策略（从 OPA 到 Casbin） */
  SyncPolicy(request: SyncPolicyRequest): Promise<SyncPolicyResponse>;
  /** 获取策略 */
  GetPolicy(request: GetPolicyRequest): Promise<GetPolicyResponse>;
  /** 添加策略 */
  AddPolicy(request: AddPolicyRequest): Promise<PolicyOperationResponse>;
  /** 删除策略 */
  RemovePolicy(request: RemovePolicyRequest): Promise<PolicyOperationResponse>;
  /** 查询审计日志 */
  QueryAuditLogs(request: QueryAuditLogsRequest): Promise<QueryAuditLogsResponse>;
  /** 健康检查 */
  HealthCheck(request: HealthCheckRequest): Promise<HealthCheckResponse>;
}

export const AuthorizationServiceServiceName = "stew.api.v1.AuthorizationService";
export class AuthorizationServiceClientImpl implements AuthorizationService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || AuthorizationServiceServiceName;
    this.rpc = rpc;
    this.CheckAuthorization = this.CheckAuthorization.bind(this);
    this.BatchCheckAuthorization = this.BatchCheckAuthorization.bind(this);
    this.SyncPolicy = this.SyncPolicy.bind(this);
    this.GetPolicy = this.GetPolicy.bind(this);
    this.AddPolicy = this.AddPolicy.bind(this);
    this.RemovePolicy = this.RemovePolicy.bind(this);
    this.QueryAuditLogs = this.QueryAuditLogs.bind(this);
    this.HealthCheck = this.HealthCheck.bind(this);
  }
  CheckAuthorization(request: AuthorizationRequest): Promise<AuthorizationResponse> {
    const data = AuthorizationRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "CheckAuthorization", data);
    return promise.then((data) => AuthorizationResponse.decode(new BinaryReader(data)));
  }

  BatchCheckAuthorization(request: BatchAuthorizationRequest): Promise<BatchAuthorizationResponse> {
    const data = BatchAuthorizationRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "BatchCheckAuthorization", data);
    return promise.then((data) => BatchAuthorizationResponse.decode(new BinaryReader(data)));
  }

  SyncPolicy(request: SyncPolicyRequest): Promise<SyncPolicyResponse> {
    const data = SyncPolicyRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "SyncPolicy", data);
    return promise.then((data) => SyncPolicyResponse.decode(new BinaryReader(data)));
  }

  GetPolicy(request: GetPolicyRequest): Promise<GetPolicyResponse> {
    const data = GetPolicyRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetPolicy", data);
    return promise.then((data) => GetPolicyResponse.decode(new BinaryReader(data)));
  }

  AddPolicy(request: AddPolicyRequest): Promise<PolicyOperationResponse> {
    const data = AddPolicyRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "AddPolicy", data);
    return promise.then((data) => PolicyOperationResponse.decode(new BinaryReader(data)));
  }

  RemovePolicy(request: RemovePolicyRequest): Promise<PolicyOperationResponse> {
    const data = RemovePolicyRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "RemovePolicy", data);
    return promise.then((data) => PolicyOperationResponse.decode(new BinaryReader(data)));
  }

  QueryAuditLogs(request: QueryAuditLogsRequest): Promise<QueryAuditLogsResponse> {
    const data = QueryAuditLogsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "QueryAuditLogs", data);
    return promise.then((data) => QueryAuditLogsResponse.decode(new BinaryReader(data)));
  }

  HealthCheck(request: HealthCheckRequest): Promise<HealthCheckResponse> {
    const data = HealthCheckRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "HealthCheck", data);
    return promise.then((data) => HealthCheckResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
