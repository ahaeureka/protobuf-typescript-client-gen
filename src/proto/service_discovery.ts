// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v3.21.12
// source: service_discovery.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Any } from "./google/protobuf/any";
import { Struct } from "./google/protobuf/struct";
import { Timestamp } from "./google/protobuf/timestamp";
import { ServiceBillingConfig } from "./stew/api/v1/billing";

export const protobufPackage = "stew.api.v1";

/**
 * pub enum BalanceType {
 *     // RoundRobin 轮询
 *     RR,
 *     // WeightedRoundRobin 加权轮询
 *     WRR,
 *     // ConsistentHash
 *     ConsistentHash,
 *     // LeastConnections 最少连接
 *     LC,
 *     // SourceIPHash 源地址哈希
 *     SED,
 *     // WeightedLeastConnections 加权最少连接
 *     WLC,
 *     // // NeverQueuejum
 *     NQ,
 * }
 */
export enum BalanceType {
  BALANCE_TYPE_UNKNOWN = 0,
  /** BALANCE_TYPE_ROUND_ROBIN - RR */
  BALANCE_TYPE_ROUND_ROBIN = 1,
  /** BALANCE_TYPE_WEIGHTED_ROUND_ROBIN - WRR */
  BALANCE_TYPE_WEIGHTED_ROUND_ROBIN = 2,
  /** BALANCE_TYPE_CONSISTENT_HASH - ConsistentHash */
  BALANCE_TYPE_CONSISTENT_HASH = 3,
  /** BALANCE_TYPE_LEAST_CONNECTIONS - LC */
  BALANCE_TYPE_LEAST_CONNECTIONS = 4,
  /** BALANCE_TYPE_SED - SED Shortest Expected Delay */
  BALANCE_TYPE_SED = 5,
  /** BALANCE_TYPE_WEIGHTED_LEAST_CONNECTIONS - WLC */
  BALANCE_TYPE_WEIGHTED_LEAST_CONNECTIONS = 6,
  /** BALANCE_TYPE_NEVER_QUEUE - NQ Never Queue */
  BALANCE_TYPE_NEVER_QUEUE = 7,
  UNRECOGNIZED = -1,
}

export function balanceTypeFromJSON(object: any): BalanceType {
  switch (object) {
    case 0:
    case "BALANCE_TYPE_UNKNOWN":
      return BalanceType.BALANCE_TYPE_UNKNOWN;
    case 1:
    case "BALANCE_TYPE_ROUND_ROBIN":
      return BalanceType.BALANCE_TYPE_ROUND_ROBIN;
    case 2:
    case "BALANCE_TYPE_WEIGHTED_ROUND_ROBIN":
      return BalanceType.BALANCE_TYPE_WEIGHTED_ROUND_ROBIN;
    case 3:
    case "BALANCE_TYPE_CONSISTENT_HASH":
      return BalanceType.BALANCE_TYPE_CONSISTENT_HASH;
    case 4:
    case "BALANCE_TYPE_LEAST_CONNECTIONS":
      return BalanceType.BALANCE_TYPE_LEAST_CONNECTIONS;
    case 5:
    case "BALANCE_TYPE_SED":
      return BalanceType.BALANCE_TYPE_SED;
    case 6:
    case "BALANCE_TYPE_WEIGHTED_LEAST_CONNECTIONS":
      return BalanceType.BALANCE_TYPE_WEIGHTED_LEAST_CONNECTIONS;
    case 7:
    case "BALANCE_TYPE_NEVER_QUEUE":
      return BalanceType.BALANCE_TYPE_NEVER_QUEUE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return BalanceType.UNRECOGNIZED;
  }
}

export function balanceTypeToJSON(object: BalanceType): string {
  switch (object) {
    case BalanceType.BALANCE_TYPE_UNKNOWN:
      return "BALANCE_TYPE_UNKNOWN";
    case BalanceType.BALANCE_TYPE_ROUND_ROBIN:
      return "BALANCE_TYPE_ROUND_ROBIN";
    case BalanceType.BALANCE_TYPE_WEIGHTED_ROUND_ROBIN:
      return "BALANCE_TYPE_WEIGHTED_ROUND_ROBIN";
    case BalanceType.BALANCE_TYPE_CONSISTENT_HASH:
      return "BALANCE_TYPE_CONSISTENT_HASH";
    case BalanceType.BALANCE_TYPE_LEAST_CONNECTIONS:
      return "BALANCE_TYPE_LEAST_CONNECTIONS";
    case BalanceType.BALANCE_TYPE_SED:
      return "BALANCE_TYPE_SED";
    case BalanceType.BALANCE_TYPE_WEIGHTED_LEAST_CONNECTIONS:
      return "BALANCE_TYPE_WEIGHTED_LEAST_CONNECTIONS";
    case BalanceType.BALANCE_TYPE_NEVER_QUEUE:
      return "BALANCE_TYPE_NEVER_QUEUE";
    case BalanceType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** 服务状态枚举 */
export enum ServiceStatus {
  SERVICE_STATUS_UNKNOWN = 0,
  SERVICE_STATUS_HEALTHY = 1,
  SERVICE_STATUS_UNHEALTHY = 2,
  SERVICE_STATUS_MAINTENANCE = 3,
  SERVICE_STATUS_DRAINING = 4,
  UNRECOGNIZED = -1,
}

export function serviceStatusFromJSON(object: any): ServiceStatus {
  switch (object) {
    case 0:
    case "SERVICE_STATUS_UNKNOWN":
      return ServiceStatus.SERVICE_STATUS_UNKNOWN;
    case 1:
    case "SERVICE_STATUS_HEALTHY":
      return ServiceStatus.SERVICE_STATUS_HEALTHY;
    case 2:
    case "SERVICE_STATUS_UNHEALTHY":
      return ServiceStatus.SERVICE_STATUS_UNHEALTHY;
    case 3:
    case "SERVICE_STATUS_MAINTENANCE":
      return ServiceStatus.SERVICE_STATUS_MAINTENANCE;
    case 4:
    case "SERVICE_STATUS_DRAINING":
      return ServiceStatus.SERVICE_STATUS_DRAINING;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ServiceStatus.UNRECOGNIZED;
  }
}

export function serviceStatusToJSON(object: ServiceStatus): string {
  switch (object) {
    case ServiceStatus.SERVICE_STATUS_UNKNOWN:
      return "SERVICE_STATUS_UNKNOWN";
    case ServiceStatus.SERVICE_STATUS_HEALTHY:
      return "SERVICE_STATUS_HEALTHY";
    case ServiceStatus.SERVICE_STATUS_UNHEALTHY:
      return "SERVICE_STATUS_UNHEALTHY";
    case ServiceStatus.SERVICE_STATUS_MAINTENANCE:
      return "SERVICE_STATUS_MAINTENANCE";
    case ServiceStatus.SERVICE_STATUS_DRAINING:
      return "SERVICE_STATUS_DRAINING";
    case ServiceStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Endpoint {
  address: string;
  port: number;
  /** 服务权重（负载均衡用） */
  weight: number;
}

export interface LoadBalancer {
  type: BalanceType;
  /** 负载均衡参数 */
  endpoints: Endpoint[];
}

/** 健康检查配置 */
export interface HealthCheckConfig {
  /**
   * gRPC 方法名（例如："stew.api.v1.UserService/Get"）
   * 如果为空，则使用服务的第一个可用方法
   */
  grpc_method: string;
  /** HTTP 健康检查路径（例如："/health" 或 "/api/v1/health"） */
  http_path: string;
  /** 检查间隔（秒），默认 30 秒 */
  interval_seconds: number;
  /** 超时时间（秒），默认 5 秒 */
  timeout_seconds: number;
  /** 连续成功次数阈值（达到后标记为健康） */
  healthy_threshold: number;
  /** 连续失败次数阈值（达到后标记为不健康） */
  unhealthy_threshold: number;
  /** 是否启用健康检查（默认启用） */
  enabled: boolean;
}

export interface ServiceCorsConfig {
  enabled: boolean;
  allow_origins: string[];
  allow_methods: string[];
  allow_headers: string[];
  expose_headers: string[];
  allow_credentials: boolean;
  max_age_secs: number;
}

export interface ServiceRiskRuleConfig {
  name: string;
  enabled: boolean;
  path_prefixes: string[];
  countries: string[];
  proxy?: boolean | undefined;
  tor?: boolean | undefined;
  datacenter?: boolean | undefined;
  min_bot_score?: number | undefined;
  max_bot_score?: number | undefined;
  action: string;
}

export interface ServiceRiskConfig {
  enabled: boolean;
  mode: string;
  default_action: string;
  challenge_paths: string[];
  block_paths: string[];
  observe_only_paths: string[];
  high_risk_countries: string[];
  challenge_proxy_traffic: boolean;
  block_datacenter_traffic: boolean;
  allow_tor_exit_nodes: boolean;
  bot_score_threshold: number;
  proxy_score_threshold: number;
  action_overrides: ServiceRiskRuleConfig[];
}

export interface ServiceTurnstileConfig {
  enabled: boolean;
  required_paths: string[];
  skip_paths: string[];
  expected_action: string;
  expected_hostname: string;
  enforce_on_risk_challenge: boolean;
}

/**
 * AI Guard: configurable body field path mapping (Tier 2 extraction strategy).
 * When set, the gateway uses these paths to locate user messages, model name,
 * etc. in the JSON request body. Leave empty to fall back to heuristic detection.
 */
export interface AiBodyFieldMap {
  messages_path: string;
  role_field: string;
  content_field: string;
  user_role_value: string;
  prompt_path: string;
  model_path: string;
  max_tokens_path: string;
}

/**
 * AI SaaS abuse prevention configuration (per-service).
 * Controls token quotas, intent classification, context truncation,
 * and topic filtering for AI-related API endpoints.
 */
export interface ServiceAiGuardConfig {
  enabled: boolean;
  mode: string;
  include_paths: string[];
  request_body_max_bytes: number;
  max_input_tokens: number;
  max_output_tokens: number;
  max_context_tokens: number;
  history_policy: string;
  daily_token_quota: number;
  daily_request_quota: number;
  minute_request_quota: number;
  allow_free_chat: boolean;
  allowed_topics: string[];
  deny_keywords: string[];
  enable_audit: boolean;
  classifier_type: string;
  llm_endpoint: string;
  llm_model: string;
  llm_system_prompt: string;
  llm_timeout_ms: number;
  llm_confidence_threshold: number;
  body_map: AiBodyFieldMap | undefined;
  quota_window_secs: number;
  /**
   * Business intent configuration for the LLM classifier.
   * When llm_system_prompt is empty, these fields are used to automatically
   * build the intent classification prompt.
   */
  business_description: string;
  valid_intent_examples: string[];
  invalid_intent_examples: string[];
  /**
   * Per-endpoint configuration overrides (v2.0).
   * Each entry can override service-level defaults for a specific API endpoint.
   * Non-zero/non-empty fields override the parent ServiceAiGuardConfig value.
   * Matching priority: exact_paths > pattern_paths (by literal score) > longest prefix_paths > service-level default.
   */
  endpoint_overrides: AiGuardEndpointConfig[];
}

/**
 * Per-endpoint AI Guard configuration override.
 * Binds to specific API paths via exact or prefix matching.
 * Non-zero/non-empty fields override the parent ServiceAiGuardConfig value;
 * zero/empty fields inherit the service-level default.
 */
export interface AiGuardEndpointConfig {
  /**
   * Endpoint identifier (admin-assigned, used in Redis keys and audit logs).
   * Must be unique within the same service.
   */
  endpoint_id: string;
  /** Exact path matches, e.g. ["/stew.api.v1.ChatService/SendMessage", "/v1/chat/completions"] */
  exact_paths: string[];
  /** Prefix path matches, e.g. ["/stew.api.v1.Chat"] matches all methods under that prefix */
  prefix_paths: string[];
  /**
   * Path template matches using {param} placeholders for single-segment wildcards,
   * e.g. ["/api/v1/sessions/{session_id}/reflection"] matches any session ID.
   * Matching priority: exact_paths > pattern_paths (by literal score) > prefix_paths.
   */
  pattern_paths: string[];
  /** Disable AI Guard for this endpoint (true = skip even if service-level is enabled) */
  disabled?: boolean | undefined;
  mode: string;
  request_body_max_bytes: number;
  max_input_tokens: number;
  max_output_tokens: number;
  max_context_tokens: number;
  history_policy: string;
  daily_token_quota: number;
  daily_request_quota: number;
  minute_request_quota: number;
  quota_window_secs: number;
  allow_free_chat?: boolean | undefined;
  allowed_topics: string[];
  deny_keywords: string[];
  enable_audit?: boolean | undefined;
  classifier_type: string;
  llm_endpoint: string;
  llm_model: string;
  llm_system_prompt: string;
  business_description: string;
  valid_intent_examples: string[];
  invalid_intent_examples: string[];
  llm_timeout_ms: number;
  llm_confidence_threshold: number;
  body_map: AiBodyFieldMap | undefined;
}

/**
 * 每个下游服务的中间件开关配置
 * 注册或编辑服务时可单独控制各中间件的启用状态和参数。
 * 配置随 ServiceInstance 一起存储在 ETCD，运行时热生效。
 */
export interface ServiceMiddlewareConfig {
  /** 是否为该服务启用限流（false = 跳过限流检查） */
  rate_limit_enabled: boolean;
  /** 该服务的每分钟请求数上限（0 = 沿用全局默认值） */
  rate_limit_rpm: number;
  /** 该服务的每用户每分钟请求数上限（0 = 沿用全局默认值） */
  rate_limit_user_rpm: number;
  cors_enabled: boolean;
  cors: ServiceCorsConfig | undefined;
  risk_enabled: boolean;
  risk: ServiceRiskConfig | undefined;
  turnstile_enabled: boolean;
  turnstile: ServiceTurnstileConfig | undefined;
  ai_guard_enabled: boolean;
  ai_guard: ServiceAiGuardConfig | undefined;
  billing_enabled: boolean;
  billing: ServiceBillingConfig | undefined;
}

/** 服务实例定义 */
export interface ServiceInstance {
  /** 服务名称 */
  service_name: string;
  /** 实例ID（服务内唯一） */
  instance_id: string;
  /**
   * // 服务端口
   *   uint32 port = 4 [json_name="port", (pydantic.field) = {description: "Service port", required: true}];
   */
  lb:
    | LoadBalancer
    | undefined;
  /** 服务版本 */
  version: string;
  /** 服务元数据 */
  metadata: { [key: string]: Any };
  /** 健康检查端点 */
  health_endpoint: string;
  /** 健康检查配置（新增） */
  health_check_config:
    | HealthCheckConfig
    | undefined;
  /** 注册时间戳 */
  registered_at:
    | Date
    | undefined;
  /** 服务状态 */
  status: ServiceStatus;
  /** 服务权重（负载均衡用） */
  weight: number;
  /** 服务标签 */
  tags: { [key: string]: string };
  /** 协议类型 */
  protocol: string;
  /** 是否支持TLS */
  tls_enabled: boolean;
  /** 服务的 protobuf 描述符文件（.pb 二进制数据） */
  protobuf_descriptor: Uint8Array;
  /** 每服务中间件配置（注册/编辑服务时设置，存储在 ETCD） */
  middleware_config: ServiceMiddlewareConfig | undefined;
}

export interface ServiceInstance_MetadataEntry {
  key: string;
  value: Any | undefined;
}

export interface ServiceInstance_TagsEntry {
  key: string;
  value: string;
}

/**
 * 初始化服务请求（管理端专属）
 * 管理员在管理端初始化服务后，系统生成 app_id + app_secret 凭证供业务侧使用
 */
export interface InitServiceRequest {
  /** Globally unique service name. Duplicate names are rejected. */
  service_name: string;
  description: string;
  protocol: string;
}

/** 初始化服务响应 */
export interface InitServiceResponse {
  success: boolean;
  message: string;
  app_id: string;
  app_secret: string;
  service_name: string;
}

/** 注册服务请求 */
export interface RegisterServiceRequest {
  service:
    | ServiceInstance
    | undefined;
  /** TTL（生存时间，秒） */
  ttl: number;
}

/** 注册服务响应 */
export interface RegisterServiceResponse {
  success: boolean;
  message: string;
  lease_id: string;
  instance_id: string;
}

/** 业务侧追加单个服务 endpoint（不会覆盖已有 endpoint 配置） */
export interface RegisterServiceEndpointRequest {
  service_name: string;
  endpoint_id: string;
  endpoint: Endpoint | undefined;
  version: string;
  protocol: string;
  tls_enabled: boolean;
  protobuf_descriptor: Uint8Array;
}

export interface RegisterServiceEndpointResponse {
  success: boolean;
  message: string;
  endpoint_id: string;
  lease_id: string;
  registered_service: ServiceInstance | undefined;
}

export interface DeregisterServiceEndpointRequest {
  service_name: string;
  endpoint_id: string;
}

export interface DeregisterServiceEndpointResponse {
  success: boolean;
  message: string;
}

/** 注销服务请求 */
export interface DeregisterServiceRequest {
  service_name: string;
  instance_id: string;
}

/** 注销服务响应 */
export interface DeregisterServiceResponse {
  success: boolean;
  message: string;
}

/** 删除持久化服务记录请求 */
export interface DeleteServiceRecordRequest {
  service_name: string;
  instance_id: string;
}

/** 删除持久化服务记录响应 */
export interface DeleteServiceRecordResponse {
  success: boolean;
  message: string;
}

/** 更新服务实例请求 */
export interface UpdateServiceInstanceRequest {
  service: ServiceInstance | undefined;
}

/** 更新服务实例响应 */
export interface UpdateServiceInstanceResponse {
  success: boolean;
  message: string;
  updated_service: ServiceInstance | undefined;
}

/** 获取服务实例请求 */
export interface GetServiceInstancesRequest {
  service_name: string;
  /** 可选的标签过滤 */
  tag_filters: { [key: string]: string };
  /** 是否只返回健康的实例 */
  healthy_only: boolean;
}

export interface GetServiceInstancesRequest_TagFiltersEntry {
  key: string;
  value: string;
}

/** 获取服务实例响应 */
export interface GetServiceInstancesResponse {
  instances: ServiceInstance[];
  total_count: number;
}

/** 列出所有服务请求 */
export interface ListServicesRequest {
  /** 服务名前缀过滤 */
  name_prefix: string;
  /** 标签过滤 */
  tag_filters: { [key: string]: string };
}

export interface ListServicesRequest_TagFiltersEntry {
  key: string;
  value: string;
}

/** 列出所有服务响应 */
export interface ListServicesResponse {
  services: ServiceInstance[];
  /**
   * 服务名称列表
   *   repeated string service_names = 1 [json_name="service_names", (pydantic.field) = {description: "List of service names"}];
   * 服务统计信息
   *   map<string, ServiceSummary> service_summaries = 2 [json_name="service_summaries", (pydantic.field) = {description: "Service summary information"}];
   */
  total_count: number;
}

/** 服务摘要信息 */
export interface ServiceSummary {
  service_name: string;
  instance_count: number;
  healthy_count: number;
  versions: string[];
}

/** 更新服务健康状态请求 */
export interface UpdateServiceHealthRequest {
  service_name: string;
  instance_id: string;
  status: ServiceStatus;
  health_message: string;
}

/** 更新服务健康状态响应 */
export interface UpdateServiceHealthResponse {
  success: boolean;
  message: string;
}

/** 服务健康检查请求 */
export interface ServiceHealthCheckRequest {
  service_name: string;
  instance_id: string;
}

/** 服务健康检查响应 */
export interface ServiceHealthCheckResponse {
  instance_healths: ServiceInstanceHealth[];
}

/** 服务实例健康状态 */
export interface ServiceInstanceHealth {
  instance: ServiceInstance | undefined;
  status: ServiceStatus;
  message: string;
  last_check: Date | undefined;
}

/** 上传服务配置请求 */
export interface UploadServiceConfigRequest {
  service_name: string;
  config_version: string;
  config_data: { [key: string]: any } | undefined;
  description: string;
}

/** 上传服务配置响应 */
export interface UploadServiceConfigResponse {
  success: boolean;
  message: string;
  config_key: string;
}

/** 获取服务配置请求 */
export interface GetServiceConfigRequest {
  service_name: string;
  config_version: string;
}

/** 获取服务配置响应 */
export interface GetServiceConfigResponse {
  config_data: { [key: string]: any } | undefined;
  config_version: string;
  updated_at: Date | undefined;
  description: string;
}

/** 上传 protobuf 描述符请求 */
export interface UploadProtobufDescriptorRequest {
  service_name: string;
  descriptor_version: string;
  descriptor_data: Uint8Array;
  description: string;
  /** HMAC-SHA256(descriptor_data, shared_secret) - optional integrity verification */
  signature: string;
  /** Force update, skip compatibility warnings */
  force: boolean;
  /** Previous version for optimistic locking (reject if active version != previous_version) */
  previous_version: string;
}

/** 上传 protobuf 描述符响应 */
export interface UploadProtobufDescriptorResponse {
  success: boolean;
  message: string;
  descriptor_key: string;
  discovered_services: string[];
  /** Compatibility warnings (breaking changes detected but not blocked) */
  compatibility_warnings: string[];
  /** Actual version applied (may differ from requested if auto-generated) */
  applied_version: string;
  /** SHA-256 hash of the descriptor data */
  descriptor_hash: string;
}

/** 获取 protobuf 描述符请求 */
export interface GetProtobufDescriptorRequest {
  service_name: string;
  descriptor_version: string;
}

/** 获取 protobuf 描述符响应 */
export interface GetProtobufDescriptorResponse {
  descriptor_data: Uint8Array;
  descriptor_version: string;
  updated_at: Date | undefined;
  description: string;
  services: string[];
}

/** 列出可用的 protobuf 描述符请求 */
export interface ListProtobufDescriptorsRequest {
  service_name_prefix: string;
}

/** 列出可用的 protobuf 描述符响应 */
export interface ListProtobufDescriptorsResponse {
  descriptors: ProtobufDescriptorInfo[];
}

/** protobuf 描述符信息 */
export interface ProtobufDescriptorInfo {
  service_name: string;
  descriptor_version: string;
  updated_at: Date | undefined;
  description: string;
  services: string[];
  size_bytes: number;
  descriptor_hash: string;
  is_active: boolean;
}

/** 描述符版本详细信息 */
export interface DescriptorVersionInfo {
  version: string;
  descriptor_hash: string;
  created_at: Date | undefined;
  description: string;
  services: string[];
  size_bytes: number;
  is_active: boolean;
}

/** 回滚描述符请求 */
export interface RollbackDescriptorRequest {
  service_name: string;
  target_version: string;
}

/** 回滚描述符响应 */
export interface RollbackDescriptorResponse {
  success: boolean;
  message: string;
  active_version: string;
  discovered_services: string[];
}

/** 列出描述符版本请求 */
export interface ListDescriptorVersionsRequest {
  service_name: string;
}

/** 列出描述符版本响应 */
export interface ListDescriptorVersionsResponse {
  versions: DescriptorVersionInfo[];
  active_version: string;
}

function createBaseEndpoint(): Endpoint {
  return { address: "", port: 0, weight: 0 };
}

export const Endpoint: MessageFns<Endpoint> = {
  encode(message: Endpoint, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.port !== 0) {
      writer.uint32(16).uint32(message.port);
    }
    if (message.weight !== 0) {
      writer.uint32(24).uint32(message.weight);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Endpoint {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEndpoint();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.port = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.weight = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Endpoint {
    return {
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      port: isSet(object.port) ? globalThis.Number(object.port) : 0,
      weight: isSet(object.weight) ? globalThis.Number(object.weight) : 0,
    };
  },

  toJSON(message: Endpoint): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.port !== 0) {
      obj.port = Math.round(message.port);
    }
    if (message.weight !== 0) {
      obj.weight = Math.round(message.weight);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Endpoint>, I>>(base?: I): Endpoint {
    return Endpoint.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Endpoint>, I>>(object: I): Endpoint {
    const message = createBaseEndpoint();
    message.address = object.address ?? "";
    message.port = object.port ?? 0;
    message.weight = object.weight ?? 0;
    return message;
  },
};

function createBaseLoadBalancer(): LoadBalancer {
  return { type: 0, endpoints: [] };
}

export const LoadBalancer: MessageFns<LoadBalancer> = {
  encode(message: LoadBalancer, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    for (const v of message.endpoints) {
      Endpoint.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LoadBalancer {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLoadBalancer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.endpoints.push(Endpoint.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LoadBalancer {
    return {
      type: isSet(object.type) ? balanceTypeFromJSON(object.type) : 0,
      endpoints: globalThis.Array.isArray(object?.parameters)
        ? object.parameters.map((e: any) => Endpoint.fromJSON(e))
        : [],
    };
  },

  toJSON(message: LoadBalancer): unknown {
    const obj: any = {};
    if (message.type !== 0) {
      obj.type = balanceTypeToJSON(message.type);
    }
    if (message.endpoints?.length) {
      obj.parameters = message.endpoints.map((e) => Endpoint.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LoadBalancer>, I>>(base?: I): LoadBalancer {
    return LoadBalancer.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LoadBalancer>, I>>(object: I): LoadBalancer {
    const message = createBaseLoadBalancer();
    message.type = object.type ?? 0;
    message.endpoints = object.endpoints?.map((e) => Endpoint.fromPartial(e)) || [];
    return message;
  },
};

function createBaseHealthCheckConfig(): HealthCheckConfig {
  return {
    grpc_method: "",
    http_path: "",
    interval_seconds: 0,
    timeout_seconds: 0,
    healthy_threshold: 0,
    unhealthy_threshold: 0,
    enabled: false,
  };
}

export const HealthCheckConfig: MessageFns<HealthCheckConfig> = {
  encode(message: HealthCheckConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.grpc_method !== "") {
      writer.uint32(10).string(message.grpc_method);
    }
    if (message.http_path !== "") {
      writer.uint32(18).string(message.http_path);
    }
    if (message.interval_seconds !== 0) {
      writer.uint32(24).uint32(message.interval_seconds);
    }
    if (message.timeout_seconds !== 0) {
      writer.uint32(32).uint32(message.timeout_seconds);
    }
    if (message.healthy_threshold !== 0) {
      writer.uint32(40).uint32(message.healthy_threshold);
    }
    if (message.unhealthy_threshold !== 0) {
      writer.uint32(48).uint32(message.unhealthy_threshold);
    }
    if (message.enabled !== false) {
      writer.uint32(56).bool(message.enabled);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthCheckConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthCheckConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.grpc_method = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.http_path = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.interval_seconds = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.timeout_seconds = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.healthy_threshold = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.unhealthy_threshold = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HealthCheckConfig {
    return {
      grpc_method: isSet(object.grpc_method) ? globalThis.String(object.grpc_method) : "",
      http_path: isSet(object.http_path) ? globalThis.String(object.http_path) : "",
      interval_seconds: isSet(object.interval_seconds) ? globalThis.Number(object.interval_seconds) : 0,
      timeout_seconds: isSet(object.timeout_seconds) ? globalThis.Number(object.timeout_seconds) : 0,
      healthy_threshold: isSet(object.healthy_threshold) ? globalThis.Number(object.healthy_threshold) : 0,
      unhealthy_threshold: isSet(object.unhealthy_threshold) ? globalThis.Number(object.unhealthy_threshold) : 0,
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
    };
  },

  toJSON(message: HealthCheckConfig): unknown {
    const obj: any = {};
    if (message.grpc_method !== "") {
      obj.grpc_method = message.grpc_method;
    }
    if (message.http_path !== "") {
      obj.http_path = message.http_path;
    }
    if (message.interval_seconds !== 0) {
      obj.interval_seconds = Math.round(message.interval_seconds);
    }
    if (message.timeout_seconds !== 0) {
      obj.timeout_seconds = Math.round(message.timeout_seconds);
    }
    if (message.healthy_threshold !== 0) {
      obj.healthy_threshold = Math.round(message.healthy_threshold);
    }
    if (message.unhealthy_threshold !== 0) {
      obj.unhealthy_threshold = Math.round(message.unhealthy_threshold);
    }
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HealthCheckConfig>, I>>(base?: I): HealthCheckConfig {
    return HealthCheckConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HealthCheckConfig>, I>>(object: I): HealthCheckConfig {
    const message = createBaseHealthCheckConfig();
    message.grpc_method = object.grpc_method ?? "";
    message.http_path = object.http_path ?? "";
    message.interval_seconds = object.interval_seconds ?? 0;
    message.timeout_seconds = object.timeout_seconds ?? 0;
    message.healthy_threshold = object.healthy_threshold ?? 0;
    message.unhealthy_threshold = object.unhealthy_threshold ?? 0;
    message.enabled = object.enabled ?? false;
    return message;
  },
};

function createBaseServiceCorsConfig(): ServiceCorsConfig {
  return {
    enabled: false,
    allow_origins: [],
    allow_methods: [],
    allow_headers: [],
    expose_headers: [],
    allow_credentials: false,
    max_age_secs: 0,
  };
}

export const ServiceCorsConfig: MessageFns<ServiceCorsConfig> = {
  encode(message: ServiceCorsConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    for (const v of message.allow_origins) {
      writer.uint32(18).string(v!);
    }
    for (const v of message.allow_methods) {
      writer.uint32(26).string(v!);
    }
    for (const v of message.allow_headers) {
      writer.uint32(34).string(v!);
    }
    for (const v of message.expose_headers) {
      writer.uint32(42).string(v!);
    }
    if (message.allow_credentials !== false) {
      writer.uint32(48).bool(message.allow_credentials);
    }
    if (message.max_age_secs !== 0) {
      writer.uint32(56).uint32(message.max_age_secs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServiceCorsConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServiceCorsConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.allow_origins.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.allow_methods.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.allow_headers.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.expose_headers.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.allow_credentials = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.max_age_secs = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServiceCorsConfig {
    return {
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      allow_origins: globalThis.Array.isArray(object?.allow_origins)
        ? object.allow_origins.map((e: any) => globalThis.String(e))
        : [],
      allow_methods: globalThis.Array.isArray(object?.allow_methods)
        ? object.allow_methods.map((e: any) => globalThis.String(e))
        : [],
      allow_headers: globalThis.Array.isArray(object?.allow_headers)
        ? object.allow_headers.map((e: any) => globalThis.String(e))
        : [],
      expose_headers: globalThis.Array.isArray(object?.expose_headers)
        ? object.expose_headers.map((e: any) => globalThis.String(e))
        : [],
      allow_credentials: isSet(object.allow_credentials) ? globalThis.Boolean(object.allow_credentials) : false,
      max_age_secs: isSet(object.max_age_secs) ? globalThis.Number(object.max_age_secs) : 0,
    };
  },

  toJSON(message: ServiceCorsConfig): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.allow_origins?.length) {
      obj.allow_origins = message.allow_origins;
    }
    if (message.allow_methods?.length) {
      obj.allow_methods = message.allow_methods;
    }
    if (message.allow_headers?.length) {
      obj.allow_headers = message.allow_headers;
    }
    if (message.expose_headers?.length) {
      obj.expose_headers = message.expose_headers;
    }
    if (message.allow_credentials !== false) {
      obj.allow_credentials = message.allow_credentials;
    }
    if (message.max_age_secs !== 0) {
      obj.max_age_secs = Math.round(message.max_age_secs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServiceCorsConfig>, I>>(base?: I): ServiceCorsConfig {
    return ServiceCorsConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServiceCorsConfig>, I>>(object: I): ServiceCorsConfig {
    const message = createBaseServiceCorsConfig();
    message.enabled = object.enabled ?? false;
    message.allow_origins = object.allow_origins?.map((e) => e) || [];
    message.allow_methods = object.allow_methods?.map((e) => e) || [];
    message.allow_headers = object.allow_headers?.map((e) => e) || [];
    message.expose_headers = object.expose_headers?.map((e) => e) || [];
    message.allow_credentials = object.allow_credentials ?? false;
    message.max_age_secs = object.max_age_secs ?? 0;
    return message;
  },
};

function createBaseServiceRiskRuleConfig(): ServiceRiskRuleConfig {
  return {
    name: "",
    enabled: false,
    path_prefixes: [],
    countries: [],
    proxy: undefined,
    tor: undefined,
    datacenter: undefined,
    min_bot_score: undefined,
    max_bot_score: undefined,
    action: "",
  };
}

export const ServiceRiskRuleConfig: MessageFns<ServiceRiskRuleConfig> = {
  encode(message: ServiceRiskRuleConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.enabled !== false) {
      writer.uint32(16).bool(message.enabled);
    }
    for (const v of message.path_prefixes) {
      writer.uint32(26).string(v!);
    }
    for (const v of message.countries) {
      writer.uint32(34).string(v!);
    }
    if (message.proxy !== undefined) {
      writer.uint32(40).bool(message.proxy);
    }
    if (message.tor !== undefined) {
      writer.uint32(48).bool(message.tor);
    }
    if (message.datacenter !== undefined) {
      writer.uint32(56).bool(message.datacenter);
    }
    if (message.min_bot_score !== undefined) {
      writer.uint32(64).uint32(message.min_bot_score);
    }
    if (message.max_bot_score !== undefined) {
      writer.uint32(72).uint32(message.max_bot_score);
    }
    if (message.action !== "") {
      writer.uint32(82).string(message.action);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServiceRiskRuleConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServiceRiskRuleConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.path_prefixes.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.countries.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.proxy = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.tor = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.datacenter = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.min_bot_score = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.max_bot_score = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.action = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServiceRiskRuleConfig {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      path_prefixes: globalThis.Array.isArray(object?.path_prefixes)
        ? object.path_prefixes.map((e: any) => globalThis.String(e))
        : [],
      countries: globalThis.Array.isArray(object?.countries)
        ? object.countries.map((e: any) => globalThis.String(e))
        : [],
      proxy: isSet(object.proxy) ? globalThis.Boolean(object.proxy) : undefined,
      tor: isSet(object.tor) ? globalThis.Boolean(object.tor) : undefined,
      datacenter: isSet(object.datacenter) ? globalThis.Boolean(object.datacenter) : undefined,
      min_bot_score: isSet(object.min_bot_score) ? globalThis.Number(object.min_bot_score) : undefined,
      max_bot_score: isSet(object.max_bot_score) ? globalThis.Number(object.max_bot_score) : undefined,
      action: isSet(object.action) ? globalThis.String(object.action) : "",
    };
  },

  toJSON(message: ServiceRiskRuleConfig): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.path_prefixes?.length) {
      obj.path_prefixes = message.path_prefixes;
    }
    if (message.countries?.length) {
      obj.countries = message.countries;
    }
    if (message.proxy !== undefined) {
      obj.proxy = message.proxy;
    }
    if (message.tor !== undefined) {
      obj.tor = message.tor;
    }
    if (message.datacenter !== undefined) {
      obj.datacenter = message.datacenter;
    }
    if (message.min_bot_score !== undefined) {
      obj.min_bot_score = Math.round(message.min_bot_score);
    }
    if (message.max_bot_score !== undefined) {
      obj.max_bot_score = Math.round(message.max_bot_score);
    }
    if (message.action !== "") {
      obj.action = message.action;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServiceRiskRuleConfig>, I>>(base?: I): ServiceRiskRuleConfig {
    return ServiceRiskRuleConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServiceRiskRuleConfig>, I>>(object: I): ServiceRiskRuleConfig {
    const message = createBaseServiceRiskRuleConfig();
    message.name = object.name ?? "";
    message.enabled = object.enabled ?? false;
    message.path_prefixes = object.path_prefixes?.map((e) => e) || [];
    message.countries = object.countries?.map((e) => e) || [];
    message.proxy = object.proxy ?? undefined;
    message.tor = object.tor ?? undefined;
    message.datacenter = object.datacenter ?? undefined;
    message.min_bot_score = object.min_bot_score ?? undefined;
    message.max_bot_score = object.max_bot_score ?? undefined;
    message.action = object.action ?? "";
    return message;
  },
};

function createBaseServiceRiskConfig(): ServiceRiskConfig {
  return {
    enabled: false,
    mode: "",
    default_action: "",
    challenge_paths: [],
    block_paths: [],
    observe_only_paths: [],
    high_risk_countries: [],
    challenge_proxy_traffic: false,
    block_datacenter_traffic: false,
    allow_tor_exit_nodes: false,
    bot_score_threshold: 0,
    proxy_score_threshold: 0,
    action_overrides: [],
  };
}

export const ServiceRiskConfig: MessageFns<ServiceRiskConfig> = {
  encode(message: ServiceRiskConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    if (message.mode !== "") {
      writer.uint32(18).string(message.mode);
    }
    if (message.default_action !== "") {
      writer.uint32(26).string(message.default_action);
    }
    for (const v of message.challenge_paths) {
      writer.uint32(34).string(v!);
    }
    for (const v of message.block_paths) {
      writer.uint32(42).string(v!);
    }
    for (const v of message.observe_only_paths) {
      writer.uint32(50).string(v!);
    }
    for (const v of message.high_risk_countries) {
      writer.uint32(58).string(v!);
    }
    if (message.challenge_proxy_traffic !== false) {
      writer.uint32(64).bool(message.challenge_proxy_traffic);
    }
    if (message.block_datacenter_traffic !== false) {
      writer.uint32(72).bool(message.block_datacenter_traffic);
    }
    if (message.allow_tor_exit_nodes !== false) {
      writer.uint32(80).bool(message.allow_tor_exit_nodes);
    }
    if (message.bot_score_threshold !== 0) {
      writer.uint32(88).uint32(message.bot_score_threshold);
    }
    if (message.proxy_score_threshold !== 0) {
      writer.uint32(96).uint32(message.proxy_score_threshold);
    }
    for (const v of message.action_overrides) {
      ServiceRiskRuleConfig.encode(v!, writer.uint32(106).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServiceRiskConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServiceRiskConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.mode = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.default_action = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.challenge_paths.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.block_paths.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.observe_only_paths.push(reader.string());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.high_risk_countries.push(reader.string());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.challenge_proxy_traffic = reader.bool();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.block_datacenter_traffic = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.allow_tor_exit_nodes = reader.bool();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.bot_score_threshold = reader.uint32();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.proxy_score_threshold = reader.uint32();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.action_overrides.push(ServiceRiskRuleConfig.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServiceRiskConfig {
    return {
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      mode: isSet(object.mode) ? globalThis.String(object.mode) : "",
      default_action: isSet(object.default_action) ? globalThis.String(object.default_action) : "",
      challenge_paths: globalThis.Array.isArray(object?.challenge_paths)
        ? object.challenge_paths.map((e: any) => globalThis.String(e))
        : [],
      block_paths: globalThis.Array.isArray(object?.block_paths)
        ? object.block_paths.map((e: any) => globalThis.String(e))
        : [],
      observe_only_paths: globalThis.Array.isArray(object?.observe_only_paths)
        ? object.observe_only_paths.map((e: any) => globalThis.String(e))
        : [],
      high_risk_countries: globalThis.Array.isArray(object?.high_risk_countries)
        ? object.high_risk_countries.map((e: any) => globalThis.String(e))
        : [],
      challenge_proxy_traffic: isSet(object.challenge_proxy_traffic)
        ? globalThis.Boolean(object.challenge_proxy_traffic)
        : false,
      block_datacenter_traffic: isSet(object.block_datacenter_traffic)
        ? globalThis.Boolean(object.block_datacenter_traffic)
        : false,
      allow_tor_exit_nodes: isSet(object.allow_tor_exit_nodes)
        ? globalThis.Boolean(object.allow_tor_exit_nodes)
        : false,
      bot_score_threshold: isSet(object.bot_score_threshold) ? globalThis.Number(object.bot_score_threshold) : 0,
      proxy_score_threshold: isSet(object.proxy_score_threshold) ? globalThis.Number(object.proxy_score_threshold) : 0,
      action_overrides: globalThis.Array.isArray(object?.action_overrides)
        ? object.action_overrides.map((e: any) => ServiceRiskRuleConfig.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ServiceRiskConfig): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.mode !== "") {
      obj.mode = message.mode;
    }
    if (message.default_action !== "") {
      obj.default_action = message.default_action;
    }
    if (message.challenge_paths?.length) {
      obj.challenge_paths = message.challenge_paths;
    }
    if (message.block_paths?.length) {
      obj.block_paths = message.block_paths;
    }
    if (message.observe_only_paths?.length) {
      obj.observe_only_paths = message.observe_only_paths;
    }
    if (message.high_risk_countries?.length) {
      obj.high_risk_countries = message.high_risk_countries;
    }
    if (message.challenge_proxy_traffic !== false) {
      obj.challenge_proxy_traffic = message.challenge_proxy_traffic;
    }
    if (message.block_datacenter_traffic !== false) {
      obj.block_datacenter_traffic = message.block_datacenter_traffic;
    }
    if (message.allow_tor_exit_nodes !== false) {
      obj.allow_tor_exit_nodes = message.allow_tor_exit_nodes;
    }
    if (message.bot_score_threshold !== 0) {
      obj.bot_score_threshold = Math.round(message.bot_score_threshold);
    }
    if (message.proxy_score_threshold !== 0) {
      obj.proxy_score_threshold = Math.round(message.proxy_score_threshold);
    }
    if (message.action_overrides?.length) {
      obj.action_overrides = message.action_overrides.map((e) => ServiceRiskRuleConfig.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServiceRiskConfig>, I>>(base?: I): ServiceRiskConfig {
    return ServiceRiskConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServiceRiskConfig>, I>>(object: I): ServiceRiskConfig {
    const message = createBaseServiceRiskConfig();
    message.enabled = object.enabled ?? false;
    message.mode = object.mode ?? "";
    message.default_action = object.default_action ?? "";
    message.challenge_paths = object.challenge_paths?.map((e) => e) || [];
    message.block_paths = object.block_paths?.map((e) => e) || [];
    message.observe_only_paths = object.observe_only_paths?.map((e) => e) || [];
    message.high_risk_countries = object.high_risk_countries?.map((e) => e) || [];
    message.challenge_proxy_traffic = object.challenge_proxy_traffic ?? false;
    message.block_datacenter_traffic = object.block_datacenter_traffic ?? false;
    message.allow_tor_exit_nodes = object.allow_tor_exit_nodes ?? false;
    message.bot_score_threshold = object.bot_score_threshold ?? 0;
    message.proxy_score_threshold = object.proxy_score_threshold ?? 0;
    message.action_overrides = object.action_overrides?.map((e) => ServiceRiskRuleConfig.fromPartial(e)) || [];
    return message;
  },
};

function createBaseServiceTurnstileConfig(): ServiceTurnstileConfig {
  return {
    enabled: false,
    required_paths: [],
    skip_paths: [],
    expected_action: "",
    expected_hostname: "",
    enforce_on_risk_challenge: false,
  };
}

export const ServiceTurnstileConfig: MessageFns<ServiceTurnstileConfig> = {
  encode(message: ServiceTurnstileConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    for (const v of message.required_paths) {
      writer.uint32(18).string(v!);
    }
    for (const v of message.skip_paths) {
      writer.uint32(26).string(v!);
    }
    if (message.expected_action !== "") {
      writer.uint32(34).string(message.expected_action);
    }
    if (message.expected_hostname !== "") {
      writer.uint32(42).string(message.expected_hostname);
    }
    if (message.enforce_on_risk_challenge !== false) {
      writer.uint32(48).bool(message.enforce_on_risk_challenge);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServiceTurnstileConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServiceTurnstileConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.required_paths.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.skip_paths.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.expected_action = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.expected_hostname = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.enforce_on_risk_challenge = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServiceTurnstileConfig {
    return {
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      required_paths: globalThis.Array.isArray(object?.required_paths)
        ? object.required_paths.map((e: any) => globalThis.String(e))
        : [],
      skip_paths: globalThis.Array.isArray(object?.skip_paths)
        ? object.skip_paths.map((e: any) => globalThis.String(e))
        : [],
      expected_action: isSet(object.expected_action) ? globalThis.String(object.expected_action) : "",
      expected_hostname: isSet(object.expected_hostname) ? globalThis.String(object.expected_hostname) : "",
      enforce_on_risk_challenge: isSet(object.enforce_on_risk_challenge)
        ? globalThis.Boolean(object.enforce_on_risk_challenge)
        : false,
    };
  },

  toJSON(message: ServiceTurnstileConfig): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.required_paths?.length) {
      obj.required_paths = message.required_paths;
    }
    if (message.skip_paths?.length) {
      obj.skip_paths = message.skip_paths;
    }
    if (message.expected_action !== "") {
      obj.expected_action = message.expected_action;
    }
    if (message.expected_hostname !== "") {
      obj.expected_hostname = message.expected_hostname;
    }
    if (message.enforce_on_risk_challenge !== false) {
      obj.enforce_on_risk_challenge = message.enforce_on_risk_challenge;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServiceTurnstileConfig>, I>>(base?: I): ServiceTurnstileConfig {
    return ServiceTurnstileConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServiceTurnstileConfig>, I>>(object: I): ServiceTurnstileConfig {
    const message = createBaseServiceTurnstileConfig();
    message.enabled = object.enabled ?? false;
    message.required_paths = object.required_paths?.map((e) => e) || [];
    message.skip_paths = object.skip_paths?.map((e) => e) || [];
    message.expected_action = object.expected_action ?? "";
    message.expected_hostname = object.expected_hostname ?? "";
    message.enforce_on_risk_challenge = object.enforce_on_risk_challenge ?? false;
    return message;
  },
};

function createBaseAiBodyFieldMap(): AiBodyFieldMap {
  return {
    messages_path: "",
    role_field: "",
    content_field: "",
    user_role_value: "",
    prompt_path: "",
    model_path: "",
    max_tokens_path: "",
  };
}

export const AiBodyFieldMap: MessageFns<AiBodyFieldMap> = {
  encode(message: AiBodyFieldMap, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.messages_path !== "") {
      writer.uint32(10).string(message.messages_path);
    }
    if (message.role_field !== "") {
      writer.uint32(18).string(message.role_field);
    }
    if (message.content_field !== "") {
      writer.uint32(26).string(message.content_field);
    }
    if (message.user_role_value !== "") {
      writer.uint32(34).string(message.user_role_value);
    }
    if (message.prompt_path !== "") {
      writer.uint32(42).string(message.prompt_path);
    }
    if (message.model_path !== "") {
      writer.uint32(50).string(message.model_path);
    }
    if (message.max_tokens_path !== "") {
      writer.uint32(58).string(message.max_tokens_path);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AiBodyFieldMap {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAiBodyFieldMap();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.messages_path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.role_field = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.content_field = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.user_role_value = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.prompt_path = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.model_path = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.max_tokens_path = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AiBodyFieldMap {
    return {
      messages_path: isSet(object.messages_path) ? globalThis.String(object.messages_path) : "",
      role_field: isSet(object.role_field) ? globalThis.String(object.role_field) : "",
      content_field: isSet(object.content_field) ? globalThis.String(object.content_field) : "",
      user_role_value: isSet(object.user_role_value) ? globalThis.String(object.user_role_value) : "",
      prompt_path: isSet(object.prompt_path) ? globalThis.String(object.prompt_path) : "",
      model_path: isSet(object.model_path) ? globalThis.String(object.model_path) : "",
      max_tokens_path: isSet(object.max_tokens_path) ? globalThis.String(object.max_tokens_path) : "",
    };
  },

  toJSON(message: AiBodyFieldMap): unknown {
    const obj: any = {};
    if (message.messages_path !== "") {
      obj.messages_path = message.messages_path;
    }
    if (message.role_field !== "") {
      obj.role_field = message.role_field;
    }
    if (message.content_field !== "") {
      obj.content_field = message.content_field;
    }
    if (message.user_role_value !== "") {
      obj.user_role_value = message.user_role_value;
    }
    if (message.prompt_path !== "") {
      obj.prompt_path = message.prompt_path;
    }
    if (message.model_path !== "") {
      obj.model_path = message.model_path;
    }
    if (message.max_tokens_path !== "") {
      obj.max_tokens_path = message.max_tokens_path;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AiBodyFieldMap>, I>>(base?: I): AiBodyFieldMap {
    return AiBodyFieldMap.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AiBodyFieldMap>, I>>(object: I): AiBodyFieldMap {
    const message = createBaseAiBodyFieldMap();
    message.messages_path = object.messages_path ?? "";
    message.role_field = object.role_field ?? "";
    message.content_field = object.content_field ?? "";
    message.user_role_value = object.user_role_value ?? "";
    message.prompt_path = object.prompt_path ?? "";
    message.model_path = object.model_path ?? "";
    message.max_tokens_path = object.max_tokens_path ?? "";
    return message;
  },
};

function createBaseServiceAiGuardConfig(): ServiceAiGuardConfig {
  return {
    enabled: false,
    mode: "",
    include_paths: [],
    request_body_max_bytes: 0,
    max_input_tokens: 0,
    max_output_tokens: 0,
    max_context_tokens: 0,
    history_policy: "",
    daily_token_quota: 0,
    daily_request_quota: 0,
    minute_request_quota: 0,
    allow_free_chat: false,
    allowed_topics: [],
    deny_keywords: [],
    enable_audit: false,
    classifier_type: "",
    llm_endpoint: "",
    llm_model: "",
    llm_system_prompt: "",
    llm_timeout_ms: 0,
    llm_confidence_threshold: 0,
    body_map: undefined,
    quota_window_secs: 0,
    business_description: "",
    valid_intent_examples: [],
    invalid_intent_examples: [],
    endpoint_overrides: [],
  };
}

export const ServiceAiGuardConfig: MessageFns<ServiceAiGuardConfig> = {
  encode(message: ServiceAiGuardConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    if (message.mode !== "") {
      writer.uint32(18).string(message.mode);
    }
    for (const v of message.include_paths) {
      writer.uint32(26).string(v!);
    }
    if (message.request_body_max_bytes !== 0) {
      writer.uint32(32).uint32(message.request_body_max_bytes);
    }
    if (message.max_input_tokens !== 0) {
      writer.uint32(40).uint32(message.max_input_tokens);
    }
    if (message.max_output_tokens !== 0) {
      writer.uint32(48).uint32(message.max_output_tokens);
    }
    if (message.max_context_tokens !== 0) {
      writer.uint32(56).uint32(message.max_context_tokens);
    }
    if (message.history_policy !== "") {
      writer.uint32(66).string(message.history_policy);
    }
    if (message.daily_token_quota !== 0) {
      writer.uint32(72).uint32(message.daily_token_quota);
    }
    if (message.daily_request_quota !== 0) {
      writer.uint32(80).uint32(message.daily_request_quota);
    }
    if (message.minute_request_quota !== 0) {
      writer.uint32(88).uint32(message.minute_request_quota);
    }
    if (message.allow_free_chat !== false) {
      writer.uint32(96).bool(message.allow_free_chat);
    }
    for (const v of message.allowed_topics) {
      writer.uint32(106).string(v!);
    }
    for (const v of message.deny_keywords) {
      writer.uint32(114).string(v!);
    }
    if (message.enable_audit !== false) {
      writer.uint32(120).bool(message.enable_audit);
    }
    if (message.classifier_type !== "") {
      writer.uint32(130).string(message.classifier_type);
    }
    if (message.llm_endpoint !== "") {
      writer.uint32(138).string(message.llm_endpoint);
    }
    if (message.llm_model !== "") {
      writer.uint32(146).string(message.llm_model);
    }
    if (message.llm_system_prompt !== "") {
      writer.uint32(154).string(message.llm_system_prompt);
    }
    if (message.llm_timeout_ms !== 0) {
      writer.uint32(160).uint32(message.llm_timeout_ms);
    }
    if (message.llm_confidence_threshold !== 0) {
      writer.uint32(173).float(message.llm_confidence_threshold);
    }
    if (message.body_map !== undefined) {
      AiBodyFieldMap.encode(message.body_map, writer.uint32(178).fork()).join();
    }
    if (message.quota_window_secs !== 0) {
      writer.uint32(184).uint32(message.quota_window_secs);
    }
    if (message.business_description !== "") {
      writer.uint32(194).string(message.business_description);
    }
    for (const v of message.valid_intent_examples) {
      writer.uint32(202).string(v!);
    }
    for (const v of message.invalid_intent_examples) {
      writer.uint32(210).string(v!);
    }
    for (const v of message.endpoint_overrides) {
      AiGuardEndpointConfig.encode(v!, writer.uint32(218).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServiceAiGuardConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServiceAiGuardConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.mode = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.include_paths.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.request_body_max_bytes = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.max_input_tokens = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.max_output_tokens = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.max_context_tokens = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.history_policy = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.daily_token_quota = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.daily_request_quota = reader.uint32();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.minute_request_quota = reader.uint32();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.allow_free_chat = reader.bool();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.allowed_topics.push(reader.string());
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.deny_keywords.push(reader.string());
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.enable_audit = reader.bool();
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.classifier_type = reader.string();
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.llm_endpoint = reader.string();
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.llm_model = reader.string();
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.llm_system_prompt = reader.string();
          continue;
        }
        case 20: {
          if (tag !== 160) {
            break;
          }

          message.llm_timeout_ms = reader.uint32();
          continue;
        }
        case 21: {
          if (tag !== 173) {
            break;
          }

          message.llm_confidence_threshold = reader.float();
          continue;
        }
        case 22: {
          if (tag !== 178) {
            break;
          }

          message.body_map = AiBodyFieldMap.decode(reader, reader.uint32());
          continue;
        }
        case 23: {
          if (tag !== 184) {
            break;
          }

          message.quota_window_secs = reader.uint32();
          continue;
        }
        case 24: {
          if (tag !== 194) {
            break;
          }

          message.business_description = reader.string();
          continue;
        }
        case 25: {
          if (tag !== 202) {
            break;
          }

          message.valid_intent_examples.push(reader.string());
          continue;
        }
        case 26: {
          if (tag !== 210) {
            break;
          }

          message.invalid_intent_examples.push(reader.string());
          continue;
        }
        case 27: {
          if (tag !== 218) {
            break;
          }

          message.endpoint_overrides.push(AiGuardEndpointConfig.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServiceAiGuardConfig {
    return {
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      mode: isSet(object.mode) ? globalThis.String(object.mode) : "",
      include_paths: globalThis.Array.isArray(object?.include_paths)
        ? object.include_paths.map((e: any) => globalThis.String(e))
        : [],
      request_body_max_bytes: isSet(object.request_body_max_bytes)
        ? globalThis.Number(object.request_body_max_bytes)
        : 0,
      max_input_tokens: isSet(object.max_input_tokens) ? globalThis.Number(object.max_input_tokens) : 0,
      max_output_tokens: isSet(object.max_output_tokens) ? globalThis.Number(object.max_output_tokens) : 0,
      max_context_tokens: isSet(object.max_context_tokens) ? globalThis.Number(object.max_context_tokens) : 0,
      history_policy: isSet(object.history_policy) ? globalThis.String(object.history_policy) : "",
      daily_token_quota: isSet(object.daily_token_quota) ? globalThis.Number(object.daily_token_quota) : 0,
      daily_request_quota: isSet(object.daily_request_quota) ? globalThis.Number(object.daily_request_quota) : 0,
      minute_request_quota: isSet(object.minute_request_quota) ? globalThis.Number(object.minute_request_quota) : 0,
      allow_free_chat: isSet(object.allow_free_chat) ? globalThis.Boolean(object.allow_free_chat) : false,
      allowed_topics: globalThis.Array.isArray(object?.allowed_topics)
        ? object.allowed_topics.map((e: any) => globalThis.String(e))
        : [],
      deny_keywords: globalThis.Array.isArray(object?.deny_keywords)
        ? object.deny_keywords.map((e: any) => globalThis.String(e))
        : [],
      enable_audit: isSet(object.enable_audit) ? globalThis.Boolean(object.enable_audit) : false,
      classifier_type: isSet(object.classifier_type) ? globalThis.String(object.classifier_type) : "",
      llm_endpoint: isSet(object.llm_endpoint) ? globalThis.String(object.llm_endpoint) : "",
      llm_model: isSet(object.llm_model) ? globalThis.String(object.llm_model) : "",
      llm_system_prompt: isSet(object.llm_system_prompt) ? globalThis.String(object.llm_system_prompt) : "",
      llm_timeout_ms: isSet(object.llm_timeout_ms) ? globalThis.Number(object.llm_timeout_ms) : 0,
      llm_confidence_threshold: isSet(object.llm_confidence_threshold)
        ? globalThis.Number(object.llm_confidence_threshold)
        : 0,
      body_map: isSet(object.body_map) ? AiBodyFieldMap.fromJSON(object.body_map) : undefined,
      quota_window_secs: isSet(object.quota_window_secs) ? globalThis.Number(object.quota_window_secs) : 0,
      business_description: isSet(object.business_description) ? globalThis.String(object.business_description) : "",
      valid_intent_examples: globalThis.Array.isArray(object?.valid_intent_examples)
        ? object.valid_intent_examples.map((e: any) => globalThis.String(e))
        : [],
      invalid_intent_examples: globalThis.Array.isArray(object?.invalid_intent_examples)
        ? object.invalid_intent_examples.map((e: any) => globalThis.String(e))
        : [],
      endpoint_overrides: globalThis.Array.isArray(object?.endpoint_overrides)
        ? object.endpoint_overrides.map((e: any) => AiGuardEndpointConfig.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ServiceAiGuardConfig): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.mode !== "") {
      obj.mode = message.mode;
    }
    if (message.include_paths?.length) {
      obj.include_paths = message.include_paths;
    }
    if (message.request_body_max_bytes !== 0) {
      obj.request_body_max_bytes = Math.round(message.request_body_max_bytes);
    }
    if (message.max_input_tokens !== 0) {
      obj.max_input_tokens = Math.round(message.max_input_tokens);
    }
    if (message.max_output_tokens !== 0) {
      obj.max_output_tokens = Math.round(message.max_output_tokens);
    }
    if (message.max_context_tokens !== 0) {
      obj.max_context_tokens = Math.round(message.max_context_tokens);
    }
    if (message.history_policy !== "") {
      obj.history_policy = message.history_policy;
    }
    if (message.daily_token_quota !== 0) {
      obj.daily_token_quota = Math.round(message.daily_token_quota);
    }
    if (message.daily_request_quota !== 0) {
      obj.daily_request_quota = Math.round(message.daily_request_quota);
    }
    if (message.minute_request_quota !== 0) {
      obj.minute_request_quota = Math.round(message.minute_request_quota);
    }
    if (message.allow_free_chat !== false) {
      obj.allow_free_chat = message.allow_free_chat;
    }
    if (message.allowed_topics?.length) {
      obj.allowed_topics = message.allowed_topics;
    }
    if (message.deny_keywords?.length) {
      obj.deny_keywords = message.deny_keywords;
    }
    if (message.enable_audit !== false) {
      obj.enable_audit = message.enable_audit;
    }
    if (message.classifier_type !== "") {
      obj.classifier_type = message.classifier_type;
    }
    if (message.llm_endpoint !== "") {
      obj.llm_endpoint = message.llm_endpoint;
    }
    if (message.llm_model !== "") {
      obj.llm_model = message.llm_model;
    }
    if (message.llm_system_prompt !== "") {
      obj.llm_system_prompt = message.llm_system_prompt;
    }
    if (message.llm_timeout_ms !== 0) {
      obj.llm_timeout_ms = Math.round(message.llm_timeout_ms);
    }
    if (message.llm_confidence_threshold !== 0) {
      obj.llm_confidence_threshold = message.llm_confidence_threshold;
    }
    if (message.body_map !== undefined) {
      obj.body_map = AiBodyFieldMap.toJSON(message.body_map);
    }
    if (message.quota_window_secs !== 0) {
      obj.quota_window_secs = Math.round(message.quota_window_secs);
    }
    if (message.business_description !== "") {
      obj.business_description = message.business_description;
    }
    if (message.valid_intent_examples?.length) {
      obj.valid_intent_examples = message.valid_intent_examples;
    }
    if (message.invalid_intent_examples?.length) {
      obj.invalid_intent_examples = message.invalid_intent_examples;
    }
    if (message.endpoint_overrides?.length) {
      obj.endpoint_overrides = message.endpoint_overrides.map((e) => AiGuardEndpointConfig.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServiceAiGuardConfig>, I>>(base?: I): ServiceAiGuardConfig {
    return ServiceAiGuardConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServiceAiGuardConfig>, I>>(object: I): ServiceAiGuardConfig {
    const message = createBaseServiceAiGuardConfig();
    message.enabled = object.enabled ?? false;
    message.mode = object.mode ?? "";
    message.include_paths = object.include_paths?.map((e) => e) || [];
    message.request_body_max_bytes = object.request_body_max_bytes ?? 0;
    message.max_input_tokens = object.max_input_tokens ?? 0;
    message.max_output_tokens = object.max_output_tokens ?? 0;
    message.max_context_tokens = object.max_context_tokens ?? 0;
    message.history_policy = object.history_policy ?? "";
    message.daily_token_quota = object.daily_token_quota ?? 0;
    message.daily_request_quota = object.daily_request_quota ?? 0;
    message.minute_request_quota = object.minute_request_quota ?? 0;
    message.allow_free_chat = object.allow_free_chat ?? false;
    message.allowed_topics = object.allowed_topics?.map((e) => e) || [];
    message.deny_keywords = object.deny_keywords?.map((e) => e) || [];
    message.enable_audit = object.enable_audit ?? false;
    message.classifier_type = object.classifier_type ?? "";
    message.llm_endpoint = object.llm_endpoint ?? "";
    message.llm_model = object.llm_model ?? "";
    message.llm_system_prompt = object.llm_system_prompt ?? "";
    message.llm_timeout_ms = object.llm_timeout_ms ?? 0;
    message.llm_confidence_threshold = object.llm_confidence_threshold ?? 0;
    message.body_map = (object.body_map !== undefined && object.body_map !== null)
      ? AiBodyFieldMap.fromPartial(object.body_map)
      : undefined;
    message.quota_window_secs = object.quota_window_secs ?? 0;
    message.business_description = object.business_description ?? "";
    message.valid_intent_examples = object.valid_intent_examples?.map((e) => e) || [];
    message.invalid_intent_examples = object.invalid_intent_examples?.map((e) => e) || [];
    message.endpoint_overrides = object.endpoint_overrides?.map((e) => AiGuardEndpointConfig.fromPartial(e)) || [];
    return message;
  },
};

function createBaseAiGuardEndpointConfig(): AiGuardEndpointConfig {
  return {
    endpoint_id: "",
    exact_paths: [],
    prefix_paths: [],
    pattern_paths: [],
    disabled: undefined,
    mode: "",
    request_body_max_bytes: 0,
    max_input_tokens: 0,
    max_output_tokens: 0,
    max_context_tokens: 0,
    history_policy: "",
    daily_token_quota: 0,
    daily_request_quota: 0,
    minute_request_quota: 0,
    quota_window_secs: 0,
    allow_free_chat: undefined,
    allowed_topics: [],
    deny_keywords: [],
    enable_audit: undefined,
    classifier_type: "",
    llm_endpoint: "",
    llm_model: "",
    llm_system_prompt: "",
    business_description: "",
    valid_intent_examples: [],
    invalid_intent_examples: [],
    llm_timeout_ms: 0,
    llm_confidence_threshold: 0,
    body_map: undefined,
  };
}

export const AiGuardEndpointConfig: MessageFns<AiGuardEndpointConfig> = {
  encode(message: AiGuardEndpointConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.endpoint_id !== "") {
      writer.uint32(10).string(message.endpoint_id);
    }
    for (const v of message.exact_paths) {
      writer.uint32(18).string(v!);
    }
    for (const v of message.prefix_paths) {
      writer.uint32(26).string(v!);
    }
    for (const v of message.pattern_paths) {
      writer.uint32(234).string(v!);
    }
    if (message.disabled !== undefined) {
      writer.uint32(32).bool(message.disabled);
    }
    if (message.mode !== "") {
      writer.uint32(42).string(message.mode);
    }
    if (message.request_body_max_bytes !== 0) {
      writer.uint32(48).uint32(message.request_body_max_bytes);
    }
    if (message.max_input_tokens !== 0) {
      writer.uint32(56).uint32(message.max_input_tokens);
    }
    if (message.max_output_tokens !== 0) {
      writer.uint32(64).uint32(message.max_output_tokens);
    }
    if (message.max_context_tokens !== 0) {
      writer.uint32(72).uint32(message.max_context_tokens);
    }
    if (message.history_policy !== "") {
      writer.uint32(82).string(message.history_policy);
    }
    if (message.daily_token_quota !== 0) {
      writer.uint32(88).uint32(message.daily_token_quota);
    }
    if (message.daily_request_quota !== 0) {
      writer.uint32(96).uint32(message.daily_request_quota);
    }
    if (message.minute_request_quota !== 0) {
      writer.uint32(104).uint32(message.minute_request_quota);
    }
    if (message.quota_window_secs !== 0) {
      writer.uint32(112).uint32(message.quota_window_secs);
    }
    if (message.allow_free_chat !== undefined) {
      writer.uint32(120).bool(message.allow_free_chat);
    }
    for (const v of message.allowed_topics) {
      writer.uint32(130).string(v!);
    }
    for (const v of message.deny_keywords) {
      writer.uint32(138).string(v!);
    }
    if (message.enable_audit !== undefined) {
      writer.uint32(144).bool(message.enable_audit);
    }
    if (message.classifier_type !== "") {
      writer.uint32(154).string(message.classifier_type);
    }
    if (message.llm_endpoint !== "") {
      writer.uint32(162).string(message.llm_endpoint);
    }
    if (message.llm_model !== "") {
      writer.uint32(170).string(message.llm_model);
    }
    if (message.llm_system_prompt !== "") {
      writer.uint32(178).string(message.llm_system_prompt);
    }
    if (message.business_description !== "") {
      writer.uint32(186).string(message.business_description);
    }
    for (const v of message.valid_intent_examples) {
      writer.uint32(194).string(v!);
    }
    for (const v of message.invalid_intent_examples) {
      writer.uint32(202).string(v!);
    }
    if (message.llm_timeout_ms !== 0) {
      writer.uint32(208).uint32(message.llm_timeout_ms);
    }
    if (message.llm_confidence_threshold !== 0) {
      writer.uint32(221).float(message.llm_confidence_threshold);
    }
    if (message.body_map !== undefined) {
      AiBodyFieldMap.encode(message.body_map, writer.uint32(226).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AiGuardEndpointConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAiGuardEndpointConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.endpoint_id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.exact_paths.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.prefix_paths.push(reader.string());
          continue;
        }
        case 29: {
          if (tag !== 234) {
            break;
          }

          message.pattern_paths.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.disabled = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.mode = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.request_body_max_bytes = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.max_input_tokens = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.max_output_tokens = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.max_context_tokens = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.history_policy = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.daily_token_quota = reader.uint32();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.daily_request_quota = reader.uint32();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.minute_request_quota = reader.uint32();
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.quota_window_secs = reader.uint32();
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.allow_free_chat = reader.bool();
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.allowed_topics.push(reader.string());
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.deny_keywords.push(reader.string());
          continue;
        }
        case 18: {
          if (tag !== 144) {
            break;
          }

          message.enable_audit = reader.bool();
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.classifier_type = reader.string();
          continue;
        }
        case 20: {
          if (tag !== 162) {
            break;
          }

          message.llm_endpoint = reader.string();
          continue;
        }
        case 21: {
          if (tag !== 170) {
            break;
          }

          message.llm_model = reader.string();
          continue;
        }
        case 22: {
          if (tag !== 178) {
            break;
          }

          message.llm_system_prompt = reader.string();
          continue;
        }
        case 23: {
          if (tag !== 186) {
            break;
          }

          message.business_description = reader.string();
          continue;
        }
        case 24: {
          if (tag !== 194) {
            break;
          }

          message.valid_intent_examples.push(reader.string());
          continue;
        }
        case 25: {
          if (tag !== 202) {
            break;
          }

          message.invalid_intent_examples.push(reader.string());
          continue;
        }
        case 26: {
          if (tag !== 208) {
            break;
          }

          message.llm_timeout_ms = reader.uint32();
          continue;
        }
        case 27: {
          if (tag !== 221) {
            break;
          }

          message.llm_confidence_threshold = reader.float();
          continue;
        }
        case 28: {
          if (tag !== 226) {
            break;
          }

          message.body_map = AiBodyFieldMap.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AiGuardEndpointConfig {
    return {
      endpoint_id: isSet(object.endpoint_id) ? globalThis.String(object.endpoint_id) : "",
      exact_paths: globalThis.Array.isArray(object?.exact_paths)
        ? object.exact_paths.map((e: any) => globalThis.String(e))
        : [],
      prefix_paths: globalThis.Array.isArray(object?.prefix_paths)
        ? object.prefix_paths.map((e: any) => globalThis.String(e))
        : [],
      pattern_paths: globalThis.Array.isArray(object?.pattern_paths)
        ? object.pattern_paths.map((e: any) => globalThis.String(e))
        : [],
      disabled: isSet(object.disabled) ? globalThis.Boolean(object.disabled) : undefined,
      mode: isSet(object.mode) ? globalThis.String(object.mode) : "",
      request_body_max_bytes: isSet(object.request_body_max_bytes)
        ? globalThis.Number(object.request_body_max_bytes)
        : 0,
      max_input_tokens: isSet(object.max_input_tokens) ? globalThis.Number(object.max_input_tokens) : 0,
      max_output_tokens: isSet(object.max_output_tokens) ? globalThis.Number(object.max_output_tokens) : 0,
      max_context_tokens: isSet(object.max_context_tokens) ? globalThis.Number(object.max_context_tokens) : 0,
      history_policy: isSet(object.history_policy) ? globalThis.String(object.history_policy) : "",
      daily_token_quota: isSet(object.daily_token_quota) ? globalThis.Number(object.daily_token_quota) : 0,
      daily_request_quota: isSet(object.daily_request_quota) ? globalThis.Number(object.daily_request_quota) : 0,
      minute_request_quota: isSet(object.minute_request_quota) ? globalThis.Number(object.minute_request_quota) : 0,
      quota_window_secs: isSet(object.quota_window_secs) ? globalThis.Number(object.quota_window_secs) : 0,
      allow_free_chat: isSet(object.allow_free_chat) ? globalThis.Boolean(object.allow_free_chat) : undefined,
      allowed_topics: globalThis.Array.isArray(object?.allowed_topics)
        ? object.allowed_topics.map((e: any) => globalThis.String(e))
        : [],
      deny_keywords: globalThis.Array.isArray(object?.deny_keywords)
        ? object.deny_keywords.map((e: any) => globalThis.String(e))
        : [],
      enable_audit: isSet(object.enable_audit) ? globalThis.Boolean(object.enable_audit) : undefined,
      classifier_type: isSet(object.classifier_type) ? globalThis.String(object.classifier_type) : "",
      llm_endpoint: isSet(object.llm_endpoint) ? globalThis.String(object.llm_endpoint) : "",
      llm_model: isSet(object.llm_model) ? globalThis.String(object.llm_model) : "",
      llm_system_prompt: isSet(object.llm_system_prompt) ? globalThis.String(object.llm_system_prompt) : "",
      business_description: isSet(object.business_description) ? globalThis.String(object.business_description) : "",
      valid_intent_examples: globalThis.Array.isArray(object?.valid_intent_examples)
        ? object.valid_intent_examples.map((e: any) => globalThis.String(e))
        : [],
      invalid_intent_examples: globalThis.Array.isArray(object?.invalid_intent_examples)
        ? object.invalid_intent_examples.map((e: any) => globalThis.String(e))
        : [],
      llm_timeout_ms: isSet(object.llm_timeout_ms) ? globalThis.Number(object.llm_timeout_ms) : 0,
      llm_confidence_threshold: isSet(object.llm_confidence_threshold)
        ? globalThis.Number(object.llm_confidence_threshold)
        : 0,
      body_map: isSet(object.body_map) ? AiBodyFieldMap.fromJSON(object.body_map) : undefined,
    };
  },

  toJSON(message: AiGuardEndpointConfig): unknown {
    const obj: any = {};
    if (message.endpoint_id !== "") {
      obj.endpoint_id = message.endpoint_id;
    }
    if (message.exact_paths?.length) {
      obj.exact_paths = message.exact_paths;
    }
    if (message.prefix_paths?.length) {
      obj.prefix_paths = message.prefix_paths;
    }
    if (message.pattern_paths?.length) {
      obj.pattern_paths = message.pattern_paths;
    }
    if (message.disabled !== undefined) {
      obj.disabled = message.disabled;
    }
    if (message.mode !== "") {
      obj.mode = message.mode;
    }
    if (message.request_body_max_bytes !== 0) {
      obj.request_body_max_bytes = Math.round(message.request_body_max_bytes);
    }
    if (message.max_input_tokens !== 0) {
      obj.max_input_tokens = Math.round(message.max_input_tokens);
    }
    if (message.max_output_tokens !== 0) {
      obj.max_output_tokens = Math.round(message.max_output_tokens);
    }
    if (message.max_context_tokens !== 0) {
      obj.max_context_tokens = Math.round(message.max_context_tokens);
    }
    if (message.history_policy !== "") {
      obj.history_policy = message.history_policy;
    }
    if (message.daily_token_quota !== 0) {
      obj.daily_token_quota = Math.round(message.daily_token_quota);
    }
    if (message.daily_request_quota !== 0) {
      obj.daily_request_quota = Math.round(message.daily_request_quota);
    }
    if (message.minute_request_quota !== 0) {
      obj.minute_request_quota = Math.round(message.minute_request_quota);
    }
    if (message.quota_window_secs !== 0) {
      obj.quota_window_secs = Math.round(message.quota_window_secs);
    }
    if (message.allow_free_chat !== undefined) {
      obj.allow_free_chat = message.allow_free_chat;
    }
    if (message.allowed_topics?.length) {
      obj.allowed_topics = message.allowed_topics;
    }
    if (message.deny_keywords?.length) {
      obj.deny_keywords = message.deny_keywords;
    }
    if (message.enable_audit !== undefined) {
      obj.enable_audit = message.enable_audit;
    }
    if (message.classifier_type !== "") {
      obj.classifier_type = message.classifier_type;
    }
    if (message.llm_endpoint !== "") {
      obj.llm_endpoint = message.llm_endpoint;
    }
    if (message.llm_model !== "") {
      obj.llm_model = message.llm_model;
    }
    if (message.llm_system_prompt !== "") {
      obj.llm_system_prompt = message.llm_system_prompt;
    }
    if (message.business_description !== "") {
      obj.business_description = message.business_description;
    }
    if (message.valid_intent_examples?.length) {
      obj.valid_intent_examples = message.valid_intent_examples;
    }
    if (message.invalid_intent_examples?.length) {
      obj.invalid_intent_examples = message.invalid_intent_examples;
    }
    if (message.llm_timeout_ms !== 0) {
      obj.llm_timeout_ms = Math.round(message.llm_timeout_ms);
    }
    if (message.llm_confidence_threshold !== 0) {
      obj.llm_confidence_threshold = message.llm_confidence_threshold;
    }
    if (message.body_map !== undefined) {
      obj.body_map = AiBodyFieldMap.toJSON(message.body_map);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AiGuardEndpointConfig>, I>>(base?: I): AiGuardEndpointConfig {
    return AiGuardEndpointConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AiGuardEndpointConfig>, I>>(object: I): AiGuardEndpointConfig {
    const message = createBaseAiGuardEndpointConfig();
    message.endpoint_id = object.endpoint_id ?? "";
    message.exact_paths = object.exact_paths?.map((e) => e) || [];
    message.prefix_paths = object.prefix_paths?.map((e) => e) || [];
    message.pattern_paths = object.pattern_paths?.map((e) => e) || [];
    message.disabled = object.disabled ?? undefined;
    message.mode = object.mode ?? "";
    message.request_body_max_bytes = object.request_body_max_bytes ?? 0;
    message.max_input_tokens = object.max_input_tokens ?? 0;
    message.max_output_tokens = object.max_output_tokens ?? 0;
    message.max_context_tokens = object.max_context_tokens ?? 0;
    message.history_policy = object.history_policy ?? "";
    message.daily_token_quota = object.daily_token_quota ?? 0;
    message.daily_request_quota = object.daily_request_quota ?? 0;
    message.minute_request_quota = object.minute_request_quota ?? 0;
    message.quota_window_secs = object.quota_window_secs ?? 0;
    message.allow_free_chat = object.allow_free_chat ?? undefined;
    message.allowed_topics = object.allowed_topics?.map((e) => e) || [];
    message.deny_keywords = object.deny_keywords?.map((e) => e) || [];
    message.enable_audit = object.enable_audit ?? undefined;
    message.classifier_type = object.classifier_type ?? "";
    message.llm_endpoint = object.llm_endpoint ?? "";
    message.llm_model = object.llm_model ?? "";
    message.llm_system_prompt = object.llm_system_prompt ?? "";
    message.business_description = object.business_description ?? "";
    message.valid_intent_examples = object.valid_intent_examples?.map((e) => e) || [];
    message.invalid_intent_examples = object.invalid_intent_examples?.map((e) => e) || [];
    message.llm_timeout_ms = object.llm_timeout_ms ?? 0;
    message.llm_confidence_threshold = object.llm_confidence_threshold ?? 0;
    message.body_map = (object.body_map !== undefined && object.body_map !== null)
      ? AiBodyFieldMap.fromPartial(object.body_map)
      : undefined;
    return message;
  },
};

function createBaseServiceMiddlewareConfig(): ServiceMiddlewareConfig {
  return {
    rate_limit_enabled: false,
    rate_limit_rpm: 0,
    rate_limit_user_rpm: 0,
    cors_enabled: false,
    cors: undefined,
    risk_enabled: false,
    risk: undefined,
    turnstile_enabled: false,
    turnstile: undefined,
    ai_guard_enabled: false,
    ai_guard: undefined,
    billing_enabled: false,
    billing: undefined,
  };
}

export const ServiceMiddlewareConfig: MessageFns<ServiceMiddlewareConfig> = {
  encode(message: ServiceMiddlewareConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rate_limit_enabled !== false) {
      writer.uint32(8).bool(message.rate_limit_enabled);
    }
    if (message.rate_limit_rpm !== 0) {
      writer.uint32(16).uint32(message.rate_limit_rpm);
    }
    if (message.rate_limit_user_rpm !== 0) {
      writer.uint32(24).uint32(message.rate_limit_user_rpm);
    }
    if (message.cors_enabled !== false) {
      writer.uint32(80).bool(message.cors_enabled);
    }
    if (message.cors !== undefined) {
      ServiceCorsConfig.encode(message.cors, writer.uint32(90).fork()).join();
    }
    if (message.risk_enabled !== false) {
      writer.uint32(160).bool(message.risk_enabled);
    }
    if (message.risk !== undefined) {
      ServiceRiskConfig.encode(message.risk, writer.uint32(170).fork()).join();
    }
    if (message.turnstile_enabled !== false) {
      writer.uint32(240).bool(message.turnstile_enabled);
    }
    if (message.turnstile !== undefined) {
      ServiceTurnstileConfig.encode(message.turnstile, writer.uint32(250).fork()).join();
    }
    if (message.ai_guard_enabled !== false) {
      writer.uint32(320).bool(message.ai_guard_enabled);
    }
    if (message.ai_guard !== undefined) {
      ServiceAiGuardConfig.encode(message.ai_guard, writer.uint32(330).fork()).join();
    }
    if (message.billing_enabled !== false) {
      writer.uint32(400).bool(message.billing_enabled);
    }
    if (message.billing !== undefined) {
      ServiceBillingConfig.encode(message.billing, writer.uint32(410).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServiceMiddlewareConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServiceMiddlewareConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.rate_limit_enabled = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.rate_limit_rpm = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.rate_limit_user_rpm = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.cors_enabled = reader.bool();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.cors = ServiceCorsConfig.decode(reader, reader.uint32());
          continue;
        }
        case 20: {
          if (tag !== 160) {
            break;
          }

          message.risk_enabled = reader.bool();
          continue;
        }
        case 21: {
          if (tag !== 170) {
            break;
          }

          message.risk = ServiceRiskConfig.decode(reader, reader.uint32());
          continue;
        }
        case 30: {
          if (tag !== 240) {
            break;
          }

          message.turnstile_enabled = reader.bool();
          continue;
        }
        case 31: {
          if (tag !== 250) {
            break;
          }

          message.turnstile = ServiceTurnstileConfig.decode(reader, reader.uint32());
          continue;
        }
        case 40: {
          if (tag !== 320) {
            break;
          }

          message.ai_guard_enabled = reader.bool();
          continue;
        }
        case 41: {
          if (tag !== 330) {
            break;
          }

          message.ai_guard = ServiceAiGuardConfig.decode(reader, reader.uint32());
          continue;
        }
        case 50: {
          if (tag !== 400) {
            break;
          }

          message.billing_enabled = reader.bool();
          continue;
        }
        case 51: {
          if (tag !== 410) {
            break;
          }

          message.billing = ServiceBillingConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServiceMiddlewareConfig {
    return {
      rate_limit_enabled: isSet(object.rate_limit_enabled) ? globalThis.Boolean(object.rate_limit_enabled) : false,
      rate_limit_rpm: isSet(object.rate_limit_rpm) ? globalThis.Number(object.rate_limit_rpm) : 0,
      rate_limit_user_rpm: isSet(object.rate_limit_user_rpm) ? globalThis.Number(object.rate_limit_user_rpm) : 0,
      cors_enabled: isSet(object.cors_enabled) ? globalThis.Boolean(object.cors_enabled) : false,
      cors: isSet(object.cors) ? ServiceCorsConfig.fromJSON(object.cors) : undefined,
      risk_enabled: isSet(object.risk_enabled) ? globalThis.Boolean(object.risk_enabled) : false,
      risk: isSet(object.risk) ? ServiceRiskConfig.fromJSON(object.risk) : undefined,
      turnstile_enabled: isSet(object.turnstile_enabled) ? globalThis.Boolean(object.turnstile_enabled) : false,
      turnstile: isSet(object.turnstile) ? ServiceTurnstileConfig.fromJSON(object.turnstile) : undefined,
      ai_guard_enabled: isSet(object.ai_guard_enabled) ? globalThis.Boolean(object.ai_guard_enabled) : false,
      ai_guard: isSet(object.ai_guard) ? ServiceAiGuardConfig.fromJSON(object.ai_guard) : undefined,
      billing_enabled: isSet(object.billing_enabled) ? globalThis.Boolean(object.billing_enabled) : false,
      billing: isSet(object.billing) ? ServiceBillingConfig.fromJSON(object.billing) : undefined,
    };
  },

  toJSON(message: ServiceMiddlewareConfig): unknown {
    const obj: any = {};
    if (message.rate_limit_enabled !== false) {
      obj.rate_limit_enabled = message.rate_limit_enabled;
    }
    if (message.rate_limit_rpm !== 0) {
      obj.rate_limit_rpm = Math.round(message.rate_limit_rpm);
    }
    if (message.rate_limit_user_rpm !== 0) {
      obj.rate_limit_user_rpm = Math.round(message.rate_limit_user_rpm);
    }
    if (message.cors_enabled !== false) {
      obj.cors_enabled = message.cors_enabled;
    }
    if (message.cors !== undefined) {
      obj.cors = ServiceCorsConfig.toJSON(message.cors);
    }
    if (message.risk_enabled !== false) {
      obj.risk_enabled = message.risk_enabled;
    }
    if (message.risk !== undefined) {
      obj.risk = ServiceRiskConfig.toJSON(message.risk);
    }
    if (message.turnstile_enabled !== false) {
      obj.turnstile_enabled = message.turnstile_enabled;
    }
    if (message.turnstile !== undefined) {
      obj.turnstile = ServiceTurnstileConfig.toJSON(message.turnstile);
    }
    if (message.ai_guard_enabled !== false) {
      obj.ai_guard_enabled = message.ai_guard_enabled;
    }
    if (message.ai_guard !== undefined) {
      obj.ai_guard = ServiceAiGuardConfig.toJSON(message.ai_guard);
    }
    if (message.billing_enabled !== false) {
      obj.billing_enabled = message.billing_enabled;
    }
    if (message.billing !== undefined) {
      obj.billing = ServiceBillingConfig.toJSON(message.billing);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServiceMiddlewareConfig>, I>>(base?: I): ServiceMiddlewareConfig {
    return ServiceMiddlewareConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServiceMiddlewareConfig>, I>>(object: I): ServiceMiddlewareConfig {
    const message = createBaseServiceMiddlewareConfig();
    message.rate_limit_enabled = object.rate_limit_enabled ?? false;
    message.rate_limit_rpm = object.rate_limit_rpm ?? 0;
    message.rate_limit_user_rpm = object.rate_limit_user_rpm ?? 0;
    message.cors_enabled = object.cors_enabled ?? false;
    message.cors = (object.cors !== undefined && object.cors !== null)
      ? ServiceCorsConfig.fromPartial(object.cors)
      : undefined;
    message.risk_enabled = object.risk_enabled ?? false;
    message.risk = (object.risk !== undefined && object.risk !== null)
      ? ServiceRiskConfig.fromPartial(object.risk)
      : undefined;
    message.turnstile_enabled = object.turnstile_enabled ?? false;
    message.turnstile = (object.turnstile !== undefined && object.turnstile !== null)
      ? ServiceTurnstileConfig.fromPartial(object.turnstile)
      : undefined;
    message.ai_guard_enabled = object.ai_guard_enabled ?? false;
    message.ai_guard = (object.ai_guard !== undefined && object.ai_guard !== null)
      ? ServiceAiGuardConfig.fromPartial(object.ai_guard)
      : undefined;
    message.billing_enabled = object.billing_enabled ?? false;
    message.billing = (object.billing !== undefined && object.billing !== null)
      ? ServiceBillingConfig.fromPartial(object.billing)
      : undefined;
    return message;
  },
};

function createBaseServiceInstance(): ServiceInstance {
  return {
    service_name: "",
    instance_id: "",
    lb: undefined,
    version: "",
    metadata: {},
    health_endpoint: "",
    health_check_config: undefined,
    registered_at: undefined,
    status: 0,
    weight: 0,
    tags: {},
    protocol: "",
    tls_enabled: false,
    protobuf_descriptor: new Uint8Array(0),
    middleware_config: undefined,
  };
}

export const ServiceInstance: MessageFns<ServiceInstance> = {
  encode(message: ServiceInstance, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service_name !== "") {
      writer.uint32(10).string(message.service_name);
    }
    if (message.instance_id !== "") {
      writer.uint32(18).string(message.instance_id);
    }
    if (message.lb !== undefined) {
      LoadBalancer.encode(message.lb, writer.uint32(26).fork()).join();
    }
    if (message.version !== "") {
      writer.uint32(42).string(message.version);
    }
    Object.entries(message.metadata).forEach(([key, value]) => {
      ServiceInstance_MetadataEntry.encode({ key: key as any, value }, writer.uint32(50).fork()).join();
    });
    if (message.health_endpoint !== "") {
      writer.uint32(58).string(message.health_endpoint);
    }
    if (message.health_check_config !== undefined) {
      HealthCheckConfig.encode(message.health_check_config, writer.uint32(122).fork()).join();
    }
    if (message.registered_at !== undefined) {
      Timestamp.encode(toTimestamp(message.registered_at), writer.uint32(66).fork()).join();
    }
    if (message.status !== 0) {
      writer.uint32(72).int32(message.status);
    }
    if (message.weight !== 0) {
      writer.uint32(80).uint32(message.weight);
    }
    Object.entries(message.tags).forEach(([key, value]) => {
      ServiceInstance_TagsEntry.encode({ key: key as any, value }, writer.uint32(90).fork()).join();
    });
    if (message.protocol !== "") {
      writer.uint32(98).string(message.protocol);
    }
    if (message.tls_enabled !== false) {
      writer.uint32(104).bool(message.tls_enabled);
    }
    if (message.protobuf_descriptor.length !== 0) {
      writer.uint32(114).bytes(message.protobuf_descriptor);
    }
    if (message.middleware_config !== undefined) {
      ServiceMiddlewareConfig.encode(message.middleware_config, writer.uint32(130).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServiceInstance {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServiceInstance();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service_name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.instance_id = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.lb = LoadBalancer.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          const entry6 = ServiceInstance_MetadataEntry.decode(reader, reader.uint32());
          if (entry6.value !== undefined) {
            message.metadata[entry6.key] = entry6.value;
          }
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.health_endpoint = reader.string();
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.health_check_config = HealthCheckConfig.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.registered_at = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.weight = reader.uint32();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          const entry11 = ServiceInstance_TagsEntry.decode(reader, reader.uint32());
          if (entry11.value !== undefined) {
            message.tags[entry11.key] = entry11.value;
          }
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.protocol = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.tls_enabled = reader.bool();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.protobuf_descriptor = reader.bytes();
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.middleware_config = ServiceMiddlewareConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServiceInstance {
    return {
      service_name: isSet(object.service_name) ? globalThis.String(object.service_name) : "",
      instance_id: isSet(object.instance_id) ? globalThis.String(object.instance_id) : "",
      lb: isSet(object.lb) ? LoadBalancer.fromJSON(object.lb) : undefined,
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      metadata: isObject(object.metadata)
        ? Object.entries(object.metadata).reduce<{ [key: string]: Any }>((acc, [key, value]) => {
          acc[key] = Any.fromJSON(value);
          return acc;
        }, {})
        : {},
      health_endpoint: isSet(object.health_endpoint) ? globalThis.String(object.health_endpoint) : "",
      health_check_config: isSet(object.health_check_config)
        ? HealthCheckConfig.fromJSON(object.health_check_config)
        : undefined,
      registered_at: isSet(object.registered_at) ? fromJsonTimestamp(object.registered_at) : undefined,
      status: isSet(object.status) ? serviceStatusFromJSON(object.status) : 0,
      weight: isSet(object.weight) ? globalThis.Number(object.weight) : 0,
      tags: isObject(object.tags)
        ? Object.entries(object.tags).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      protocol: isSet(object.protocol) ? globalThis.String(object.protocol) : "",
      tls_enabled: isSet(object.tls_enabled) ? globalThis.Boolean(object.tls_enabled) : false,
      protobuf_descriptor: isSet(object.protobuf_descriptor)
        ? bytesFromBase64(object.protobuf_descriptor)
        : new Uint8Array(0),
      middleware_config: isSet(object.middleware_config)
        ? ServiceMiddlewareConfig.fromJSON(object.middleware_config)
        : undefined,
    };
  },

  toJSON(message: ServiceInstance): unknown {
    const obj: any = {};
    if (message.service_name !== "") {
      obj.service_name = message.service_name;
    }
    if (message.instance_id !== "") {
      obj.instance_id = message.instance_id;
    }
    if (message.lb !== undefined) {
      obj.lb = LoadBalancer.toJSON(message.lb);
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.metadata) {
      const entries = Object.entries(message.metadata);
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = Any.toJSON(v);
        });
      }
    }
    if (message.health_endpoint !== "") {
      obj.health_endpoint = message.health_endpoint;
    }
    if (message.health_check_config !== undefined) {
      obj.health_check_config = HealthCheckConfig.toJSON(message.health_check_config);
    }
    if (message.registered_at !== undefined) {
      obj.registered_at = message.registered_at.toISOString();
    }
    if (message.status !== 0) {
      obj.status = serviceStatusToJSON(message.status);
    }
    if (message.weight !== 0) {
      obj.weight = Math.round(message.weight);
    }
    if (message.tags) {
      const entries = Object.entries(message.tags);
      if (entries.length > 0) {
        obj.tags = {};
        entries.forEach(([k, v]) => {
          obj.tags[k] = v;
        });
      }
    }
    if (message.protocol !== "") {
      obj.protocol = message.protocol;
    }
    if (message.tls_enabled !== false) {
      obj.tls_enabled = message.tls_enabled;
    }
    if (message.protobuf_descriptor.length !== 0) {
      obj.protobuf_descriptor = base64FromBytes(message.protobuf_descriptor);
    }
    if (message.middleware_config !== undefined) {
      obj.middleware_config = ServiceMiddlewareConfig.toJSON(message.middleware_config);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServiceInstance>, I>>(base?: I): ServiceInstance {
    return ServiceInstance.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServiceInstance>, I>>(object: I): ServiceInstance {
    const message = createBaseServiceInstance();
    message.service_name = object.service_name ?? "";
    message.instance_id = object.instance_id ?? "";
    message.lb = (object.lb !== undefined && object.lb !== null) ? LoadBalancer.fromPartial(object.lb) : undefined;
    message.version = object.version ?? "";
    message.metadata = Object.entries(object.metadata ?? {}).reduce<{ [key: string]: Any }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = Any.fromPartial(value);
      }
      return acc;
    }, {});
    message.health_endpoint = object.health_endpoint ?? "";
    message.health_check_config = (object.health_check_config !== undefined && object.health_check_config !== null)
      ? HealthCheckConfig.fromPartial(object.health_check_config)
      : undefined;
    message.registered_at = object.registered_at ?? undefined;
    message.status = object.status ?? 0;
    message.weight = object.weight ?? 0;
    message.tags = Object.entries(object.tags ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    message.protocol = object.protocol ?? "";
    message.tls_enabled = object.tls_enabled ?? false;
    message.protobuf_descriptor = object.protobuf_descriptor ?? new Uint8Array(0);
    message.middleware_config = (object.middleware_config !== undefined && object.middleware_config !== null)
      ? ServiceMiddlewareConfig.fromPartial(object.middleware_config)
      : undefined;
    return message;
  },
};

function createBaseServiceInstance_MetadataEntry(): ServiceInstance_MetadataEntry {
  return { key: "", value: undefined };
}

export const ServiceInstance_MetadataEntry: MessageFns<ServiceInstance_MetadataEntry> = {
  encode(message: ServiceInstance_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      Any.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServiceInstance_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServiceInstance_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = Any.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServiceInstance_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? Any.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: ServiceInstance_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = Any.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServiceInstance_MetadataEntry>, I>>(base?: I): ServiceInstance_MetadataEntry {
    return ServiceInstance_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServiceInstance_MetadataEntry>, I>>(
    object: I,
  ): ServiceInstance_MetadataEntry {
    const message = createBaseServiceInstance_MetadataEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null) ? Any.fromPartial(object.value) : undefined;
    return message;
  },
};

function createBaseServiceInstance_TagsEntry(): ServiceInstance_TagsEntry {
  return { key: "", value: "" };
}

export const ServiceInstance_TagsEntry: MessageFns<ServiceInstance_TagsEntry> = {
  encode(message: ServiceInstance_TagsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServiceInstance_TagsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServiceInstance_TagsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServiceInstance_TagsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ServiceInstance_TagsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServiceInstance_TagsEntry>, I>>(base?: I): ServiceInstance_TagsEntry {
    return ServiceInstance_TagsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServiceInstance_TagsEntry>, I>>(object: I): ServiceInstance_TagsEntry {
    const message = createBaseServiceInstance_TagsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseInitServiceRequest(): InitServiceRequest {
  return { service_name: "", description: "", protocol: "" };
}

export const InitServiceRequest: MessageFns<InitServiceRequest> = {
  encode(message: InitServiceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service_name !== "") {
      writer.uint32(10).string(message.service_name);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.protocol !== "") {
      writer.uint32(26).string(message.protocol);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InitServiceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInitServiceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service_name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.protocol = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InitServiceRequest {
    return {
      service_name: isSet(object.service_name) ? globalThis.String(object.service_name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      protocol: isSet(object.protocol) ? globalThis.String(object.protocol) : "",
    };
  },

  toJSON(message: InitServiceRequest): unknown {
    const obj: any = {};
    if (message.service_name !== "") {
      obj.service_name = message.service_name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.protocol !== "") {
      obj.protocol = message.protocol;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InitServiceRequest>, I>>(base?: I): InitServiceRequest {
    return InitServiceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InitServiceRequest>, I>>(object: I): InitServiceRequest {
    const message = createBaseInitServiceRequest();
    message.service_name = object.service_name ?? "";
    message.description = object.description ?? "";
    message.protocol = object.protocol ?? "";
    return message;
  },
};

function createBaseInitServiceResponse(): InitServiceResponse {
  return { success: false, message: "", app_id: "", app_secret: "", service_name: "" };
}

export const InitServiceResponse: MessageFns<InitServiceResponse> = {
  encode(message: InitServiceResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.app_id !== "") {
      writer.uint32(26).string(message.app_id);
    }
    if (message.app_secret !== "") {
      writer.uint32(34).string(message.app_secret);
    }
    if (message.service_name !== "") {
      writer.uint32(42).string(message.service_name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InitServiceResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInitServiceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.app_id = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.app_secret = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.service_name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InitServiceResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      app_id: isSet(object.app_id) ? globalThis.String(object.app_id) : "",
      app_secret: isSet(object.app_secret) ? globalThis.String(object.app_secret) : "",
      service_name: isSet(object.service_name) ? globalThis.String(object.service_name) : "",
    };
  },

  toJSON(message: InitServiceResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.app_id !== "") {
      obj.app_id = message.app_id;
    }
    if (message.app_secret !== "") {
      obj.app_secret = message.app_secret;
    }
    if (message.service_name !== "") {
      obj.service_name = message.service_name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InitServiceResponse>, I>>(base?: I): InitServiceResponse {
    return InitServiceResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InitServiceResponse>, I>>(object: I): InitServiceResponse {
    const message = createBaseInitServiceResponse();
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    message.app_id = object.app_id ?? "";
    message.app_secret = object.app_secret ?? "";
    message.service_name = object.service_name ?? "";
    return message;
  },
};

function createBaseRegisterServiceRequest(): RegisterServiceRequest {
  return { service: undefined, ttl: 0 };
}

export const RegisterServiceRequest: MessageFns<RegisterServiceRequest> = {
  encode(message: RegisterServiceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service !== undefined) {
      ServiceInstance.encode(message.service, writer.uint32(10).fork()).join();
    }
    if (message.ttl !== 0) {
      writer.uint32(16).uint32(message.ttl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterServiceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterServiceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service = ServiceInstance.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.ttl = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterServiceRequest {
    return {
      service: isSet(object.service) ? ServiceInstance.fromJSON(object.service) : undefined,
      ttl: isSet(object.ttl) ? globalThis.Number(object.ttl) : 0,
    };
  },

  toJSON(message: RegisterServiceRequest): unknown {
    const obj: any = {};
    if (message.service !== undefined) {
      obj.service = ServiceInstance.toJSON(message.service);
    }
    if (message.ttl !== 0) {
      obj.ttl = Math.round(message.ttl);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterServiceRequest>, I>>(base?: I): RegisterServiceRequest {
    return RegisterServiceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterServiceRequest>, I>>(object: I): RegisterServiceRequest {
    const message = createBaseRegisterServiceRequest();
    message.service = (object.service !== undefined && object.service !== null)
      ? ServiceInstance.fromPartial(object.service)
      : undefined;
    message.ttl = object.ttl ?? 0;
    return message;
  },
};

function createBaseRegisterServiceResponse(): RegisterServiceResponse {
  return { success: false, message: "", lease_id: "", instance_id: "" };
}

export const RegisterServiceResponse: MessageFns<RegisterServiceResponse> = {
  encode(message: RegisterServiceResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.lease_id !== "") {
      writer.uint32(26).string(message.lease_id);
    }
    if (message.instance_id !== "") {
      writer.uint32(34).string(message.instance_id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterServiceResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterServiceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.lease_id = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.instance_id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterServiceResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      lease_id: isSet(object.lease_id) ? globalThis.String(object.lease_id) : "",
      instance_id: isSet(object.instance_id) ? globalThis.String(object.instance_id) : "",
    };
  },

  toJSON(message: RegisterServiceResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.lease_id !== "") {
      obj.lease_id = message.lease_id;
    }
    if (message.instance_id !== "") {
      obj.instance_id = message.instance_id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterServiceResponse>, I>>(base?: I): RegisterServiceResponse {
    return RegisterServiceResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterServiceResponse>, I>>(object: I): RegisterServiceResponse {
    const message = createBaseRegisterServiceResponse();
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    message.lease_id = object.lease_id ?? "";
    message.instance_id = object.instance_id ?? "";
    return message;
  },
};

function createBaseRegisterServiceEndpointRequest(): RegisterServiceEndpointRequest {
  return {
    service_name: "",
    endpoint_id: "",
    endpoint: undefined,
    version: "",
    protocol: "",
    tls_enabled: false,
    protobuf_descriptor: new Uint8Array(0),
  };
}

export const RegisterServiceEndpointRequest: MessageFns<RegisterServiceEndpointRequest> = {
  encode(message: RegisterServiceEndpointRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service_name !== "") {
      writer.uint32(10).string(message.service_name);
    }
    if (message.endpoint_id !== "") {
      writer.uint32(18).string(message.endpoint_id);
    }
    if (message.endpoint !== undefined) {
      Endpoint.encode(message.endpoint, writer.uint32(26).fork()).join();
    }
    if (message.version !== "") {
      writer.uint32(34).string(message.version);
    }
    if (message.protocol !== "") {
      writer.uint32(42).string(message.protocol);
    }
    if (message.tls_enabled !== false) {
      writer.uint32(48).bool(message.tls_enabled);
    }
    if (message.protobuf_descriptor.length !== 0) {
      writer.uint32(58).bytes(message.protobuf_descriptor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterServiceEndpointRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterServiceEndpointRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service_name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.endpoint_id = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.endpoint = Endpoint.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.protocol = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.tls_enabled = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.protobuf_descriptor = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterServiceEndpointRequest {
    return {
      service_name: isSet(object.service_name) ? globalThis.String(object.service_name) : "",
      endpoint_id: isSet(object.endpoint_id) ? globalThis.String(object.endpoint_id) : "",
      endpoint: isSet(object.endpoint) ? Endpoint.fromJSON(object.endpoint) : undefined,
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      protocol: isSet(object.protocol) ? globalThis.String(object.protocol) : "",
      tls_enabled: isSet(object.tls_enabled) ? globalThis.Boolean(object.tls_enabled) : false,
      protobuf_descriptor: isSet(object.protobuf_descriptor)
        ? bytesFromBase64(object.protobuf_descriptor)
        : new Uint8Array(0),
    };
  },

  toJSON(message: RegisterServiceEndpointRequest): unknown {
    const obj: any = {};
    if (message.service_name !== "") {
      obj.service_name = message.service_name;
    }
    if (message.endpoint_id !== "") {
      obj.endpoint_id = message.endpoint_id;
    }
    if (message.endpoint !== undefined) {
      obj.endpoint = Endpoint.toJSON(message.endpoint);
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.protocol !== "") {
      obj.protocol = message.protocol;
    }
    if (message.tls_enabled !== false) {
      obj.tls_enabled = message.tls_enabled;
    }
    if (message.protobuf_descriptor.length !== 0) {
      obj.protobuf_descriptor = base64FromBytes(message.protobuf_descriptor);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterServiceEndpointRequest>, I>>(base?: I): RegisterServiceEndpointRequest {
    return RegisterServiceEndpointRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterServiceEndpointRequest>, I>>(
    object: I,
  ): RegisterServiceEndpointRequest {
    const message = createBaseRegisterServiceEndpointRequest();
    message.service_name = object.service_name ?? "";
    message.endpoint_id = object.endpoint_id ?? "";
    message.endpoint = (object.endpoint !== undefined && object.endpoint !== null)
      ? Endpoint.fromPartial(object.endpoint)
      : undefined;
    message.version = object.version ?? "";
    message.protocol = object.protocol ?? "";
    message.tls_enabled = object.tls_enabled ?? false;
    message.protobuf_descriptor = object.protobuf_descriptor ?? new Uint8Array(0);
    return message;
  },
};

function createBaseRegisterServiceEndpointResponse(): RegisterServiceEndpointResponse {
  return { success: false, message: "", endpoint_id: "", lease_id: "", registered_service: undefined };
}

export const RegisterServiceEndpointResponse: MessageFns<RegisterServiceEndpointResponse> = {
  encode(message: RegisterServiceEndpointResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.endpoint_id !== "") {
      writer.uint32(26).string(message.endpoint_id);
    }
    if (message.lease_id !== "") {
      writer.uint32(34).string(message.lease_id);
    }
    if (message.registered_service !== undefined) {
      ServiceInstance.encode(message.registered_service, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterServiceEndpointResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterServiceEndpointResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.endpoint_id = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.lease_id = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.registered_service = ServiceInstance.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterServiceEndpointResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      endpoint_id: isSet(object.endpoint_id) ? globalThis.String(object.endpoint_id) : "",
      lease_id: isSet(object.lease_id) ? globalThis.String(object.lease_id) : "",
      registered_service: isSet(object.registered_service)
        ? ServiceInstance.fromJSON(object.registered_service)
        : undefined,
    };
  },

  toJSON(message: RegisterServiceEndpointResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.endpoint_id !== "") {
      obj.endpoint_id = message.endpoint_id;
    }
    if (message.lease_id !== "") {
      obj.lease_id = message.lease_id;
    }
    if (message.registered_service !== undefined) {
      obj.registered_service = ServiceInstance.toJSON(message.registered_service);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterServiceEndpointResponse>, I>>(base?: I): RegisterServiceEndpointResponse {
    return RegisterServiceEndpointResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterServiceEndpointResponse>, I>>(
    object: I,
  ): RegisterServiceEndpointResponse {
    const message = createBaseRegisterServiceEndpointResponse();
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    message.endpoint_id = object.endpoint_id ?? "";
    message.lease_id = object.lease_id ?? "";
    message.registered_service = (object.registered_service !== undefined && object.registered_service !== null)
      ? ServiceInstance.fromPartial(object.registered_service)
      : undefined;
    return message;
  },
};

function createBaseDeregisterServiceEndpointRequest(): DeregisterServiceEndpointRequest {
  return { service_name: "", endpoint_id: "" };
}

export const DeregisterServiceEndpointRequest: MessageFns<DeregisterServiceEndpointRequest> = {
  encode(message: DeregisterServiceEndpointRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service_name !== "") {
      writer.uint32(10).string(message.service_name);
    }
    if (message.endpoint_id !== "") {
      writer.uint32(18).string(message.endpoint_id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeregisterServiceEndpointRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeregisterServiceEndpointRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service_name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.endpoint_id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeregisterServiceEndpointRequest {
    return {
      service_name: isSet(object.service_name) ? globalThis.String(object.service_name) : "",
      endpoint_id: isSet(object.endpoint_id) ? globalThis.String(object.endpoint_id) : "",
    };
  },

  toJSON(message: DeregisterServiceEndpointRequest): unknown {
    const obj: any = {};
    if (message.service_name !== "") {
      obj.service_name = message.service_name;
    }
    if (message.endpoint_id !== "") {
      obj.endpoint_id = message.endpoint_id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeregisterServiceEndpointRequest>, I>>(
    base?: I,
  ): DeregisterServiceEndpointRequest {
    return DeregisterServiceEndpointRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeregisterServiceEndpointRequest>, I>>(
    object: I,
  ): DeregisterServiceEndpointRequest {
    const message = createBaseDeregisterServiceEndpointRequest();
    message.service_name = object.service_name ?? "";
    message.endpoint_id = object.endpoint_id ?? "";
    return message;
  },
};

function createBaseDeregisterServiceEndpointResponse(): DeregisterServiceEndpointResponse {
  return { success: false, message: "" };
}

export const DeregisterServiceEndpointResponse: MessageFns<DeregisterServiceEndpointResponse> = {
  encode(message: DeregisterServiceEndpointResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeregisterServiceEndpointResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeregisterServiceEndpointResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeregisterServiceEndpointResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: DeregisterServiceEndpointResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeregisterServiceEndpointResponse>, I>>(
    base?: I,
  ): DeregisterServiceEndpointResponse {
    return DeregisterServiceEndpointResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeregisterServiceEndpointResponse>, I>>(
    object: I,
  ): DeregisterServiceEndpointResponse {
    const message = createBaseDeregisterServiceEndpointResponse();
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseDeregisterServiceRequest(): DeregisterServiceRequest {
  return { service_name: "", instance_id: "" };
}

export const DeregisterServiceRequest: MessageFns<DeregisterServiceRequest> = {
  encode(message: DeregisterServiceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service_name !== "") {
      writer.uint32(10).string(message.service_name);
    }
    if (message.instance_id !== "") {
      writer.uint32(18).string(message.instance_id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeregisterServiceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeregisterServiceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service_name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.instance_id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeregisterServiceRequest {
    return {
      service_name: isSet(object.service_name) ? globalThis.String(object.service_name) : "",
      instance_id: isSet(object.instance_id) ? globalThis.String(object.instance_id) : "",
    };
  },

  toJSON(message: DeregisterServiceRequest): unknown {
    const obj: any = {};
    if (message.service_name !== "") {
      obj.service_name = message.service_name;
    }
    if (message.instance_id !== "") {
      obj.instance_id = message.instance_id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeregisterServiceRequest>, I>>(base?: I): DeregisterServiceRequest {
    return DeregisterServiceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeregisterServiceRequest>, I>>(object: I): DeregisterServiceRequest {
    const message = createBaseDeregisterServiceRequest();
    message.service_name = object.service_name ?? "";
    message.instance_id = object.instance_id ?? "";
    return message;
  },
};

function createBaseDeregisterServiceResponse(): DeregisterServiceResponse {
  return { success: false, message: "" };
}

export const DeregisterServiceResponse: MessageFns<DeregisterServiceResponse> = {
  encode(message: DeregisterServiceResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeregisterServiceResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeregisterServiceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeregisterServiceResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: DeregisterServiceResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeregisterServiceResponse>, I>>(base?: I): DeregisterServiceResponse {
    return DeregisterServiceResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeregisterServiceResponse>, I>>(object: I): DeregisterServiceResponse {
    const message = createBaseDeregisterServiceResponse();
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseDeleteServiceRecordRequest(): DeleteServiceRecordRequest {
  return { service_name: "", instance_id: "" };
}

export const DeleteServiceRecordRequest: MessageFns<DeleteServiceRecordRequest> = {
  encode(message: DeleteServiceRecordRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service_name !== "") {
      writer.uint32(10).string(message.service_name);
    }
    if (message.instance_id !== "") {
      writer.uint32(18).string(message.instance_id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteServiceRecordRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteServiceRecordRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service_name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.instance_id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteServiceRecordRequest {
    return {
      service_name: isSet(object.service_name) ? globalThis.String(object.service_name) : "",
      instance_id: isSet(object.instance_id) ? globalThis.String(object.instance_id) : "",
    };
  },

  toJSON(message: DeleteServiceRecordRequest): unknown {
    const obj: any = {};
    if (message.service_name !== "") {
      obj.service_name = message.service_name;
    }
    if (message.instance_id !== "") {
      obj.instance_id = message.instance_id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteServiceRecordRequest>, I>>(base?: I): DeleteServiceRecordRequest {
    return DeleteServiceRecordRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteServiceRecordRequest>, I>>(object: I): DeleteServiceRecordRequest {
    const message = createBaseDeleteServiceRecordRequest();
    message.service_name = object.service_name ?? "";
    message.instance_id = object.instance_id ?? "";
    return message;
  },
};

function createBaseDeleteServiceRecordResponse(): DeleteServiceRecordResponse {
  return { success: false, message: "" };
}

export const DeleteServiceRecordResponse: MessageFns<DeleteServiceRecordResponse> = {
  encode(message: DeleteServiceRecordResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteServiceRecordResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteServiceRecordResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteServiceRecordResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: DeleteServiceRecordResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteServiceRecordResponse>, I>>(base?: I): DeleteServiceRecordResponse {
    return DeleteServiceRecordResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteServiceRecordResponse>, I>>(object: I): DeleteServiceRecordResponse {
    const message = createBaseDeleteServiceRecordResponse();
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseUpdateServiceInstanceRequest(): UpdateServiceInstanceRequest {
  return { service: undefined };
}

export const UpdateServiceInstanceRequest: MessageFns<UpdateServiceInstanceRequest> = {
  encode(message: UpdateServiceInstanceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service !== undefined) {
      ServiceInstance.encode(message.service, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateServiceInstanceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateServiceInstanceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service = ServiceInstance.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateServiceInstanceRequest {
    return { service: isSet(object.service) ? ServiceInstance.fromJSON(object.service) : undefined };
  },

  toJSON(message: UpdateServiceInstanceRequest): unknown {
    const obj: any = {};
    if (message.service !== undefined) {
      obj.service = ServiceInstance.toJSON(message.service);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateServiceInstanceRequest>, I>>(base?: I): UpdateServiceInstanceRequest {
    return UpdateServiceInstanceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateServiceInstanceRequest>, I>>(object: I): UpdateServiceInstanceRequest {
    const message = createBaseUpdateServiceInstanceRequest();
    message.service = (object.service !== undefined && object.service !== null)
      ? ServiceInstance.fromPartial(object.service)
      : undefined;
    return message;
  },
};

function createBaseUpdateServiceInstanceResponse(): UpdateServiceInstanceResponse {
  return { success: false, message: "", updated_service: undefined };
}

export const UpdateServiceInstanceResponse: MessageFns<UpdateServiceInstanceResponse> = {
  encode(message: UpdateServiceInstanceResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.updated_service !== undefined) {
      ServiceInstance.encode(message.updated_service, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateServiceInstanceResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateServiceInstanceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updated_service = ServiceInstance.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateServiceInstanceResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      updated_service: isSet(object.updated_service) ? ServiceInstance.fromJSON(object.updated_service) : undefined,
    };
  },

  toJSON(message: UpdateServiceInstanceResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.updated_service !== undefined) {
      obj.updated_service = ServiceInstance.toJSON(message.updated_service);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateServiceInstanceResponse>, I>>(base?: I): UpdateServiceInstanceResponse {
    return UpdateServiceInstanceResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateServiceInstanceResponse>, I>>(
    object: I,
  ): UpdateServiceInstanceResponse {
    const message = createBaseUpdateServiceInstanceResponse();
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    message.updated_service = (object.updated_service !== undefined && object.updated_service !== null)
      ? ServiceInstance.fromPartial(object.updated_service)
      : undefined;
    return message;
  },
};

function createBaseGetServiceInstancesRequest(): GetServiceInstancesRequest {
  return { service_name: "", tag_filters: {}, healthy_only: false };
}

export const GetServiceInstancesRequest: MessageFns<GetServiceInstancesRequest> = {
  encode(message: GetServiceInstancesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service_name !== "") {
      writer.uint32(10).string(message.service_name);
    }
    Object.entries(message.tag_filters).forEach(([key, value]) => {
      GetServiceInstancesRequest_TagFiltersEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    if (message.healthy_only !== false) {
      writer.uint32(24).bool(message.healthy_only);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetServiceInstancesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetServiceInstancesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service_name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = GetServiceInstancesRequest_TagFiltersEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.tag_filters[entry2.key] = entry2.value;
          }
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.healthy_only = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetServiceInstancesRequest {
    return {
      service_name: isSet(object.service_name) ? globalThis.String(object.service_name) : "",
      tag_filters: isObject(object.tag_filters)
        ? Object.entries(object.tag_filters).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      healthy_only: isSet(object.healthy_only) ? globalThis.Boolean(object.healthy_only) : false,
    };
  },

  toJSON(message: GetServiceInstancesRequest): unknown {
    const obj: any = {};
    if (message.service_name !== "") {
      obj.service_name = message.service_name;
    }
    if (message.tag_filters) {
      const entries = Object.entries(message.tag_filters);
      if (entries.length > 0) {
        obj.tag_filters = {};
        entries.forEach(([k, v]) => {
          obj.tag_filters[k] = v;
        });
      }
    }
    if (message.healthy_only !== false) {
      obj.healthy_only = message.healthy_only;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetServiceInstancesRequest>, I>>(base?: I): GetServiceInstancesRequest {
    return GetServiceInstancesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetServiceInstancesRequest>, I>>(object: I): GetServiceInstancesRequest {
    const message = createBaseGetServiceInstancesRequest();
    message.service_name = object.service_name ?? "";
    message.tag_filters = Object.entries(object.tag_filters ?? {}).reduce<{ [key: string]: string }>(
      (acc, [key, value]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.healthy_only = object.healthy_only ?? false;
    return message;
  },
};

function createBaseGetServiceInstancesRequest_TagFiltersEntry(): GetServiceInstancesRequest_TagFiltersEntry {
  return { key: "", value: "" };
}

export const GetServiceInstancesRequest_TagFiltersEntry: MessageFns<GetServiceInstancesRequest_TagFiltersEntry> = {
  encode(message: GetServiceInstancesRequest_TagFiltersEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetServiceInstancesRequest_TagFiltersEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetServiceInstancesRequest_TagFiltersEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetServiceInstancesRequest_TagFiltersEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: GetServiceInstancesRequest_TagFiltersEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetServiceInstancesRequest_TagFiltersEntry>, I>>(
    base?: I,
  ): GetServiceInstancesRequest_TagFiltersEntry {
    return GetServiceInstancesRequest_TagFiltersEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetServiceInstancesRequest_TagFiltersEntry>, I>>(
    object: I,
  ): GetServiceInstancesRequest_TagFiltersEntry {
    const message = createBaseGetServiceInstancesRequest_TagFiltersEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseGetServiceInstancesResponse(): GetServiceInstancesResponse {
  return { instances: [], total_count: 0 };
}

export const GetServiceInstancesResponse: MessageFns<GetServiceInstancesResponse> = {
  encode(message: GetServiceInstancesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.instances) {
      ServiceInstance.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.total_count !== 0) {
      writer.uint32(16).uint32(message.total_count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetServiceInstancesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetServiceInstancesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.instances.push(ServiceInstance.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.total_count = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetServiceInstancesResponse {
    return {
      instances: globalThis.Array.isArray(object?.instances)
        ? object.instances.map((e: any) => ServiceInstance.fromJSON(e))
        : [],
      total_count: isSet(object.total_count) ? globalThis.Number(object.total_count) : 0,
    };
  },

  toJSON(message: GetServiceInstancesResponse): unknown {
    const obj: any = {};
    if (message.instances?.length) {
      obj.instances = message.instances.map((e) => ServiceInstance.toJSON(e));
    }
    if (message.total_count !== 0) {
      obj.total_count = Math.round(message.total_count);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetServiceInstancesResponse>, I>>(base?: I): GetServiceInstancesResponse {
    return GetServiceInstancesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetServiceInstancesResponse>, I>>(object: I): GetServiceInstancesResponse {
    const message = createBaseGetServiceInstancesResponse();
    message.instances = object.instances?.map((e) => ServiceInstance.fromPartial(e)) || [];
    message.total_count = object.total_count ?? 0;
    return message;
  },
};

function createBaseListServicesRequest(): ListServicesRequest {
  return { name_prefix: "", tag_filters: {} };
}

export const ListServicesRequest: MessageFns<ListServicesRequest> = {
  encode(message: ListServicesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name_prefix !== "") {
      writer.uint32(10).string(message.name_prefix);
    }
    Object.entries(message.tag_filters).forEach(([key, value]) => {
      ListServicesRequest_TagFiltersEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListServicesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListServicesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name_prefix = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = ListServicesRequest_TagFiltersEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.tag_filters[entry2.key] = entry2.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListServicesRequest {
    return {
      name_prefix: isSet(object.name_prefix) ? globalThis.String(object.name_prefix) : "",
      tag_filters: isObject(object.tag_filters)
        ? Object.entries(object.tag_filters).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
    };
  },

  toJSON(message: ListServicesRequest): unknown {
    const obj: any = {};
    if (message.name_prefix !== "") {
      obj.name_prefix = message.name_prefix;
    }
    if (message.tag_filters) {
      const entries = Object.entries(message.tag_filters);
      if (entries.length > 0) {
        obj.tag_filters = {};
        entries.forEach(([k, v]) => {
          obj.tag_filters[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListServicesRequest>, I>>(base?: I): ListServicesRequest {
    return ListServicesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListServicesRequest>, I>>(object: I): ListServicesRequest {
    const message = createBaseListServicesRequest();
    message.name_prefix = object.name_prefix ?? "";
    message.tag_filters = Object.entries(object.tag_filters ?? {}).reduce<{ [key: string]: string }>(
      (acc, [key, value]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseListServicesRequest_TagFiltersEntry(): ListServicesRequest_TagFiltersEntry {
  return { key: "", value: "" };
}

export const ListServicesRequest_TagFiltersEntry: MessageFns<ListServicesRequest_TagFiltersEntry> = {
  encode(message: ListServicesRequest_TagFiltersEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListServicesRequest_TagFiltersEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListServicesRequest_TagFiltersEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListServicesRequest_TagFiltersEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ListServicesRequest_TagFiltersEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListServicesRequest_TagFiltersEntry>, I>>(
    base?: I,
  ): ListServicesRequest_TagFiltersEntry {
    return ListServicesRequest_TagFiltersEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListServicesRequest_TagFiltersEntry>, I>>(
    object: I,
  ): ListServicesRequest_TagFiltersEntry {
    const message = createBaseListServicesRequest_TagFiltersEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseListServicesResponse(): ListServicesResponse {
  return { services: [], total_count: 0 };
}

export const ListServicesResponse: MessageFns<ListServicesResponse> = {
  encode(message: ListServicesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.services) {
      ServiceInstance.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.total_count !== 0) {
      writer.uint32(16).uint32(message.total_count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListServicesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListServicesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.services.push(ServiceInstance.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.total_count = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListServicesResponse {
    return {
      services: globalThis.Array.isArray(object?.services)
        ? object.services.map((e: any) => ServiceInstance.fromJSON(e))
        : [],
      total_count: isSet(object.total_count) ? globalThis.Number(object.total_count) : 0,
    };
  },

  toJSON(message: ListServicesResponse): unknown {
    const obj: any = {};
    if (message.services?.length) {
      obj.services = message.services.map((e) => ServiceInstance.toJSON(e));
    }
    if (message.total_count !== 0) {
      obj.total_count = Math.round(message.total_count);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListServicesResponse>, I>>(base?: I): ListServicesResponse {
    return ListServicesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListServicesResponse>, I>>(object: I): ListServicesResponse {
    const message = createBaseListServicesResponse();
    message.services = object.services?.map((e) => ServiceInstance.fromPartial(e)) || [];
    message.total_count = object.total_count ?? 0;
    return message;
  },
};

function createBaseServiceSummary(): ServiceSummary {
  return { service_name: "", instance_count: 0, healthy_count: 0, versions: [] };
}

export const ServiceSummary: MessageFns<ServiceSummary> = {
  encode(message: ServiceSummary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service_name !== "") {
      writer.uint32(10).string(message.service_name);
    }
    if (message.instance_count !== 0) {
      writer.uint32(16).uint32(message.instance_count);
    }
    if (message.healthy_count !== 0) {
      writer.uint32(24).uint32(message.healthy_count);
    }
    for (const v of message.versions) {
      writer.uint32(34).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServiceSummary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServiceSummary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service_name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.instance_count = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.healthy_count = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.versions.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServiceSummary {
    return {
      service_name: isSet(object.service_name) ? globalThis.String(object.service_name) : "",
      instance_count: isSet(object.instance_count) ? globalThis.Number(object.instance_count) : 0,
      healthy_count: isSet(object.healthy_count) ? globalThis.Number(object.healthy_count) : 0,
      versions: globalThis.Array.isArray(object?.versions) ? object.versions.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: ServiceSummary): unknown {
    const obj: any = {};
    if (message.service_name !== "") {
      obj.service_name = message.service_name;
    }
    if (message.instance_count !== 0) {
      obj.instance_count = Math.round(message.instance_count);
    }
    if (message.healthy_count !== 0) {
      obj.healthy_count = Math.round(message.healthy_count);
    }
    if (message.versions?.length) {
      obj.versions = message.versions;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServiceSummary>, I>>(base?: I): ServiceSummary {
    return ServiceSummary.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServiceSummary>, I>>(object: I): ServiceSummary {
    const message = createBaseServiceSummary();
    message.service_name = object.service_name ?? "";
    message.instance_count = object.instance_count ?? 0;
    message.healthy_count = object.healthy_count ?? 0;
    message.versions = object.versions?.map((e) => e) || [];
    return message;
  },
};

function createBaseUpdateServiceHealthRequest(): UpdateServiceHealthRequest {
  return { service_name: "", instance_id: "", status: 0, health_message: "" };
}

export const UpdateServiceHealthRequest: MessageFns<UpdateServiceHealthRequest> = {
  encode(message: UpdateServiceHealthRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service_name !== "") {
      writer.uint32(10).string(message.service_name);
    }
    if (message.instance_id !== "") {
      writer.uint32(18).string(message.instance_id);
    }
    if (message.status !== 0) {
      writer.uint32(24).int32(message.status);
    }
    if (message.health_message !== "") {
      writer.uint32(34).string(message.health_message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateServiceHealthRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateServiceHealthRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service_name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.instance_id = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.health_message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateServiceHealthRequest {
    return {
      service_name: isSet(object.service_name) ? globalThis.String(object.service_name) : "",
      instance_id: isSet(object.instance_id) ? globalThis.String(object.instance_id) : "",
      status: isSet(object.status) ? serviceStatusFromJSON(object.status) : 0,
      health_message: isSet(object.health_message) ? globalThis.String(object.health_message) : "",
    };
  },

  toJSON(message: UpdateServiceHealthRequest): unknown {
    const obj: any = {};
    if (message.service_name !== "") {
      obj.service_name = message.service_name;
    }
    if (message.instance_id !== "") {
      obj.instance_id = message.instance_id;
    }
    if (message.status !== 0) {
      obj.status = serviceStatusToJSON(message.status);
    }
    if (message.health_message !== "") {
      obj.health_message = message.health_message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateServiceHealthRequest>, I>>(base?: I): UpdateServiceHealthRequest {
    return UpdateServiceHealthRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateServiceHealthRequest>, I>>(object: I): UpdateServiceHealthRequest {
    const message = createBaseUpdateServiceHealthRequest();
    message.service_name = object.service_name ?? "";
    message.instance_id = object.instance_id ?? "";
    message.status = object.status ?? 0;
    message.health_message = object.health_message ?? "";
    return message;
  },
};

function createBaseUpdateServiceHealthResponse(): UpdateServiceHealthResponse {
  return { success: false, message: "" };
}

export const UpdateServiceHealthResponse: MessageFns<UpdateServiceHealthResponse> = {
  encode(message: UpdateServiceHealthResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateServiceHealthResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateServiceHealthResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateServiceHealthResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: UpdateServiceHealthResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateServiceHealthResponse>, I>>(base?: I): UpdateServiceHealthResponse {
    return UpdateServiceHealthResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateServiceHealthResponse>, I>>(object: I): UpdateServiceHealthResponse {
    const message = createBaseUpdateServiceHealthResponse();
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseServiceHealthCheckRequest(): ServiceHealthCheckRequest {
  return { service_name: "", instance_id: "" };
}

export const ServiceHealthCheckRequest: MessageFns<ServiceHealthCheckRequest> = {
  encode(message: ServiceHealthCheckRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service_name !== "") {
      writer.uint32(10).string(message.service_name);
    }
    if (message.instance_id !== "") {
      writer.uint32(18).string(message.instance_id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServiceHealthCheckRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServiceHealthCheckRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service_name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.instance_id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServiceHealthCheckRequest {
    return {
      service_name: isSet(object.service_name) ? globalThis.String(object.service_name) : "",
      instance_id: isSet(object.instance_id) ? globalThis.String(object.instance_id) : "",
    };
  },

  toJSON(message: ServiceHealthCheckRequest): unknown {
    const obj: any = {};
    if (message.service_name !== "") {
      obj.service_name = message.service_name;
    }
    if (message.instance_id !== "") {
      obj.instance_id = message.instance_id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServiceHealthCheckRequest>, I>>(base?: I): ServiceHealthCheckRequest {
    return ServiceHealthCheckRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServiceHealthCheckRequest>, I>>(object: I): ServiceHealthCheckRequest {
    const message = createBaseServiceHealthCheckRequest();
    message.service_name = object.service_name ?? "";
    message.instance_id = object.instance_id ?? "";
    return message;
  },
};

function createBaseServiceHealthCheckResponse(): ServiceHealthCheckResponse {
  return { instance_healths: [] };
}

export const ServiceHealthCheckResponse: MessageFns<ServiceHealthCheckResponse> = {
  encode(message: ServiceHealthCheckResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.instance_healths) {
      ServiceInstanceHealth.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServiceHealthCheckResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServiceHealthCheckResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.instance_healths.push(ServiceInstanceHealth.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServiceHealthCheckResponse {
    return {
      instance_healths: globalThis.Array.isArray(object?.instance_healths)
        ? object.instance_healths.map((e: any) => ServiceInstanceHealth.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ServiceHealthCheckResponse): unknown {
    const obj: any = {};
    if (message.instance_healths?.length) {
      obj.instance_healths = message.instance_healths.map((e) => ServiceInstanceHealth.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServiceHealthCheckResponse>, I>>(base?: I): ServiceHealthCheckResponse {
    return ServiceHealthCheckResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServiceHealthCheckResponse>, I>>(object: I): ServiceHealthCheckResponse {
    const message = createBaseServiceHealthCheckResponse();
    message.instance_healths = object.instance_healths?.map((e) => ServiceInstanceHealth.fromPartial(e)) || [];
    return message;
  },
};

function createBaseServiceInstanceHealth(): ServiceInstanceHealth {
  return { instance: undefined, status: 0, message: "", last_check: undefined };
}

export const ServiceInstanceHealth: MessageFns<ServiceInstanceHealth> = {
  encode(message: ServiceInstanceHealth, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.instance !== undefined) {
      ServiceInstance.encode(message.instance, writer.uint32(10).fork()).join();
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    if (message.last_check !== undefined) {
      Timestamp.encode(toTimestamp(message.last_check), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServiceInstanceHealth {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServiceInstanceHealth();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.instance = ServiceInstance.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.last_check = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServiceInstanceHealth {
    return {
      instance: isSet(object.instance) ? ServiceInstance.fromJSON(object.instance) : undefined,
      status: isSet(object.status) ? serviceStatusFromJSON(object.status) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      last_check: isSet(object.last_check) ? fromJsonTimestamp(object.last_check) : undefined,
    };
  },

  toJSON(message: ServiceInstanceHealth): unknown {
    const obj: any = {};
    if (message.instance !== undefined) {
      obj.instance = ServiceInstance.toJSON(message.instance);
    }
    if (message.status !== 0) {
      obj.status = serviceStatusToJSON(message.status);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.last_check !== undefined) {
      obj.last_check = message.last_check.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServiceInstanceHealth>, I>>(base?: I): ServiceInstanceHealth {
    return ServiceInstanceHealth.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServiceInstanceHealth>, I>>(object: I): ServiceInstanceHealth {
    const message = createBaseServiceInstanceHealth();
    message.instance = (object.instance !== undefined && object.instance !== null)
      ? ServiceInstance.fromPartial(object.instance)
      : undefined;
    message.status = object.status ?? 0;
    message.message = object.message ?? "";
    message.last_check = object.last_check ?? undefined;
    return message;
  },
};

function createBaseUploadServiceConfigRequest(): UploadServiceConfigRequest {
  return { service_name: "", config_version: "", config_data: undefined, description: "" };
}

export const UploadServiceConfigRequest: MessageFns<UploadServiceConfigRequest> = {
  encode(message: UploadServiceConfigRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service_name !== "") {
      writer.uint32(10).string(message.service_name);
    }
    if (message.config_version !== "") {
      writer.uint32(18).string(message.config_version);
    }
    if (message.config_data !== undefined) {
      Struct.encode(Struct.wrap(message.config_data), writer.uint32(26).fork()).join();
    }
    if (message.description !== "") {
      writer.uint32(34).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadServiceConfigRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadServiceConfigRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service_name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.config_version = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.config_data = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadServiceConfigRequest {
    return {
      service_name: isSet(object.service_name) ? globalThis.String(object.service_name) : "",
      config_version: isSet(object.config_version) ? globalThis.String(object.config_version) : "",
      config_data: isObject(object.config_data) ? object.config_data : undefined,
      description: isSet(object.description) ? globalThis.String(object.description) : "",
    };
  },

  toJSON(message: UploadServiceConfigRequest): unknown {
    const obj: any = {};
    if (message.service_name !== "") {
      obj.service_name = message.service_name;
    }
    if (message.config_version !== "") {
      obj.config_version = message.config_version;
    }
    if (message.config_data !== undefined) {
      obj.config_data = message.config_data;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UploadServiceConfigRequest>, I>>(base?: I): UploadServiceConfigRequest {
    return UploadServiceConfigRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UploadServiceConfigRequest>, I>>(object: I): UploadServiceConfigRequest {
    const message = createBaseUploadServiceConfigRequest();
    message.service_name = object.service_name ?? "";
    message.config_version = object.config_version ?? "";
    message.config_data = object.config_data ?? undefined;
    message.description = object.description ?? "";
    return message;
  },
};

function createBaseUploadServiceConfigResponse(): UploadServiceConfigResponse {
  return { success: false, message: "", config_key: "" };
}

export const UploadServiceConfigResponse: MessageFns<UploadServiceConfigResponse> = {
  encode(message: UploadServiceConfigResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.config_key !== "") {
      writer.uint32(26).string(message.config_key);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadServiceConfigResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadServiceConfigResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.config_key = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadServiceConfigResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      config_key: isSet(object.config_key) ? globalThis.String(object.config_key) : "",
    };
  },

  toJSON(message: UploadServiceConfigResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.config_key !== "") {
      obj.config_key = message.config_key;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UploadServiceConfigResponse>, I>>(base?: I): UploadServiceConfigResponse {
    return UploadServiceConfigResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UploadServiceConfigResponse>, I>>(object: I): UploadServiceConfigResponse {
    const message = createBaseUploadServiceConfigResponse();
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    message.config_key = object.config_key ?? "";
    return message;
  },
};

function createBaseGetServiceConfigRequest(): GetServiceConfigRequest {
  return { service_name: "", config_version: "" };
}

export const GetServiceConfigRequest: MessageFns<GetServiceConfigRequest> = {
  encode(message: GetServiceConfigRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service_name !== "") {
      writer.uint32(10).string(message.service_name);
    }
    if (message.config_version !== "") {
      writer.uint32(18).string(message.config_version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetServiceConfigRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetServiceConfigRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service_name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.config_version = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetServiceConfigRequest {
    return {
      service_name: isSet(object.service_name) ? globalThis.String(object.service_name) : "",
      config_version: isSet(object.config_version) ? globalThis.String(object.config_version) : "",
    };
  },

  toJSON(message: GetServiceConfigRequest): unknown {
    const obj: any = {};
    if (message.service_name !== "") {
      obj.service_name = message.service_name;
    }
    if (message.config_version !== "") {
      obj.config_version = message.config_version;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetServiceConfigRequest>, I>>(base?: I): GetServiceConfigRequest {
    return GetServiceConfigRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetServiceConfigRequest>, I>>(object: I): GetServiceConfigRequest {
    const message = createBaseGetServiceConfigRequest();
    message.service_name = object.service_name ?? "";
    message.config_version = object.config_version ?? "";
    return message;
  },
};

function createBaseGetServiceConfigResponse(): GetServiceConfigResponse {
  return { config_data: undefined, config_version: "", updated_at: undefined, description: "" };
}

export const GetServiceConfigResponse: MessageFns<GetServiceConfigResponse> = {
  encode(message: GetServiceConfigResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.config_data !== undefined) {
      Struct.encode(Struct.wrap(message.config_data), writer.uint32(10).fork()).join();
    }
    if (message.config_version !== "") {
      writer.uint32(18).string(message.config_version);
    }
    if (message.updated_at !== undefined) {
      Timestamp.encode(toTimestamp(message.updated_at), writer.uint32(26).fork()).join();
    }
    if (message.description !== "") {
      writer.uint32(34).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetServiceConfigResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetServiceConfigResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.config_data = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.config_version = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updated_at = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetServiceConfigResponse {
    return {
      config_data: isObject(object.config_data) ? object.config_data : undefined,
      config_version: isSet(object.config_version) ? globalThis.String(object.config_version) : "",
      updated_at: isSet(object.updated_at) ? fromJsonTimestamp(object.updated_at) : undefined,
      description: isSet(object.description) ? globalThis.String(object.description) : "",
    };
  },

  toJSON(message: GetServiceConfigResponse): unknown {
    const obj: any = {};
    if (message.config_data !== undefined) {
      obj.config_data = message.config_data;
    }
    if (message.config_version !== "") {
      obj.config_version = message.config_version;
    }
    if (message.updated_at !== undefined) {
      obj.updated_at = message.updated_at.toISOString();
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetServiceConfigResponse>, I>>(base?: I): GetServiceConfigResponse {
    return GetServiceConfigResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetServiceConfigResponse>, I>>(object: I): GetServiceConfigResponse {
    const message = createBaseGetServiceConfigResponse();
    message.config_data = object.config_data ?? undefined;
    message.config_version = object.config_version ?? "";
    message.updated_at = object.updated_at ?? undefined;
    message.description = object.description ?? "";
    return message;
  },
};

function createBaseUploadProtobufDescriptorRequest(): UploadProtobufDescriptorRequest {
  return {
    service_name: "",
    descriptor_version: "",
    descriptor_data: new Uint8Array(0),
    description: "",
    signature: "",
    force: false,
    previous_version: "",
  };
}

export const UploadProtobufDescriptorRequest: MessageFns<UploadProtobufDescriptorRequest> = {
  encode(message: UploadProtobufDescriptorRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service_name !== "") {
      writer.uint32(10).string(message.service_name);
    }
    if (message.descriptor_version !== "") {
      writer.uint32(18).string(message.descriptor_version);
    }
    if (message.descriptor_data.length !== 0) {
      writer.uint32(26).bytes(message.descriptor_data);
    }
    if (message.description !== "") {
      writer.uint32(34).string(message.description);
    }
    if (message.signature !== "") {
      writer.uint32(42).string(message.signature);
    }
    if (message.force !== false) {
      writer.uint32(48).bool(message.force);
    }
    if (message.previous_version !== "") {
      writer.uint32(58).string(message.previous_version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadProtobufDescriptorRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadProtobufDescriptorRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service_name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.descriptor_version = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.descriptor_data = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.signature = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.force = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.previous_version = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadProtobufDescriptorRequest {
    return {
      service_name: isSet(object.service_name) ? globalThis.String(object.service_name) : "",
      descriptor_version: isSet(object.descriptor_version) ? globalThis.String(object.descriptor_version) : "",
      descriptor_data: isSet(object.descriptor_data) ? bytesFromBase64(object.descriptor_data) : new Uint8Array(0),
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      signature: isSet(object.signature) ? globalThis.String(object.signature) : "",
      force: isSet(object.force) ? globalThis.Boolean(object.force) : false,
      previous_version: isSet(object.previous_version) ? globalThis.String(object.previous_version) : "",
    };
  },

  toJSON(message: UploadProtobufDescriptorRequest): unknown {
    const obj: any = {};
    if (message.service_name !== "") {
      obj.service_name = message.service_name;
    }
    if (message.descriptor_version !== "") {
      obj.descriptor_version = message.descriptor_version;
    }
    if (message.descriptor_data.length !== 0) {
      obj.descriptor_data = base64FromBytes(message.descriptor_data);
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.signature !== "") {
      obj.signature = message.signature;
    }
    if (message.force !== false) {
      obj.force = message.force;
    }
    if (message.previous_version !== "") {
      obj.previous_version = message.previous_version;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UploadProtobufDescriptorRequest>, I>>(base?: I): UploadProtobufDescriptorRequest {
    return UploadProtobufDescriptorRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UploadProtobufDescriptorRequest>, I>>(
    object: I,
  ): UploadProtobufDescriptorRequest {
    const message = createBaseUploadProtobufDescriptorRequest();
    message.service_name = object.service_name ?? "";
    message.descriptor_version = object.descriptor_version ?? "";
    message.descriptor_data = object.descriptor_data ?? new Uint8Array(0);
    message.description = object.description ?? "";
    message.signature = object.signature ?? "";
    message.force = object.force ?? false;
    message.previous_version = object.previous_version ?? "";
    return message;
  },
};

function createBaseUploadProtobufDescriptorResponse(): UploadProtobufDescriptorResponse {
  return {
    success: false,
    message: "",
    descriptor_key: "",
    discovered_services: [],
    compatibility_warnings: [],
    applied_version: "",
    descriptor_hash: "",
  };
}

export const UploadProtobufDescriptorResponse: MessageFns<UploadProtobufDescriptorResponse> = {
  encode(message: UploadProtobufDescriptorResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.descriptor_key !== "") {
      writer.uint32(26).string(message.descriptor_key);
    }
    for (const v of message.discovered_services) {
      writer.uint32(34).string(v!);
    }
    for (const v of message.compatibility_warnings) {
      writer.uint32(42).string(v!);
    }
    if (message.applied_version !== "") {
      writer.uint32(50).string(message.applied_version);
    }
    if (message.descriptor_hash !== "") {
      writer.uint32(58).string(message.descriptor_hash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadProtobufDescriptorResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadProtobufDescriptorResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.descriptor_key = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.discovered_services.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.compatibility_warnings.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.applied_version = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.descriptor_hash = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadProtobufDescriptorResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      descriptor_key: isSet(object.descriptor_key) ? globalThis.String(object.descriptor_key) : "",
      discovered_services: globalThis.Array.isArray(object?.discovered_services)
        ? object.discovered_services.map((e: any) => globalThis.String(e))
        : [],
      compatibility_warnings: globalThis.Array.isArray(object?.compatibility_warnings)
        ? object.compatibility_warnings.map((e: any) => globalThis.String(e))
        : [],
      applied_version: isSet(object.applied_version) ? globalThis.String(object.applied_version) : "",
      descriptor_hash: isSet(object.descriptor_hash) ? globalThis.String(object.descriptor_hash) : "",
    };
  },

  toJSON(message: UploadProtobufDescriptorResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.descriptor_key !== "") {
      obj.descriptor_key = message.descriptor_key;
    }
    if (message.discovered_services?.length) {
      obj.discovered_services = message.discovered_services;
    }
    if (message.compatibility_warnings?.length) {
      obj.compatibility_warnings = message.compatibility_warnings;
    }
    if (message.applied_version !== "") {
      obj.applied_version = message.applied_version;
    }
    if (message.descriptor_hash !== "") {
      obj.descriptor_hash = message.descriptor_hash;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UploadProtobufDescriptorResponse>, I>>(
    base?: I,
  ): UploadProtobufDescriptorResponse {
    return UploadProtobufDescriptorResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UploadProtobufDescriptorResponse>, I>>(
    object: I,
  ): UploadProtobufDescriptorResponse {
    const message = createBaseUploadProtobufDescriptorResponse();
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    message.descriptor_key = object.descriptor_key ?? "";
    message.discovered_services = object.discovered_services?.map((e) => e) || [];
    message.compatibility_warnings = object.compatibility_warnings?.map((e) => e) || [];
    message.applied_version = object.applied_version ?? "";
    message.descriptor_hash = object.descriptor_hash ?? "";
    return message;
  },
};

function createBaseGetProtobufDescriptorRequest(): GetProtobufDescriptorRequest {
  return { service_name: "", descriptor_version: "" };
}

export const GetProtobufDescriptorRequest: MessageFns<GetProtobufDescriptorRequest> = {
  encode(message: GetProtobufDescriptorRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service_name !== "") {
      writer.uint32(10).string(message.service_name);
    }
    if (message.descriptor_version !== "") {
      writer.uint32(18).string(message.descriptor_version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetProtobufDescriptorRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetProtobufDescriptorRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service_name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.descriptor_version = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetProtobufDescriptorRequest {
    return {
      service_name: isSet(object.service_name) ? globalThis.String(object.service_name) : "",
      descriptor_version: isSet(object.descriptor_version) ? globalThis.String(object.descriptor_version) : "",
    };
  },

  toJSON(message: GetProtobufDescriptorRequest): unknown {
    const obj: any = {};
    if (message.service_name !== "") {
      obj.service_name = message.service_name;
    }
    if (message.descriptor_version !== "") {
      obj.descriptor_version = message.descriptor_version;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetProtobufDescriptorRequest>, I>>(base?: I): GetProtobufDescriptorRequest {
    return GetProtobufDescriptorRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetProtobufDescriptorRequest>, I>>(object: I): GetProtobufDescriptorRequest {
    const message = createBaseGetProtobufDescriptorRequest();
    message.service_name = object.service_name ?? "";
    message.descriptor_version = object.descriptor_version ?? "";
    return message;
  },
};

function createBaseGetProtobufDescriptorResponse(): GetProtobufDescriptorResponse {
  return {
    descriptor_data: new Uint8Array(0),
    descriptor_version: "",
    updated_at: undefined,
    description: "",
    services: [],
  };
}

export const GetProtobufDescriptorResponse: MessageFns<GetProtobufDescriptorResponse> = {
  encode(message: GetProtobufDescriptorResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.descriptor_data.length !== 0) {
      writer.uint32(10).bytes(message.descriptor_data);
    }
    if (message.descriptor_version !== "") {
      writer.uint32(18).string(message.descriptor_version);
    }
    if (message.updated_at !== undefined) {
      Timestamp.encode(toTimestamp(message.updated_at), writer.uint32(26).fork()).join();
    }
    if (message.description !== "") {
      writer.uint32(34).string(message.description);
    }
    for (const v of message.services) {
      writer.uint32(42).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetProtobufDescriptorResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetProtobufDescriptorResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.descriptor_data = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.descriptor_version = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updated_at = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.services.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetProtobufDescriptorResponse {
    return {
      descriptor_data: isSet(object.descriptor_data) ? bytesFromBase64(object.descriptor_data) : new Uint8Array(0),
      descriptor_version: isSet(object.descriptor_version) ? globalThis.String(object.descriptor_version) : "",
      updated_at: isSet(object.updated_at) ? fromJsonTimestamp(object.updated_at) : undefined,
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      services: globalThis.Array.isArray(object?.services) ? object.services.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: GetProtobufDescriptorResponse): unknown {
    const obj: any = {};
    if (message.descriptor_data.length !== 0) {
      obj.descriptor_data = base64FromBytes(message.descriptor_data);
    }
    if (message.descriptor_version !== "") {
      obj.descriptor_version = message.descriptor_version;
    }
    if (message.updated_at !== undefined) {
      obj.updated_at = message.updated_at.toISOString();
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.services?.length) {
      obj.services = message.services;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetProtobufDescriptorResponse>, I>>(base?: I): GetProtobufDescriptorResponse {
    return GetProtobufDescriptorResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetProtobufDescriptorResponse>, I>>(
    object: I,
  ): GetProtobufDescriptorResponse {
    const message = createBaseGetProtobufDescriptorResponse();
    message.descriptor_data = object.descriptor_data ?? new Uint8Array(0);
    message.descriptor_version = object.descriptor_version ?? "";
    message.updated_at = object.updated_at ?? undefined;
    message.description = object.description ?? "";
    message.services = object.services?.map((e) => e) || [];
    return message;
  },
};

function createBaseListProtobufDescriptorsRequest(): ListProtobufDescriptorsRequest {
  return { service_name_prefix: "" };
}

export const ListProtobufDescriptorsRequest: MessageFns<ListProtobufDescriptorsRequest> = {
  encode(message: ListProtobufDescriptorsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service_name_prefix !== "") {
      writer.uint32(10).string(message.service_name_prefix);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListProtobufDescriptorsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListProtobufDescriptorsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service_name_prefix = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListProtobufDescriptorsRequest {
    return {
      service_name_prefix: isSet(object.service_name_prefix) ? globalThis.String(object.service_name_prefix) : "",
    };
  },

  toJSON(message: ListProtobufDescriptorsRequest): unknown {
    const obj: any = {};
    if (message.service_name_prefix !== "") {
      obj.service_name_prefix = message.service_name_prefix;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListProtobufDescriptorsRequest>, I>>(base?: I): ListProtobufDescriptorsRequest {
    return ListProtobufDescriptorsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListProtobufDescriptorsRequest>, I>>(
    object: I,
  ): ListProtobufDescriptorsRequest {
    const message = createBaseListProtobufDescriptorsRequest();
    message.service_name_prefix = object.service_name_prefix ?? "";
    return message;
  },
};

function createBaseListProtobufDescriptorsResponse(): ListProtobufDescriptorsResponse {
  return { descriptors: [] };
}

export const ListProtobufDescriptorsResponse: MessageFns<ListProtobufDescriptorsResponse> = {
  encode(message: ListProtobufDescriptorsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.descriptors) {
      ProtobufDescriptorInfo.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListProtobufDescriptorsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListProtobufDescriptorsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.descriptors.push(ProtobufDescriptorInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListProtobufDescriptorsResponse {
    return {
      descriptors: globalThis.Array.isArray(object?.descriptors)
        ? object.descriptors.map((e: any) => ProtobufDescriptorInfo.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListProtobufDescriptorsResponse): unknown {
    const obj: any = {};
    if (message.descriptors?.length) {
      obj.descriptors = message.descriptors.map((e) => ProtobufDescriptorInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListProtobufDescriptorsResponse>, I>>(base?: I): ListProtobufDescriptorsResponse {
    return ListProtobufDescriptorsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListProtobufDescriptorsResponse>, I>>(
    object: I,
  ): ListProtobufDescriptorsResponse {
    const message = createBaseListProtobufDescriptorsResponse();
    message.descriptors = object.descriptors?.map((e) => ProtobufDescriptorInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseProtobufDescriptorInfo(): ProtobufDescriptorInfo {
  return {
    service_name: "",
    descriptor_version: "",
    updated_at: undefined,
    description: "",
    services: [],
    size_bytes: 0,
    descriptor_hash: "",
    is_active: false,
  };
}

export const ProtobufDescriptorInfo: MessageFns<ProtobufDescriptorInfo> = {
  encode(message: ProtobufDescriptorInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service_name !== "") {
      writer.uint32(10).string(message.service_name);
    }
    if (message.descriptor_version !== "") {
      writer.uint32(18).string(message.descriptor_version);
    }
    if (message.updated_at !== undefined) {
      Timestamp.encode(toTimestamp(message.updated_at), writer.uint32(26).fork()).join();
    }
    if (message.description !== "") {
      writer.uint32(34).string(message.description);
    }
    for (const v of message.services) {
      writer.uint32(42).string(v!);
    }
    if (message.size_bytes !== 0) {
      writer.uint32(48).uint64(message.size_bytes);
    }
    if (message.descriptor_hash !== "") {
      writer.uint32(58).string(message.descriptor_hash);
    }
    if (message.is_active !== false) {
      writer.uint32(64).bool(message.is_active);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProtobufDescriptorInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProtobufDescriptorInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service_name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.descriptor_version = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updated_at = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.services.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.size_bytes = longToNumber(reader.uint64());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.descriptor_hash = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.is_active = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProtobufDescriptorInfo {
    return {
      service_name: isSet(object.service_name) ? globalThis.String(object.service_name) : "",
      descriptor_version: isSet(object.descriptor_version) ? globalThis.String(object.descriptor_version) : "",
      updated_at: isSet(object.updated_at) ? fromJsonTimestamp(object.updated_at) : undefined,
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      services: globalThis.Array.isArray(object?.services) ? object.services.map((e: any) => globalThis.String(e)) : [],
      size_bytes: isSet(object.size_bytes) ? globalThis.Number(object.size_bytes) : 0,
      descriptor_hash: isSet(object.descriptor_hash) ? globalThis.String(object.descriptor_hash) : "",
      is_active: isSet(object.is_active) ? globalThis.Boolean(object.is_active) : false,
    };
  },

  toJSON(message: ProtobufDescriptorInfo): unknown {
    const obj: any = {};
    if (message.service_name !== "") {
      obj.service_name = message.service_name;
    }
    if (message.descriptor_version !== "") {
      obj.descriptor_version = message.descriptor_version;
    }
    if (message.updated_at !== undefined) {
      obj.updated_at = message.updated_at.toISOString();
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.services?.length) {
      obj.services = message.services;
    }
    if (message.size_bytes !== 0) {
      obj.size_bytes = Math.round(message.size_bytes);
    }
    if (message.descriptor_hash !== "") {
      obj.descriptor_hash = message.descriptor_hash;
    }
    if (message.is_active !== false) {
      obj.is_active = message.is_active;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProtobufDescriptorInfo>, I>>(base?: I): ProtobufDescriptorInfo {
    return ProtobufDescriptorInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProtobufDescriptorInfo>, I>>(object: I): ProtobufDescriptorInfo {
    const message = createBaseProtobufDescriptorInfo();
    message.service_name = object.service_name ?? "";
    message.descriptor_version = object.descriptor_version ?? "";
    message.updated_at = object.updated_at ?? undefined;
    message.description = object.description ?? "";
    message.services = object.services?.map((e) => e) || [];
    message.size_bytes = object.size_bytes ?? 0;
    message.descriptor_hash = object.descriptor_hash ?? "";
    message.is_active = object.is_active ?? false;
    return message;
  },
};

function createBaseDescriptorVersionInfo(): DescriptorVersionInfo {
  return {
    version: "",
    descriptor_hash: "",
    created_at: undefined,
    description: "",
    services: [],
    size_bytes: 0,
    is_active: false,
  };
}

export const DescriptorVersionInfo: MessageFns<DescriptorVersionInfo> = {
  encode(message: DescriptorVersionInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== "") {
      writer.uint32(10).string(message.version);
    }
    if (message.descriptor_hash !== "") {
      writer.uint32(18).string(message.descriptor_hash);
    }
    if (message.created_at !== undefined) {
      Timestamp.encode(toTimestamp(message.created_at), writer.uint32(26).fork()).join();
    }
    if (message.description !== "") {
      writer.uint32(34).string(message.description);
    }
    for (const v of message.services) {
      writer.uint32(42).string(v!);
    }
    if (message.size_bytes !== 0) {
      writer.uint32(48).uint64(message.size_bytes);
    }
    if (message.is_active !== false) {
      writer.uint32(56).bool(message.is_active);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DescriptorVersionInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDescriptorVersionInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.descriptor_hash = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.created_at = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.services.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.size_bytes = longToNumber(reader.uint64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.is_active = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DescriptorVersionInfo {
    return {
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      descriptor_hash: isSet(object.descriptor_hash) ? globalThis.String(object.descriptor_hash) : "",
      created_at: isSet(object.created_at) ? fromJsonTimestamp(object.created_at) : undefined,
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      services: globalThis.Array.isArray(object?.services) ? object.services.map((e: any) => globalThis.String(e)) : [],
      size_bytes: isSet(object.size_bytes) ? globalThis.Number(object.size_bytes) : 0,
      is_active: isSet(object.is_active) ? globalThis.Boolean(object.is_active) : false,
    };
  },

  toJSON(message: DescriptorVersionInfo): unknown {
    const obj: any = {};
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.descriptor_hash !== "") {
      obj.descriptor_hash = message.descriptor_hash;
    }
    if (message.created_at !== undefined) {
      obj.created_at = message.created_at.toISOString();
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.services?.length) {
      obj.services = message.services;
    }
    if (message.size_bytes !== 0) {
      obj.size_bytes = Math.round(message.size_bytes);
    }
    if (message.is_active !== false) {
      obj.is_active = message.is_active;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DescriptorVersionInfo>, I>>(base?: I): DescriptorVersionInfo {
    return DescriptorVersionInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DescriptorVersionInfo>, I>>(object: I): DescriptorVersionInfo {
    const message = createBaseDescriptorVersionInfo();
    message.version = object.version ?? "";
    message.descriptor_hash = object.descriptor_hash ?? "";
    message.created_at = object.created_at ?? undefined;
    message.description = object.description ?? "";
    message.services = object.services?.map((e) => e) || [];
    message.size_bytes = object.size_bytes ?? 0;
    message.is_active = object.is_active ?? false;
    return message;
  },
};

function createBaseRollbackDescriptorRequest(): RollbackDescriptorRequest {
  return { service_name: "", target_version: "" };
}

export const RollbackDescriptorRequest: MessageFns<RollbackDescriptorRequest> = {
  encode(message: RollbackDescriptorRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service_name !== "") {
      writer.uint32(10).string(message.service_name);
    }
    if (message.target_version !== "") {
      writer.uint32(18).string(message.target_version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RollbackDescriptorRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRollbackDescriptorRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service_name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.target_version = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RollbackDescriptorRequest {
    return {
      service_name: isSet(object.service_name) ? globalThis.String(object.service_name) : "",
      target_version: isSet(object.target_version) ? globalThis.String(object.target_version) : "",
    };
  },

  toJSON(message: RollbackDescriptorRequest): unknown {
    const obj: any = {};
    if (message.service_name !== "") {
      obj.service_name = message.service_name;
    }
    if (message.target_version !== "") {
      obj.target_version = message.target_version;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RollbackDescriptorRequest>, I>>(base?: I): RollbackDescriptorRequest {
    return RollbackDescriptorRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RollbackDescriptorRequest>, I>>(object: I): RollbackDescriptorRequest {
    const message = createBaseRollbackDescriptorRequest();
    message.service_name = object.service_name ?? "";
    message.target_version = object.target_version ?? "";
    return message;
  },
};

function createBaseRollbackDescriptorResponse(): RollbackDescriptorResponse {
  return { success: false, message: "", active_version: "", discovered_services: [] };
}

export const RollbackDescriptorResponse: MessageFns<RollbackDescriptorResponse> = {
  encode(message: RollbackDescriptorResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.active_version !== "") {
      writer.uint32(26).string(message.active_version);
    }
    for (const v of message.discovered_services) {
      writer.uint32(34).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RollbackDescriptorResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRollbackDescriptorResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.active_version = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.discovered_services.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RollbackDescriptorResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      active_version: isSet(object.active_version) ? globalThis.String(object.active_version) : "",
      discovered_services: globalThis.Array.isArray(object?.discovered_services)
        ? object.discovered_services.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: RollbackDescriptorResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.active_version !== "") {
      obj.active_version = message.active_version;
    }
    if (message.discovered_services?.length) {
      obj.discovered_services = message.discovered_services;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RollbackDescriptorResponse>, I>>(base?: I): RollbackDescriptorResponse {
    return RollbackDescriptorResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RollbackDescriptorResponse>, I>>(object: I): RollbackDescriptorResponse {
    const message = createBaseRollbackDescriptorResponse();
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    message.active_version = object.active_version ?? "";
    message.discovered_services = object.discovered_services?.map((e) => e) || [];
    return message;
  },
};

function createBaseListDescriptorVersionsRequest(): ListDescriptorVersionsRequest {
  return { service_name: "" };
}

export const ListDescriptorVersionsRequest: MessageFns<ListDescriptorVersionsRequest> = {
  encode(message: ListDescriptorVersionsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service_name !== "") {
      writer.uint32(10).string(message.service_name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListDescriptorVersionsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListDescriptorVersionsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service_name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListDescriptorVersionsRequest {
    return { service_name: isSet(object.service_name) ? globalThis.String(object.service_name) : "" };
  },

  toJSON(message: ListDescriptorVersionsRequest): unknown {
    const obj: any = {};
    if (message.service_name !== "") {
      obj.service_name = message.service_name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListDescriptorVersionsRequest>, I>>(base?: I): ListDescriptorVersionsRequest {
    return ListDescriptorVersionsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListDescriptorVersionsRequest>, I>>(
    object: I,
  ): ListDescriptorVersionsRequest {
    const message = createBaseListDescriptorVersionsRequest();
    message.service_name = object.service_name ?? "";
    return message;
  },
};

function createBaseListDescriptorVersionsResponse(): ListDescriptorVersionsResponse {
  return { versions: [], active_version: "" };
}

export const ListDescriptorVersionsResponse: MessageFns<ListDescriptorVersionsResponse> = {
  encode(message: ListDescriptorVersionsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.versions) {
      DescriptorVersionInfo.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.active_version !== "") {
      writer.uint32(18).string(message.active_version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListDescriptorVersionsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListDescriptorVersionsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.versions.push(DescriptorVersionInfo.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.active_version = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListDescriptorVersionsResponse {
    return {
      versions: globalThis.Array.isArray(object?.versions)
        ? object.versions.map((e: any) => DescriptorVersionInfo.fromJSON(e))
        : [],
      active_version: isSet(object.active_version) ? globalThis.String(object.active_version) : "",
    };
  },

  toJSON(message: ListDescriptorVersionsResponse): unknown {
    const obj: any = {};
    if (message.versions?.length) {
      obj.versions = message.versions.map((e) => DescriptorVersionInfo.toJSON(e));
    }
    if (message.active_version !== "") {
      obj.active_version = message.active_version;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListDescriptorVersionsResponse>, I>>(base?: I): ListDescriptorVersionsResponse {
    return ListDescriptorVersionsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListDescriptorVersionsResponse>, I>>(
    object: I,
  ): ListDescriptorVersionsResponse {
    const message = createBaseListDescriptorVersionsResponse();
    message.versions = object.versions?.map((e) => DescriptorVersionInfo.fromPartial(e)) || [];
    message.active_version = object.active_version ?? "";
    return message;
  },
};

/** 服务注册和发现服务 */
export interface ServiceDiscoveryService {
  /** 初始化服务（管理端专属，生成 app_id + app_secret） */
  InitService(request: InitServiceRequest): Promise<InitServiceResponse>;
  /** 注册服务实例 */
  RegisterService(request: RegisterServiceRequest): Promise<RegisterServiceResponse>;
  /** 业务侧追加单个 endpoint（service-scoped） */
  RegisterServiceEndpoint(request: RegisterServiceEndpointRequest): Promise<RegisterServiceEndpointResponse>;
  /** 业务侧注销单个 endpoint（service-scoped） */
  DeregisterServiceEndpoint(request: DeregisterServiceEndpointRequest): Promise<DeregisterServiceEndpointResponse>;
  /** 注销服务实例 */
  DeregisterService(request: DeregisterServiceRequest): Promise<DeregisterServiceResponse>;
  /** 删除持久化服务记录 */
  DeleteServiceRecord(request: DeleteServiceRecordRequest): Promise<DeleteServiceRecordResponse>;
  /** 更新服务实例 */
  UpdateServiceInstance(request: UpdateServiceInstanceRequest): Promise<ServiceInstance>;
  /** 获取服务实例列表 */
  GetServiceInstances(request: GetServiceInstancesRequest): Promise<GetServiceInstancesResponse>;
  /** 列出所有服务 */
  ListServices(request: ListServicesRequest): Promise<ListServicesResponse>;
  /** 更新服务健康状态 */
  UpdateServiceHealth(request: UpdateServiceHealthRequest): Promise<UpdateServiceHealthResponse>;
  /** 服务健康检查 */
  ServiceHealthCheck(request: ServiceHealthCheckRequest): Promise<ServiceHealthCheckResponse>;
  /** 上传服务配置 */
  UploadServiceConfig(request: UploadServiceConfigRequest): Promise<UploadServiceConfigResponse>;
  /** 获取服务配置 */
  GetServiceConfig(request: GetServiceConfigRequest): Promise<GetServiceConfigResponse>;
  /** 上传 protobuf 描述符 */
  UploadProtobufDescriptor(request: UploadProtobufDescriptorRequest): Promise<UploadProtobufDescriptorResponse>;
  /** 获取 protobuf 描述符 */
  GetProtobufDescriptor(request: GetProtobufDescriptorRequest): Promise<GetProtobufDescriptorResponse>;
  /** 列出可用的 protobuf 描述符 */
  ListProtobufDescriptors(request: ListProtobufDescriptorsRequest): Promise<ListProtobufDescriptorsResponse>;
  /** 回滚描述符到指定版本 */
  RollbackDescriptor(request: RollbackDescriptorRequest): Promise<RollbackDescriptorResponse>;
  /** 列出描述符版本历史 */
  ListDescriptorVersions(request: ListDescriptorVersionsRequest): Promise<ListDescriptorVersionsResponse>;
}

export const ServiceDiscoveryServiceServiceName = "stew.api.v1.ServiceDiscoveryService";
export class ServiceDiscoveryServiceClientImpl implements ServiceDiscoveryService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || ServiceDiscoveryServiceServiceName;
    this.rpc = rpc;
    this.InitService = this.InitService.bind(this);
    this.RegisterService = this.RegisterService.bind(this);
    this.RegisterServiceEndpoint = this.RegisterServiceEndpoint.bind(this);
    this.DeregisterServiceEndpoint = this.DeregisterServiceEndpoint.bind(this);
    this.DeregisterService = this.DeregisterService.bind(this);
    this.DeleteServiceRecord = this.DeleteServiceRecord.bind(this);
    this.UpdateServiceInstance = this.UpdateServiceInstance.bind(this);
    this.GetServiceInstances = this.GetServiceInstances.bind(this);
    this.ListServices = this.ListServices.bind(this);
    this.UpdateServiceHealth = this.UpdateServiceHealth.bind(this);
    this.ServiceHealthCheck = this.ServiceHealthCheck.bind(this);
    this.UploadServiceConfig = this.UploadServiceConfig.bind(this);
    this.GetServiceConfig = this.GetServiceConfig.bind(this);
    this.UploadProtobufDescriptor = this.UploadProtobufDescriptor.bind(this);
    this.GetProtobufDescriptor = this.GetProtobufDescriptor.bind(this);
    this.ListProtobufDescriptors = this.ListProtobufDescriptors.bind(this);
    this.RollbackDescriptor = this.RollbackDescriptor.bind(this);
    this.ListDescriptorVersions = this.ListDescriptorVersions.bind(this);
  }
  InitService(request: InitServiceRequest): Promise<InitServiceResponse> {
    const data = InitServiceRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "InitService", data);
    return promise.then((data) => InitServiceResponse.decode(new BinaryReader(data)));
  }

  RegisterService(request: RegisterServiceRequest): Promise<RegisterServiceResponse> {
    const data = RegisterServiceRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "RegisterService", data);
    return promise.then((data) => RegisterServiceResponse.decode(new BinaryReader(data)));
  }

  RegisterServiceEndpoint(request: RegisterServiceEndpointRequest): Promise<RegisterServiceEndpointResponse> {
    const data = RegisterServiceEndpointRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "RegisterServiceEndpoint", data);
    return promise.then((data) => RegisterServiceEndpointResponse.decode(new BinaryReader(data)));
  }

  DeregisterServiceEndpoint(request: DeregisterServiceEndpointRequest): Promise<DeregisterServiceEndpointResponse> {
    const data = DeregisterServiceEndpointRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "DeregisterServiceEndpoint", data);
    return promise.then((data) => DeregisterServiceEndpointResponse.decode(new BinaryReader(data)));
  }

  DeregisterService(request: DeregisterServiceRequest): Promise<DeregisterServiceResponse> {
    const data = DeregisterServiceRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "DeregisterService", data);
    return promise.then((data) => DeregisterServiceResponse.decode(new BinaryReader(data)));
  }

  DeleteServiceRecord(request: DeleteServiceRecordRequest): Promise<DeleteServiceRecordResponse> {
    const data = DeleteServiceRecordRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "DeleteServiceRecord", data);
    return promise.then((data) => DeleteServiceRecordResponse.decode(new BinaryReader(data)));
  }

  UpdateServiceInstance(request: UpdateServiceInstanceRequest): Promise<ServiceInstance> {
    const data = UpdateServiceInstanceRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "UpdateServiceInstance", data);
    return promise.then((data) => ServiceInstance.decode(new BinaryReader(data)));
  }

  GetServiceInstances(request: GetServiceInstancesRequest): Promise<GetServiceInstancesResponse> {
    const data = GetServiceInstancesRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetServiceInstances", data);
    return promise.then((data) => GetServiceInstancesResponse.decode(new BinaryReader(data)));
  }

  ListServices(request: ListServicesRequest): Promise<ListServicesResponse> {
    const data = ListServicesRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ListServices", data);
    return promise.then((data) => ListServicesResponse.decode(new BinaryReader(data)));
  }

  UpdateServiceHealth(request: UpdateServiceHealthRequest): Promise<UpdateServiceHealthResponse> {
    const data = UpdateServiceHealthRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "UpdateServiceHealth", data);
    return promise.then((data) => UpdateServiceHealthResponse.decode(new BinaryReader(data)));
  }

  ServiceHealthCheck(request: ServiceHealthCheckRequest): Promise<ServiceHealthCheckResponse> {
    const data = ServiceHealthCheckRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ServiceHealthCheck", data);
    return promise.then((data) => ServiceHealthCheckResponse.decode(new BinaryReader(data)));
  }

  UploadServiceConfig(request: UploadServiceConfigRequest): Promise<UploadServiceConfigResponse> {
    const data = UploadServiceConfigRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "UploadServiceConfig", data);
    return promise.then((data) => UploadServiceConfigResponse.decode(new BinaryReader(data)));
  }

  GetServiceConfig(request: GetServiceConfigRequest): Promise<GetServiceConfigResponse> {
    const data = GetServiceConfigRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetServiceConfig", data);
    return promise.then((data) => GetServiceConfigResponse.decode(new BinaryReader(data)));
  }

  UploadProtobufDescriptor(request: UploadProtobufDescriptorRequest): Promise<UploadProtobufDescriptorResponse> {
    const data = UploadProtobufDescriptorRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "UploadProtobufDescriptor", data);
    return promise.then((data) => UploadProtobufDescriptorResponse.decode(new BinaryReader(data)));
  }

  GetProtobufDescriptor(request: GetProtobufDescriptorRequest): Promise<GetProtobufDescriptorResponse> {
    const data = GetProtobufDescriptorRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetProtobufDescriptor", data);
    return promise.then((data) => GetProtobufDescriptorResponse.decode(new BinaryReader(data)));
  }

  ListProtobufDescriptors(request: ListProtobufDescriptorsRequest): Promise<ListProtobufDescriptorsResponse> {
    const data = ListProtobufDescriptorsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ListProtobufDescriptors", data);
    return promise.then((data) => ListProtobufDescriptorsResponse.decode(new BinaryReader(data)));
  }

  RollbackDescriptor(request: RollbackDescriptorRequest): Promise<RollbackDescriptorResponse> {
    const data = RollbackDescriptorRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "RollbackDescriptor", data);
    return promise.then((data) => RollbackDescriptorResponse.decode(new BinaryReader(data)));
  }

  ListDescriptorVersions(request: ListDescriptorVersionsRequest): Promise<ListDescriptorVersionsResponse> {
    const data = ListDescriptorVersionsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ListDescriptorVersions", data);
    return promise.then((data) => ListDescriptorVersionsResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
