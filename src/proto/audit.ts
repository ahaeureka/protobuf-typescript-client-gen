// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v3.21.12
// source: audit.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "./google/protobuf/timestamp";

export const protobufPackage = "stew.api.v1";

/** 审计日志条目 */
export interface AuditLogEntry {
  id: string;
  trace_id: string;
  user_id: string;
  api_key_id: string;
  session_id: string;
  action: string;
  resource: string;
  domain: string;
  ip_address: string;
  user_agent: string;
  success: boolean;
  decision_source: string;
  reason: string;
  error_message: string;
  request_size: number;
  response_size: number;
  duration_ms: number;
  metadata: { [key: string]: string };
  created_at: Date | undefined;
}

export interface AuditLogEntry_MetadataEntry {
  key: string;
  value: string;
}

/** 获取审计日志请求 */
export interface GetAuditLogsRequest {
  trace_id: string;
  user_id: string;
  api_key_id: string;
  session_id: string;
  action: string;
  resource: string;
  domain: string;
  success_filter: number;
  decision_source: string;
  start_time: Date | undefined;
  end_time: Date | undefined;
  page: number;
  limit: number;
}

/** 获取审计日志响应 */
export interface GetAuditLogsResponse {
  logs: AuditLogEntry[];
  total: number;
  page: number;
  limit: number;
}

/** 获取审计统计请求 */
export interface GetAuditStatisticsRequest {
  start_time: Date | undefined;
  end_time: Date | undefined;
}

/** 动作统计 */
export interface ActionStatistic {
  action: string;
  count: number;
}

/** 获取审计统计响应 */
export interface GetAuditStatisticsResponse {
  total_count: number;
  success_count: number;
  failure_count: number;
  action_stats: ActionStatistic[];
}

function createBaseAuditLogEntry(): AuditLogEntry {
  return {
    id: "",
    trace_id: "",
    user_id: "",
    api_key_id: "",
    session_id: "",
    action: "",
    resource: "",
    domain: "",
    ip_address: "",
    user_agent: "",
    success: false,
    decision_source: "",
    reason: "",
    error_message: "",
    request_size: 0,
    response_size: 0,
    duration_ms: 0,
    metadata: {},
    created_at: undefined,
  };
}

export const AuditLogEntry: MessageFns<AuditLogEntry> = {
  encode(message: AuditLogEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.trace_id !== "") {
      writer.uint32(18).string(message.trace_id);
    }
    if (message.user_id !== "") {
      writer.uint32(26).string(message.user_id);
    }
    if (message.api_key_id !== "") {
      writer.uint32(34).string(message.api_key_id);
    }
    if (message.session_id !== "") {
      writer.uint32(42).string(message.session_id);
    }
    if (message.action !== "") {
      writer.uint32(50).string(message.action);
    }
    if (message.resource !== "") {
      writer.uint32(58).string(message.resource);
    }
    if (message.domain !== "") {
      writer.uint32(66).string(message.domain);
    }
    if (message.ip_address !== "") {
      writer.uint32(74).string(message.ip_address);
    }
    if (message.user_agent !== "") {
      writer.uint32(82).string(message.user_agent);
    }
    if (message.success !== false) {
      writer.uint32(88).bool(message.success);
    }
    if (message.decision_source !== "") {
      writer.uint32(98).string(message.decision_source);
    }
    if (message.reason !== "") {
      writer.uint32(106).string(message.reason);
    }
    if (message.error_message !== "") {
      writer.uint32(114).string(message.error_message);
    }
    if (message.request_size !== 0) {
      writer.uint32(120).int64(message.request_size);
    }
    if (message.response_size !== 0) {
      writer.uint32(128).int64(message.response_size);
    }
    if (message.duration_ms !== 0) {
      writer.uint32(136).int64(message.duration_ms);
    }
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, string]) => {
      AuditLogEntry_MetadataEntry.encode({ key: key as any, value }, writer.uint32(146).fork()).join();
    });
    if (message.created_at !== undefined) {
      Timestamp.encode(toTimestamp(message.created_at), writer.uint32(154).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuditLogEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuditLogEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.trace_id = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.user_id = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.api_key_id = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.session_id = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.action = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.resource = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.ip_address = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.user_agent = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.decision_source = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.error_message = reader.string();
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.request_size = longToNumber(reader.int64());
          continue;
        }
        case 16: {
          if (tag !== 128) {
            break;
          }

          message.response_size = longToNumber(reader.int64());
          continue;
        }
        case 17: {
          if (tag !== 136) {
            break;
          }

          message.duration_ms = longToNumber(reader.int64());
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          const entry18 = AuditLogEntry_MetadataEntry.decode(reader, reader.uint32());
          if (entry18.value !== undefined) {
            message.metadata[entry18.key] = entry18.value;
          }
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.created_at = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuditLogEntry {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      trace_id: isSet(object.trace_id) ? globalThis.String(object.trace_id) : "",
      user_id: isSet(object.user_id) ? globalThis.String(object.user_id) : "",
      api_key_id: isSet(object.api_key_id) ? globalThis.String(object.api_key_id) : "",
      session_id: isSet(object.session_id) ? globalThis.String(object.session_id) : "",
      action: isSet(object.action) ? globalThis.String(object.action) : "",
      resource: isSet(object.resource) ? globalThis.String(object.resource) : "",
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      ip_address: isSet(object.ip_address) ? globalThis.String(object.ip_address) : "",
      user_agent: isSet(object.user_agent) ? globalThis.String(object.user_agent) : "",
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      decision_source: isSet(object.decision_source) ? globalThis.String(object.decision_source) : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      error_message: isSet(object.error_message) ? globalThis.String(object.error_message) : "",
      request_size: isSet(object.request_size) ? globalThis.Number(object.request_size) : 0,
      response_size: isSet(object.response_size) ? globalThis.Number(object.response_size) : 0,
      duration_ms: isSet(object.duration_ms) ? globalThis.Number(object.duration_ms) : 0,
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      created_at: isSet(object.created_at) ? fromJsonTimestamp(object.created_at) : undefined,
    };
  },

  toJSON(message: AuditLogEntry): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.trace_id !== "") {
      obj.trace_id = message.trace_id;
    }
    if (message.user_id !== "") {
      obj.user_id = message.user_id;
    }
    if (message.api_key_id !== "") {
      obj.api_key_id = message.api_key_id;
    }
    if (message.session_id !== "") {
      obj.session_id = message.session_id;
    }
    if (message.action !== "") {
      obj.action = message.action;
    }
    if (message.resource !== "") {
      obj.resource = message.resource;
    }
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.ip_address !== "") {
      obj.ip_address = message.ip_address;
    }
    if (message.user_agent !== "") {
      obj.user_agent = message.user_agent;
    }
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.decision_source !== "") {
      obj.decision_source = message.decision_source;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.error_message !== "") {
      obj.error_message = message.error_message;
    }
    if (message.request_size !== 0) {
      obj.request_size = Math.round(message.request_size);
    }
    if (message.response_size !== 0) {
      obj.response_size = Math.round(message.response_size);
    }
    if (message.duration_ms !== 0) {
      obj.duration_ms = Math.round(message.duration_ms);
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    if (message.created_at !== undefined) {
      obj.created_at = message.created_at.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AuditLogEntry>, I>>(base?: I): AuditLogEntry {
    return AuditLogEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AuditLogEntry>, I>>(object: I): AuditLogEntry {
    const message = createBaseAuditLogEntry();
    message.id = object.id ?? "";
    message.trace_id = object.trace_id ?? "";
    message.user_id = object.user_id ?? "";
    message.api_key_id = object.api_key_id ?? "";
    message.session_id = object.session_id ?? "";
    message.action = object.action ?? "";
    message.resource = object.resource ?? "";
    message.domain = object.domain ?? "";
    message.ip_address = object.ip_address ?? "";
    message.user_agent = object.user_agent ?? "";
    message.success = object.success ?? false;
    message.decision_source = object.decision_source ?? "";
    message.reason = object.reason ?? "";
    message.error_message = object.error_message ?? "";
    message.request_size = object.request_size ?? 0;
    message.response_size = object.response_size ?? 0;
    message.duration_ms = object.duration_ms ?? 0;
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.created_at = object.created_at ?? undefined;
    return message;
  },
};

function createBaseAuditLogEntry_MetadataEntry(): AuditLogEntry_MetadataEntry {
  return { key: "", value: "" };
}

export const AuditLogEntry_MetadataEntry: MessageFns<AuditLogEntry_MetadataEntry> = {
  encode(message: AuditLogEntry_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuditLogEntry_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuditLogEntry_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuditLogEntry_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: AuditLogEntry_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AuditLogEntry_MetadataEntry>, I>>(base?: I): AuditLogEntry_MetadataEntry {
    return AuditLogEntry_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AuditLogEntry_MetadataEntry>, I>>(object: I): AuditLogEntry_MetadataEntry {
    const message = createBaseAuditLogEntry_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseGetAuditLogsRequest(): GetAuditLogsRequest {
  return {
    trace_id: "",
    user_id: "",
    api_key_id: "",
    session_id: "",
    action: "",
    resource: "",
    domain: "",
    success_filter: 0,
    decision_source: "",
    start_time: undefined,
    end_time: undefined,
    page: 0,
    limit: 0,
  };
}

export const GetAuditLogsRequest: MessageFns<GetAuditLogsRequest> = {
  encode(message: GetAuditLogsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.trace_id !== "") {
      writer.uint32(10).string(message.trace_id);
    }
    if (message.user_id !== "") {
      writer.uint32(18).string(message.user_id);
    }
    if (message.api_key_id !== "") {
      writer.uint32(26).string(message.api_key_id);
    }
    if (message.session_id !== "") {
      writer.uint32(34).string(message.session_id);
    }
    if (message.action !== "") {
      writer.uint32(42).string(message.action);
    }
    if (message.resource !== "") {
      writer.uint32(50).string(message.resource);
    }
    if (message.domain !== "") {
      writer.uint32(58).string(message.domain);
    }
    if (message.success_filter !== 0) {
      writer.uint32(64).int32(message.success_filter);
    }
    if (message.decision_source !== "") {
      writer.uint32(74).string(message.decision_source);
    }
    if (message.start_time !== undefined) {
      Timestamp.encode(toTimestamp(message.start_time), writer.uint32(82).fork()).join();
    }
    if (message.end_time !== undefined) {
      Timestamp.encode(toTimestamp(message.end_time), writer.uint32(90).fork()).join();
    }
    if (message.page !== 0) {
      writer.uint32(96).int32(message.page);
    }
    if (message.limit !== 0) {
      writer.uint32(104).int32(message.limit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetAuditLogsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetAuditLogsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.trace_id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.user_id = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.api_key_id = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.session_id = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.action = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.resource = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.success_filter = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.decision_source = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.start_time = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.end_time = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.page = reader.int32();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetAuditLogsRequest {
    return {
      trace_id: isSet(object.trace_id) ? globalThis.String(object.trace_id) : "",
      user_id: isSet(object.user_id) ? globalThis.String(object.user_id) : "",
      api_key_id: isSet(object.api_key_id) ? globalThis.String(object.api_key_id) : "",
      session_id: isSet(object.session_id) ? globalThis.String(object.session_id) : "",
      action: isSet(object.action) ? globalThis.String(object.action) : "",
      resource: isSet(object.resource) ? globalThis.String(object.resource) : "",
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      success_filter: isSet(object.success_filter) ? globalThis.Number(object.success_filter) : 0,
      decision_source: isSet(object.decision_source) ? globalThis.String(object.decision_source) : "",
      start_time: isSet(object.start_time) ? fromJsonTimestamp(object.start_time) : undefined,
      end_time: isSet(object.end_time) ? fromJsonTimestamp(object.end_time) : undefined,
      page: isSet(object.page) ? globalThis.Number(object.page) : 0,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
    };
  },

  toJSON(message: GetAuditLogsRequest): unknown {
    const obj: any = {};
    if (message.trace_id !== "") {
      obj.trace_id = message.trace_id;
    }
    if (message.user_id !== "") {
      obj.user_id = message.user_id;
    }
    if (message.api_key_id !== "") {
      obj.api_key_id = message.api_key_id;
    }
    if (message.session_id !== "") {
      obj.session_id = message.session_id;
    }
    if (message.action !== "") {
      obj.action = message.action;
    }
    if (message.resource !== "") {
      obj.resource = message.resource;
    }
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.success_filter !== 0) {
      obj.success_filter = Math.round(message.success_filter);
    }
    if (message.decision_source !== "") {
      obj.decision_source = message.decision_source;
    }
    if (message.start_time !== undefined) {
      obj.start_time = message.start_time.toISOString();
    }
    if (message.end_time !== undefined) {
      obj.end_time = message.end_time.toISOString();
    }
    if (message.page !== 0) {
      obj.page = Math.round(message.page);
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetAuditLogsRequest>, I>>(base?: I): GetAuditLogsRequest {
    return GetAuditLogsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetAuditLogsRequest>, I>>(object: I): GetAuditLogsRequest {
    const message = createBaseGetAuditLogsRequest();
    message.trace_id = object.trace_id ?? "";
    message.user_id = object.user_id ?? "";
    message.api_key_id = object.api_key_id ?? "";
    message.session_id = object.session_id ?? "";
    message.action = object.action ?? "";
    message.resource = object.resource ?? "";
    message.domain = object.domain ?? "";
    message.success_filter = object.success_filter ?? 0;
    message.decision_source = object.decision_source ?? "";
    message.start_time = object.start_time ?? undefined;
    message.end_time = object.end_time ?? undefined;
    message.page = object.page ?? 0;
    message.limit = object.limit ?? 0;
    return message;
  },
};

function createBaseGetAuditLogsResponse(): GetAuditLogsResponse {
  return { logs: [], total: 0, page: 0, limit: 0 };
}

export const GetAuditLogsResponse: MessageFns<GetAuditLogsResponse> = {
  encode(message: GetAuditLogsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.logs) {
      AuditLogEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.total !== 0) {
      writer.uint32(16).int32(message.total);
    }
    if (message.page !== 0) {
      writer.uint32(24).int32(message.page);
    }
    if (message.limit !== 0) {
      writer.uint32(32).int32(message.limit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetAuditLogsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetAuditLogsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.logs.push(AuditLogEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.total = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.page = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetAuditLogsResponse {
    return {
      logs: globalThis.Array.isArray(object?.logs) ? object.logs.map((e: any) => AuditLogEntry.fromJSON(e)) : [],
      total: isSet(object.total) ? globalThis.Number(object.total) : 0,
      page: isSet(object.page) ? globalThis.Number(object.page) : 0,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
    };
  },

  toJSON(message: GetAuditLogsResponse): unknown {
    const obj: any = {};
    if (message.logs?.length) {
      obj.logs = message.logs.map((e) => AuditLogEntry.toJSON(e));
    }
    if (message.total !== 0) {
      obj.total = Math.round(message.total);
    }
    if (message.page !== 0) {
      obj.page = Math.round(message.page);
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetAuditLogsResponse>, I>>(base?: I): GetAuditLogsResponse {
    return GetAuditLogsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetAuditLogsResponse>, I>>(object: I): GetAuditLogsResponse {
    const message = createBaseGetAuditLogsResponse();
    message.logs = object.logs?.map((e) => AuditLogEntry.fromPartial(e)) || [];
    message.total = object.total ?? 0;
    message.page = object.page ?? 0;
    message.limit = object.limit ?? 0;
    return message;
  },
};

function createBaseGetAuditStatisticsRequest(): GetAuditStatisticsRequest {
  return { start_time: undefined, end_time: undefined };
}

export const GetAuditStatisticsRequest: MessageFns<GetAuditStatisticsRequest> = {
  encode(message: GetAuditStatisticsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.start_time !== undefined) {
      Timestamp.encode(toTimestamp(message.start_time), writer.uint32(10).fork()).join();
    }
    if (message.end_time !== undefined) {
      Timestamp.encode(toTimestamp(message.end_time), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetAuditStatisticsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetAuditStatisticsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.start_time = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.end_time = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetAuditStatisticsRequest {
    return {
      start_time: isSet(object.start_time) ? fromJsonTimestamp(object.start_time) : undefined,
      end_time: isSet(object.end_time) ? fromJsonTimestamp(object.end_time) : undefined,
    };
  },

  toJSON(message: GetAuditStatisticsRequest): unknown {
    const obj: any = {};
    if (message.start_time !== undefined) {
      obj.start_time = message.start_time.toISOString();
    }
    if (message.end_time !== undefined) {
      obj.end_time = message.end_time.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetAuditStatisticsRequest>, I>>(base?: I): GetAuditStatisticsRequest {
    return GetAuditStatisticsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetAuditStatisticsRequest>, I>>(object: I): GetAuditStatisticsRequest {
    const message = createBaseGetAuditStatisticsRequest();
    message.start_time = object.start_time ?? undefined;
    message.end_time = object.end_time ?? undefined;
    return message;
  },
};

function createBaseActionStatistic(): ActionStatistic {
  return { action: "", count: 0 };
}

export const ActionStatistic: MessageFns<ActionStatistic> = {
  encode(message: ActionStatistic, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.action !== "") {
      writer.uint32(10).string(message.action);
    }
    if (message.count !== 0) {
      writer.uint32(16).int64(message.count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ActionStatistic {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseActionStatistic();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.action = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.count = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ActionStatistic {
    return {
      action: isSet(object.action) ? globalThis.String(object.action) : "",
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
    };
  },

  toJSON(message: ActionStatistic): unknown {
    const obj: any = {};
    if (message.action !== "") {
      obj.action = message.action;
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ActionStatistic>, I>>(base?: I): ActionStatistic {
    return ActionStatistic.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ActionStatistic>, I>>(object: I): ActionStatistic {
    const message = createBaseActionStatistic();
    message.action = object.action ?? "";
    message.count = object.count ?? 0;
    return message;
  },
};

function createBaseGetAuditStatisticsResponse(): GetAuditStatisticsResponse {
  return { total_count: 0, success_count: 0, failure_count: 0, action_stats: [] };
}

export const GetAuditStatisticsResponse: MessageFns<GetAuditStatisticsResponse> = {
  encode(message: GetAuditStatisticsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.total_count !== 0) {
      writer.uint32(8).int64(message.total_count);
    }
    if (message.success_count !== 0) {
      writer.uint32(16).int64(message.success_count);
    }
    if (message.failure_count !== 0) {
      writer.uint32(24).int64(message.failure_count);
    }
    for (const v of message.action_stats) {
      ActionStatistic.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetAuditStatisticsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetAuditStatisticsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.total_count = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.success_count = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.failure_count = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.action_stats.push(ActionStatistic.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetAuditStatisticsResponse {
    return {
      total_count: isSet(object.total_count) ? globalThis.Number(object.total_count) : 0,
      success_count: isSet(object.success_count) ? globalThis.Number(object.success_count) : 0,
      failure_count: isSet(object.failure_count) ? globalThis.Number(object.failure_count) : 0,
      action_stats: globalThis.Array.isArray(object?.action_stats)
        ? object.action_stats.map((e: any) => ActionStatistic.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetAuditStatisticsResponse): unknown {
    const obj: any = {};
    if (message.total_count !== 0) {
      obj.total_count = Math.round(message.total_count);
    }
    if (message.success_count !== 0) {
      obj.success_count = Math.round(message.success_count);
    }
    if (message.failure_count !== 0) {
      obj.failure_count = Math.round(message.failure_count);
    }
    if (message.action_stats?.length) {
      obj.action_stats = message.action_stats.map((e) => ActionStatistic.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetAuditStatisticsResponse>, I>>(base?: I): GetAuditStatisticsResponse {
    return GetAuditStatisticsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetAuditStatisticsResponse>, I>>(object: I): GetAuditStatisticsResponse {
    const message = createBaseGetAuditStatisticsResponse();
    message.total_count = object.total_count ?? 0;
    message.success_count = object.success_count ?? 0;
    message.failure_count = object.failure_count ?? 0;
    message.action_stats = object.action_stats?.map((e) => ActionStatistic.fromPartial(e)) || [];
    return message;
  },
};

/** 审计日志服务 */
export interface AuditService {
  /** 获取审计日志 */
  GetAuditLogs(request: GetAuditLogsRequest): Promise<GetAuditLogsResponse>;
  /** 获取审计统计信息 */
  GetAuditStatistics(request: GetAuditStatisticsRequest): Promise<GetAuditStatisticsResponse>;
}

export const AuditServiceServiceName = "stew.api.v1.AuditService";
export class AuditServiceClientImpl implements AuditService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || AuditServiceServiceName;
    this.rpc = rpc;
    this.GetAuditLogs = this.GetAuditLogs.bind(this);
    this.GetAuditStatistics = this.GetAuditStatistics.bind(this);
  }
  GetAuditLogs(request: GetAuditLogsRequest): Promise<GetAuditLogsResponse> {
    const data = GetAuditLogsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetAuditLogs", data);
    return promise.then((data) => GetAuditLogsResponse.decode(new BinaryReader(data)));
  }

  GetAuditStatistics(request: GetAuditStatisticsRequest): Promise<GetAuditStatisticsResponse> {
    const data = GetAuditStatisticsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetAuditStatistics", data);
    return promise.then((data) => GetAuditStatisticsResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
